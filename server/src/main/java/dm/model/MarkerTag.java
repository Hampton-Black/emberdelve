package dm.model;

/**
 * Closed tags a reconcile-placed marker may wear. The glyph and the pick show this;
 * the free-form text never reaches the renderer. Spec §8f, invariant #7.
 */
public enum MarkerTag {
    SCORCH,
    SIGIL,
    TRACKS
}
