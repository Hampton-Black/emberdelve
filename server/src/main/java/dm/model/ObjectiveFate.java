package dm.model;

/**
 * What happened to the objective when the delve ended. Closed, descriptive.
 * Spec §9.
 */
public enum ObjectiveFate {
    CARRIED_OUT,
    LEFT_WHERE_IT_LAY,
    FELL_WITH_HIM
}
