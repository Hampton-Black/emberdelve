{"_type":"issue","id":"emberdelve-4h9.15","title":"Play the M4 gate and write m4-evaluation.md","description":"Play the M4 gate and write `docs/milestones/m4-evaluation.md`.\n\n**Can you lose a delve, and does losing it sting?**\n\nStated as something to feel rather than to measure, in the same spirit as M0's. §9: \"you should be\nable to stand in front of a door at 5 hit points and genuinely not know whether to open it. If you\ncan, the loop exists. If you cannot, no amount of narration quality substitutes.\"\n\nEvery prior milestone has one of these and they are the most valuable documents in the repo —\n`m3-evaluation.md` §8 is what made M4 plannable at all. The workflow M2 built and M3 used applies:\nevery session writes itself to `server/sessions/`, so when something goes wrong in play the file\nthat proves it is already on disk. Copy the session worth keeping into `docs/evidence/` and, if it\nearns it, into the suite as a replayed fixture.\n\nWhat this gate has to answer that the others did not:\n\n- Whether attrition actually bites, or whether the numbers make the door decision a formality in\n  either direction.\n- Whether the clock is felt. It is deliberately diegetic and unlabelled — the shrinking light, and\n  signs for the alert clock. If the player never noticed a clock was running, that is the finding.\n- Whether losing stings when nothing persists. The sting is meant to come from within the session:\n  what was spent, how far you got, what you left behind. This is the assumption the milestone rests\n  on and it may not hold — and if it does not, that is a better finding than a pass.\n- Whether the DM's improvisation and the board agree more often than they did. Risk 3 is the one\n  M4 attacks from two sides, with the engine-owned objective and with markers.\n\nCarry forward what did not get exercised, the way M3 did with the room-qualified `PropRef`.","acceptance_criteria":"A played session, its log copied into docs/evidence/, and docs/milestones/m4-evaluation.md with a verdict on the gate question and a section 8 listing what M4 hands forward. Findings that were not exercised are named as such.","status":"open","priority":1,"issue_type":"task","owner":"hampton.j.black@gmail.com","created_at":"2026-09-08T23:25:24Z","created_by":"hampton.black","updated_at":"2026-09-08T23:27:32Z","dependencies":[{"issue_id":"emberdelve-4h9.15","depends_on_id":"emberdelve-4h9","type":"parent-child","created_at":"2026-09-08T18:25:24Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.15","depends_on_id":"emberdelve-4h9.6","type":"blocks","created_at":"2026-09-08T18:25:47Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.15","depends_on_id":"emberdelve-4h9.11","type":"blocks","created_at":"2026-09-08T18:25:48Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.15","depends_on_id":"emberdelve-4h9.9","type":"blocks","created_at":"2026-09-08T18:25:48Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.15","depends_on_id":"emberdelve-4h9.13","type":"blocks","created_at":"2026-09-08T18:25:50Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.15","depends_on_id":"emberdelve-4h9.5","type":"blocks","created_at":"2026-09-08T18:25:47Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.15","depends_on_id":"emberdelve-4h9.12","type":"blocks","created_at":"2026-09-08T18:25:49Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.15","depends_on_id":"emberdelve-4h9.2","type":"blocks","created_at":"2026-09-08T18:25:46Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.15","depends_on_id":"emberdelve-4h9.14","type":"blocks","created_at":"2026-09-08T18:25:50Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":8,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-ffi.1","title":"What the projection carries about hurt, clocks and the objective","description":"## Question\n\nWhat does the world-state projection carry into the DM prompt about hurt, clocks, the objective and\nconsumables — and in what form?\n\n§15 names this as the expensive part of M4: \"the projection work that carries hurt, clock and\nobjective state across a room boundary as fiction, which is the same problem M3 is already inside.\"\nNothing else in the epic owns it.\n\n## Where it lives\n\n`DmService.java:842` and around it. The projection is a hand-built markdown block: `## The fight`,\n`## Momentum`, `## Established`, `## Ways out`, `## Grid`. It never truncates (that is the whole\npoint of it against the 6-turn transcript window), so anything added here is in every prompt for\nthe rest of the session.\n\n## The precedents disagree, and that is the question\n\n- `## Momentum` ships a **count**: \"2 checks have failed in a row.\" A bare number, deliberately\n  labelled as a block rather than a sentence because the model read a trailing sentence aloud.\n- `## The fight` **forbids** counts: \"never state the order, the round number, or anyone's hit\n  points. Those are on screen.\" `dm.md` forbids reading hit points aloud, and `AGENTS.md` combat\n  narration says a wound is described, not counted — \"handing the model a fraction is an invitation\n  to read it.\"\n\nSo: does a filling light clock arrive as `4/6`, or as \"the lantern is guttering\"? Does hurt arrive\nas `5/12`, or as \"badly hurt\"? The two blocks in the same file answer differently and both have\nreasons behind them.\n\n## `## Ways out` is the cautionary tale, and it is recent\n\nThe block listed doors and said nothing about their state. The model re-improvised the answer every\nturn; both 2026-09-07 sessions invented an ATHLETICS DC 20 to kick a door that was never shut, and\none spent two turns on a key against a lock that does not exist. The fix was the prompt telling the\ntruth. **A projection block that is vague about state is worse than no block**, because the model\nwill fill the gap and be consistent about the wrong thing.\n\n## What has to be decided\n\n- Hurt: counted or described, and if described, what the bands are and who owns them.\n- Clocks: which clocks appear, in what form, and whether a clock the player cannot see (ALERT) is\n  described differently from one they can (LIGHT).\n- The objective: carried or not carried, and what stops the narrator inventing a second one.\n- Consumables: counts, and how the block makes \"you have no potions\" something the model will not\n  narrate around. §9 is blunt that the model always says yes.\n- Whether any of this belongs in `dm.md` and `dm-tools.md` instead of, or as well as, the\n  projection. `AGENTS.md` records that prompt and parser must agree, and that terse instructions\n  win on length — measure before believing an edit helped.\n\n## Watch for\n\nThe Established block already leaks grid coordinates (emberdelve-pa8, and m2-evaluation §8). Adding\nblocks is adding surfaces for that. Nothing in this projection may produce a number the narrator\nwould read aloud.","acceptance_criteria":"A decision, recorded as a resolution comment, on what hurt, clock, objective and consumable state reaches the DM prompt and in what form — counted or described, in which block, with the wording. No number that a narrator would read aloud.","status":"closed","priority":1,"issue_type":"task","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-08T23:21:56Z","created_by":"hampton.black","updated_at":"2026-09-09T21:03:29Z","started_at":"2026-09-09T20:42:54Z","closed_at":"2026-09-09T21:03:29Z","close_reason":"Resolved: one always-present ## The party block; bands not fractions, except counts that drive a tool decision. Full answer in the resolution comment.","labels":["wayfinder:grilling","wayfinder:map"],"dependencies":[{"issue_id":"emberdelve-ffi.1","depends_on_id":"emberdelve-ffi","type":"parent-child","created_at":"2026-09-08T18:21:55Z","created_by":"hampton.black","metadata":"{}"}],"comments":[{"id":"01a087fb-392a-7042-ae3b-9795562578d1","issue_id":"emberdelve-ffi.1","author":"hampton.black","text":"# Resolution — 2026-09-09\n\nHurt, clocks, the objective and consumables reach the DM prompt as **one always-present\n`## The party` block**, in **bands, not fractions**, with the counts that drive a tool\ndecision left as counts.\n\n## The rule that decides all of it\n\n**A count that drives a tool decision stays a count. A count that only describes state\nbecomes a band.**\n\nThis is the reconciliation the ticket asked for, and it explains both precedents rather\nthan picking a winner between them. `## Momentum`'s \"2 checks have failed in a row\" is the\ninput to `dm-tools.md`'s two-failure escalation rule, so it stays a number — and\nemberdelve-vy7 is the evidence, because both 2026-09-07 sessions escalated on the *first*\nfailure and a band there (\"things have been going badly\") is exactly the vagueness that\nmade turn-1 escalation a coin flip. `## The fight`'s ban on hit points stands for the\nopposite reason: no tool takes hit points as an argument. None of the seven does. The\nmechanics model decides *whether* to roll, never how much damage, so the fraction buys\nnothing and costs what AGENTS.md warns about — \"handing the model a fraction is an\ninvitation to read it.\"\n\nThe rule also predicts the answer for anything added later, which is the point of writing\nit down rather than listing four decisions.\n\n## What the code does today, which is not what anyone intended\n\n`DmService.java:797` ships **every entity as `5/12 hp`**, every turn, in both prompts. So\nthe ticket's question — 5/12 or \"badly hurt\" — is already answered as 5/12 *by accident*,\nagainst AGENTS.md and against `BeatRenderer.condition()`, which sits twelve files away with\nfour bands and a comment explaining why a fraction in a beat is an invitation to read one.\nTwo mechanisms in one codebase disagreeing. That fraction goes.\n\n## Hurt\n\nBands, from **one shared source** — `BeatRenderer.condition()`'s existing four, extracted so\nthere is one wound vocabulary in the system rather than two:\n\n    \u003e 0.7  barely marked\n    \u003e 0.4  bloodied\n    \u003e 0.15 badly hurt\n    else   barely standing\n\nNot a second set of thresholds for the projection. A creature described one way in a combat\nbeat and another way in the block above it is the copper-key failure in miniature.\n\nThe number the player needs is on their own hit point bar. The DM never needed it.\n\n## Whose hurt, and where\n\nThe party gets **its own block**, `## The party`, above `## Entities present`.\n\nToday the party member sits *inside* `## Entities present`, marked `[the player]`. After\nemberdelve-4h9.1 that list holds two different lifetimes: party state is session-scoped and\nsurvives a room boundary, hostiles are rebuilt from room content. The block that has to\nsurvive a crossing should not be the one that gets rebuilt. Invariant 2 also applies —\n`List\u003cPartyMember\u003e`, so the block is a list, not a line about the player.\n\nIt gives the clocks, the consumables and the objective somewhere coherent to sit: all three\nare party facts, not room facts, which is why none of them fits in any block that exists.\n\n## The block, as wording\n\n```markdown\n## The party\n\nSession state. It followed them in here and it will follow them out.\n\n- `roderick` — Roderick, badly hurt, at (6,1)\n- objective: not yet found\n- lantern: guttering\n- potions: 0 · torches: 1 · rope: 0\n\nThere is nothing else here worth carrying out. Do not invent a second.\n\nThe lantern and anything moving toward the party are the engine's to change, not yours.\nNarrate what you are told has happened. Do not decide that the light goes out.\n```\n\nGrid coordinates stay on the party line: `move_entity` needs bounds and adjacency, `## Grid`\nexists to make them mean something, and `dm.md:47` already forbids saying one aloud. Dropping\nthem here while every prop keeps its own would break \"what is near what\" — emberdelve-pa8 is\nabout facts leaking coordinates into `## Established`, which is a different surface.\n\n## Clocks\n\n**Both LIGHT and ALERT, as bands, with a fact-not-cue line.**\n\nALERT has to be in the projection even though the player cannot see it, or honest\nforeshadowing is impossible — that was already settled at charting (\"the projection lets the\nDM foreshadow the alert clock honestly\"). LIGHT has to be there or the narrator writes a\nsteady lantern while the radius shrinks on screen.\n\nThe fact-not-cue line is load-bearing and it is not boilerplate. The `## Secrets` block\nproves this exact model narrates a thing into being when a block reads as a promise: told a\ngoblin \"will come out fighting\", the prose model wrote it climbing out — lid grinding, hand\non the rim — on a turn where nothing had spawned and no fight had started. The engine owns\nthe dark (settled at charting, from §9: \"it never decides that the torch went out\"), so the\nblock that mentions the lantern is the block that has to say so.\n\n**The ALERT band vocabulary is deliberately not decided here.** It is emberdelve-ffi.2's —\nwhat is on the LIGHT and ALERT tables, and the signs under them. This ticket leaves it a\nnamed slot rather than pre-empting the ticket that owns it.\n\n## The objective\n\nA **state line plus a closed-world sentence**.\n\nThe objective is an engine-owned prop (emberdelve-4h9.4), so it is already listed under\n`## What the player can see` or `## Hidden`. What that listing cannot express is that the\nparty is *carrying it out of a room the prop no longer belongs to* — which is the whole\nreason §15 calls this the expensive part of M4. Hence `objective: not yet found` /\n`objective: carried by \u003cname\u003e`.\n\n\"There is nothing else here worth carrying out. Do not invent a second.\" is the `## Ways out`\nlesson applied *before* the playtest instead of after it. A block vague about state is worse\nthan no block, because the model fills the gap and is then consistent about the wrong thing.\n\n## Consumables\n\n**All three counters, always, zeroes included.** `potions: 0 · torches: 1 · rope: 0`.\n\nAn omitted line is an invitation — the argument this codebase already makes three times over\n(an empty tool enum, an empty `## Ways out`, an empty `## Secrets`). §9 is blunt that the\nmodel always says yes, so the block's job is to stop the narrator handing over a potion\nnobody has. Enforcement stays `use_item`'s rejection; the block is honesty, not a gate.\n\nThese are counts and they stay counts under the rule above: `use_item` takes a consumable\nand the model has to know whether it can call it.\n\n## Always present, never conditional\n\nThe block is always there, following `## Ways out`'s precedent rather than `## Momentum`'s or\n`## Secrets`'.\n\nA conditional pressure block makes its **absence** information, and the model will read that\nabsence as \"no pressure\" on exactly the turn a clock starts running. Always-present also\nmeans the block's shape never changes mid-session, which is the property that stopped\n`## Ways out` being re-improvised every turn.\n\n## Where the rules live\n\n**Facts in the projection, permissions in the prompts, nothing said twice.** The\n`worldState(boolean forProse)` split already works this way — the two models are told the\nsame facts and given opposite permissions. One exception: \"never read a hit point aloud\" is a\nstanding rule rather than a per-turn fact, so it stays in `dm.md`, where it already is.\n\nNo new text in `dm.md` or `dm-tools.md` for any of the above. AGENTS.md's finding stands —\nwriting more about brevity made the model more verbose, and terse instructions win. The\nfact-not-cue line inside the block is the only instruction added anywhere, and it is two\nsentences.\n\n## Term collision, resolved\n\n`CONTEXT.md:123` defined **Clock** as the ordered narration queue (`godot/autoload/clock.gd`);\n§9's `Clock` record is a ticking counter with three scales. §9's keeps the name — it is the one\nwith a campaign-scale future and a design document naming it. The glossary entry for the Godot\none is renamed to **Narration queue**, noting `clock.gd` as its file, and a **Delve clock**\nentry added. Glossary-only: renaming `clock.gd` is a build-side chore that can follow or never,\nand `Clock.hold` reads correctly in AGENTS.md as it stands.\n\n## Not decided here\n\n**How the narrator conveys a downed party member.** `emberdelve-4h9.13` and the map's fog list\nboth hoped this would fold into this ticket. It does not: the band vocabulary above bottoms out\nat \"barely standing\", which is a creature still on its feet, and downed is a different state\nwith a clock attached. Graduated to its own ticket.\n","created_at":"2026-09-09T21:03:03Z"}],"dependency_count":0,"dependent_count":2,"comment_count":1}
{"_type":"issue","id":"emberdelve-ffi","title":"M4 wayfinder — settle the delve design before it is built","description":"## Destination\n\nA signed M4 design spec at `docs/superpowers/specs/2026-09-XX-m4-delve-design.md` — the document\nemberdelve-4h9's ten build children get executed against. §9 of `docs/ai-dm-system-design.md` is\nexplicit that it is \"a bet, not a record\", written at lower confidence than everything above it;\nthis map settles what it left open so that no build session has to stop and decide.\n\nThe map ends when the spec is signed. The gate session itself (play it, write `m4-evaluation.md`)\nis a child of 4h9, not of this map.\n\n## Notes\n\nDomain: the Emberdelve delve loop. Read `AGENTS.md` first (it wins over the design doc where the\ntwo disagree), then `docs/ai-dm-system-design.md` §9, then `CONTEXT.md`.\n\nEvery session on this map calls the `grilling` and `domain-modeling` skills unless the ticket's\nown type says otherwise. Prototype tickets call `prototype`.\n\n**Plan, don't do.** Tickets here resolve decisions. The building happens under emberdelve-4h9.\n\n**Amend 4h9 in place.** When a decision here lands, `bd update` whichever 4h9 child it supersedes,\nso the build ticket is always true. A build ticket that contradicts the spec is the copper-key\nfailure applied to process: durable, consistent, and pointing at a lock that is not there.\n\n**The `Ending` enum is descriptive, never a verdict.** `EXTRACTED_WITH_OBJECTIVE` /\n`EXTRACTED_WITHOUT` / `PARTY_LOST`, not WON/LOST. §9's own first argument for bounded sites is that\n\"a cleared site is a durable fact\" the campaign layer consumes. Scoring the delve here pre-empts\n§10, and the standing design intent is that a bad outcome must never guarantee campaign failure —\nplayer creativity and DM improvisation should be able to open another path. An enum that judges\ntakes that away.\n\n## Settled at charting — 2026-09-08\n\nThe grilling session that produced this map settled twenty-four decisions. They are the spec's raw\nmaterial and are recorded here because they are not tickets and have nowhere else to live.\n\n**The delve ends as an event.** `DelveEnded`, folded into `WorldState` and shipped in `SceneState`,\nunder invariant 8 — the end of a delve is the most important thing that happens in a session and\nmust be in the log and must replay. Three endings; only `PARTY_LOST` is losing. Extracting\nempty-handed is a legitimate cautious outcome, because §9's greed decision only works if leaving\nearly is respectable. `godot/chrome/defeat.gd`'s client-side `hp \u003c= 0` check on a hardcoded\n`\"fighter\"` goes: it cannot distinguish three endings and it names a singleton player against\ninvariant 2.\n\n**Light moves from the room to the party.** `room.gd:315` builds wall torches from the room's own\n`LightingPreset`; `Room.Level` is a separate axis and the file is emphatic about not confusing\nthem. So a LIGHT clock has nothing to burn down — the party carries no light. It does now: a\ncarried lantern, fuelled by the LIGHT clock, read as a shrinking radius. Room torches stay\nroom-owned so `Level.DIM` keeps its one meaning. The 2026-09-07 playtest recorded that invented\nlanterns already travel with the party as facts with no prop behind them, and `kaykit_halloween`\nships an unused `lantern_standing`: this is the hotdog problem solved the cheap way round, by\ngiving the board the thing the fiction keeps inventing.\n\n**The engine owns the dark.** No DM lighting tool, in either phase. §9: \"The DM narrates what the\ntable produced; it never decides that the torch went out.\" A narrator that can turn the lights off\nwill do it because it is dramatic rather than because anything was spent, and then the clock means\nnothing. Scarcity is the engine's alone.\n\n**A failed check ticks the light clock one segment; a natural 1 ticks two.** §9 already names noisy\nfailures as a tick trigger. Any-failure-alone taxes trying things; nat-1-alone is too rare to build\npressure across 4-5 rooms. The natural die is already special (invariant 5). This is also the\nreconciliation of a real play observation: the DM narrating a dropped lantern on a 1 was the right\nbeat reached the wrong way — now the engine ticks, the table fires, and the DM narrates a real\nevent.\n\n**The `torch` consumable refuels the light clock.** §9's Consumable enum is already potion / torch\n/ rope. This links the counter map to the clock and states the greed loop in one line: how deep you\ngo is how many torches you brought. It also keeps the clock non-terminal.\n\n**Clocks cannot kill.** The clock's job is to make leaving attractive, not to be a second health\nbar. A terminal clock turns the delve into a speedrun; §9 wants a greed decision. Death follows\nfrom a filled light clock indirectly, which is enough.\n\n**Clocks speak twice.** A threshold fires a sign; the fill fires the consequence. Signs are §9's\nregion-scale \"spoor, smoke, tracks\" brought to delve scale — engine-fired, table-driven, narrated\nby the DM. A clock that speaks only at full is a jump scare; one that speaks every tick is a\ncounter with extra steps.\n\n**The clock is diegetic and unlabelled.** No segment counter on screen. The LIGHT clock reads as\nthe shrinking radius; the ALERT clock's entire read is its signs. One legible clock beats two\nhalf-legible ones, and the projection lets the DM foreshadow the alert clock honestly.\n\n**Rest exists, and it is the pressure valve.** An explicit player action; only in a room with no\nliving hostile in it; ticks every running clock one segment; restores a fixed small amount. Not a\nroll — the dice are for questions the DM is about to answer, and \"how much did you recover\" is not\na moment. Fixed and small keeps the arithmetic in the player's head, which is where the tension is.\n\n**Downed is a clock.** 0 HP drops you and starts a short clock; if it fills before combat ends, the\ndelve is lost. Not death saves — that is §6 and it belongs to the next milestone. The plain\ninstant-death alternative makes \"5 hit points in front of a door\" already-lost rather than tense,\nat HP 12 against 1d6+2. A downed clock uses the primitive being built anyway, at a scale it is\nalready sized for, and stays correct when the party grows past one.\n\n**`EnvTorchlit`'s 1.1 ambient is a prerequisite, rescoped to one number.** emberdelve-27l. A\nshrinking light radius cannot be judged at ambient 1.1. Scoped to changing the number and looking\nonce, so the lighting anti-goal's timebox holds.\n\n**Structure.** This map is a sibling of 4h9, not its parent and not its child: \"what can I decide\"\nand \"what can I build\" are different sessions and `bd ready` should tell them apart. The spec is\nwritten in one pass at the end, from the resolution comments — M1/M2/M3's specs read as documents\nwith an argument, and a case written in eight sittings by eight agents is not one.\n\n## Decisions so far\n\n\u003c!-- one line per closed ticket --\u003e\n\n- [What the projection carries about hurt, clocks and the objective](emberdelve-ffi.1): one\n  always-present `## The party` block above `## Entities present`, carrying hurt, the clocks, the\n  objective's state and all three consumable counts. **A count that drives a tool decision stays a\n  count; a count that only describes state becomes a band** — which is why `## Momentum` keeps its\n  number and hurt loses its fraction. Hurt uses `BeatRenderer.condition()`'s existing four bands,\n  extracted so there is one wound vocabulary. `DmService.java:797`'s `5/12 hp`, which no decision\n  ever asked for, goes. Facts in the projection, permissions in the prompts, nothing said twice.\n\n- [What is on the LIGHT and ALERT tables, and the signs under them](emberdelve-ffi.2): four tables,\n  written out. **A consequence may only do what a tool can already do** — a named, pre-validated\n  bundle of events that already have an `Event` + `Diff` pair, never an effect language. **A sign is\n  a consequence whose bundle is empty**, so there is one enum and one firing path. LIGHT is a fixed\n  one-entry table (`LIGHT_OUT`), because a clock you can plan against is what makes the greed\n  decision computable; ALERT is drawn from four, because its entire read is its signs. Signs ride\n  the latched directive rail and cost no model call. Rooms and rests tick both clocks; failed checks\n  tick LIGHT; fights and a filled LIGHT tick ALERT — declared on the tick side, never inside a\n  consequence. ALERT resets on fill, LIGHT stays full until a torch. `Disposition { HOSTILE, WARY }`\n  — a consequence brings something in, it does not decide how the meeting goes. Also takes\n  [emberdelve-vy7](emberdelve-vy7)'s answer: `start_combat` stays for provoked fights, the\n  two-failure escalation rule goes, and the ALERT clock becomes the only source of an unprovoked one.\n\n- [The numbers — tune attrition until 5 hit points is a real decision](emberdelve-ffi.6): fighter\n  **HP 12 → 20**, goblin weakened to **AC 12 / HP 6 / +3 / 1d4+1**, new brute **AC 13 / HP 16 / +4 /\n  1d8+2**. Rest restores 4, a potion 8; LIGHT and ALERT are 6 segments; 2 torches, 2 potions; a site\n  is 5 rooms with ~3 fights. **The ticket's premise was wrong** — at HP 12, five hit points against a\n  goblin is a 72% survival, median loss per fight *zero*. The finding that unlocked it: **the\n  encounter decides where the doubt window sits, max hit points decide how much bar is above it**,\n  and conflating those two is what made attrition untunable. The brute lands the window on **4–10,\n  45.7% survival at exactly 5**, making §9's gate sentence arithmetically true. Against a party of\n  one the action economy inverts the obvious ladder — N weak enemies is always harder than one strong\n  one — which is why the goblin had to become the mob it was already described as. **Two charting\n  reversals:** an ordinary failed check no longer ticks LIGHT (only a natural 1 does, one segment),\n  because measured sessions differ sevenfold in failure rate on the DM's choice of DC and that hands\n  scarcity to the narrator; and the downed clock is dropped, because `isOver()` ends the fight when\n  the only player-controlled entity drops, so it can never fill. Numbers reproducible from\n  `docs/evidence/m4-attrition-sim.py`.\n\n## Not yet specified\n\n- **The ambient sound bed the signs would eventually want.** `AGENTS.md` records that there is none\n  at all — \"the braziers burn without a sound\". An alert clock is a reason to build one, but\n  building it *for* the clock is the tail wagging the dog. Revisit once signs exist and have\n  something to be about.\n- **Whether \"something worth more, deeper\" needs a value concept.** §9's greed decision wants\n  something past the objective worth going for. Whether that is a second objective-shaped prop or\n  something with a value on it is not decidable until the site is being authored.\n- **Whether generated rooms need a safe-room concept.** Resting is room-conditional (no living\n  hostile). That rule is engine-owned and survives generation, but whether the generator has to\n  guarantee a restable room is a question for the generator milestone.\n\n## Out of scope\n\nRuled beyond this destination. These do not graduate.\n\n- **Meta-progression** — stash, upgrades, run tiers. §9: never. It competes with the campaign layer\n  for the same design space.\n- **Resume from a session log, snapshotting, schema migration** — §16. Sting comes from within the\n  session; a lost delve costs the run, and the run is the session.\n- **Inventory, equipment, encumbrance** — §16. Consumables are a counter map and nothing more.\n- **The effect interpreter and the stance layer** — §15 puts them with the content milestone,\n  together, because each is a tax alone.\n- **The region and the watch loop** — §16.\n- **`WATCH` and `FRONT` clock scales** — the enum carries all three because the record is built\n  once; only DELVE is in scope.\n- **A DM lighting tool** — see Settled above.\n- **Terminal clocks** — see Settled above.\n- **The downed clock, and how a downed member reads to the narrator** — settled at charting, ruled\n  out by [The numbers](emberdelve-ffi.6). `CombatEngine.isOver()` ends the fight when no living\n  player-controlled entity remains, so at party size one the clock is inert by construction, and the\n  \"five hit points is one bad roll from over\" premise it rested on measured false. 0 hp is\n  `PARTY_LOST`. Closed: [emberdelve-4h9.13](emberdelve-4h9.13) and\n  [How a downed party member reads to the narrator](emberdelve-ffi.8), the latter existing only to\n  decide questions the downed state raised. Re-enters with a second party member or §6's death saves.\n- **§10 judging a delve's outcome** — the `Ending` enum stays descriptive so that it can, later.\n- **The M4 gate session and `m4-evaluation.md`** — filed as a child of emberdelve-4h9. The gate is\n  what the spec is for, not a step on the way to it.","acceptance_criteria":"A signed spec at docs/superpowers/specs/2026-09-XX-m4-delve-design.md, every child ticket closed with a resolution comment, and nothing left to decide before emberdelve-4h9's children can be built. AGENTS.md and CONTEXT.md updated to match.","status":"open","priority":1,"issue_type":"epic","owner":"hampton.j.black@gmail.com","created_at":"2026-09-08T23:21:28Z","created_by":"hampton.black","updated_at":"2026-09-09T23:59:10Z","labels":["wayfinder:map"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-4h9","title":"M4 — Can you lose a delve, and does losing it sting?","description":"M4. Gate question, from docs/ai-dm-system-design.md section 9: can you lose a delve,\nand does losing it sting?\n\nStated as something to feel, not to measure, in the same spirit as M0. You should be\nable to stand in front of a door at 5 hit points and genuinely not know whether to\nopen it. If you can, the loop exists.\n\nWHY THIS AND NOT THE GENERATOR\n\nM1's outstanding half (LayoutGenerator, ExitPlacer, world-space packing) is the other\ncandidate and it loses on three counts.\n\n1. The design doc already ruled on the ordering. Section 15, Still ahead: \"Prove it on\n   authored rooms before generated ones, or a generator bug and a game-feel problem are\n   indistinguishable.\"\n2. M3's own carry-forward is the evidence. \"Generated rooms still have nothing to find.\n   PropPlacer marks nothing hidden, so reveal_prop is never offered.\" That is not a\n   missing feature in PropPlacer, it is PropPlacer written without knowing what a room\n   is for. LayoutGenerator would be written against the same unknown, and a dungeon that\n   generates six rooms with nothing to find is the M1 hole multiplied by six.\n3. Risk 2 in section 17 is the top live risk and generation makes it worse. \"Nothing is\n   scarce, so nothing is at stake... the game runs on novelty, which decays in about\n   twenty minutes.\" More rooms is more novelty. It buys nothing against that risk.\n\nM3 already paid most of the setup. Section 9 lists \"party state is session-scoped, not\nroom-scoped\" as the small refactor everything else depends on. GameEngine.java:73 spawns\nthe party once at the entrance and entities live in WorldState.entities across\ncrossExit, so that refactor looks done, pending the audit in the first child.\n\nWHAT IS IN\n\nAttrition (session-scoped party state, scarce healing, consumables as a counter map),\nthe Clock primitive at DELVE scale, an engine-owned objective that is takeable, an exit\nthat checks for it, a delve that can be lost, clickable props, DM-placed markers, a\n4-5 room authored site, and more than one enemy on the board at once.\n\nWHAT IS OUT, AND STAYS OUT\n\nThe effect interpreter and the stance layer. Section 15 puts them with the content\nmilestone, together, because each is a tax alone. Several skeletons in one room is N\ninstances of one or two stat blocks against the existing ~40-line resolveAttack. It is\nnot permission to open section 6.\n\nInventory, equipment, encumbrance. Section 16 has the re-entry condition. Consumables\nare a counter map and nothing more.\n\nThe region and the watch loop. Section 16 defers them until enough sites exist that\ntravelling between them means something.\n\nMeta-progression. Section 9: never. It competes with the campaign layer for the same\ndesign space.\n\nTHE ANTI-GOAL THIS RETIRES\n\nAGENTS.md, Anti-goals: \"Do not add a third room.\" That was M3-scoped and it is spent.\nA delve needs enough rooms for \"push deeper or get out\" to be a real decision. The\ngenerator anti-goal stands until this epic closes.\n\nORDER\n\nProve it on authored rooms. The generator comes after, with a spec: the authored site\nis what LayoutGenerator has to be able to produce.","acceptance_criteria":"A played session, in the real Godot client, in which the player takes the objective and\nchooses between leaving with it and going deeper, and a second played session in which\nthe delve is lost. Both written to server/sessions/ and one copied into docs/evidence/\nso it replays in the suite. Judged the way M0 and M3 were judged: an evaluation doc with\na verdict, not a metric.","notes":"THE DESIGN IS ON A WAYFINDER MAP 2026-09-08: emberdelve-ffi, \"M4 wayfinder — settle the delve design\nbefore it is built\". Read its `## Settled at charting` block before opening any child of this epic.\n\nThe two trees answer different questions. **This epic is what gets built; the map is what gets\ndecided.** Several children here are now blocked on a decision ticket, so `bd ready` will not offer\nbuild work whose design is still open. When a decision closes, amend the child it supersedes in\nplace — a build ticket that contradicts the spec is the copper-key failure applied to process.\n\nFive children were missing and have been filed: 4h9.11 (rest), 4h9.12 (the party carries a light),\n4h9.13 (downed is a clock), 4h9.14 (the projection carries the delve — §15's \"expensive part\", which\nnothing owned), and 4h9.15 (play the gate, write `m4-evaluation.md`).\n\nemberdelve-ql3 was pulled in without a blocking edge; emberdelve-27l was pulled onto the map and\nrescoped from an evening's polish to a one-number prerequisite.\n\nThe map's destination is a signed spec at `docs/superpowers/specs/2026-09-XX-m4-delve-design.md`.","status":"open","priority":1,"issue_type":"epic","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T22:26:00Z","created_by":"hampton.black","updated_at":"2026-09-08T23:26:38Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-xgg.10","title":"Doors read as shut but every exit is always passable","description":"Playtest 2026-09-07, two sessions. The player could not tell whether a door was locked, unlocked, or crossable yet, and the DM answered differently every turn.\n\ncrossExit (GameEngine.java:284) gates on combat and nothing else. There is no shut, locked or barred state anywhere. Every exit is passable at all times outside a fight.\n\nEvery authored door description asserts the opposite:\n- crypt door-north: 'banded in tarnished iron. It has no handle on this side.'\n- gallery door-south: 'The slab door back to the crypt, shut on a dark stair-head.'\n\nNothing resolves the conflict for the model. waysOut() emits only '- a way out in the north wall' with no state. The use_exit schema says 'Only when the player has said they are leaving' and dm-tools.md:39 closes with 'When in doubt, do not.' So the model holds a sealed-sounding description, a bare ways-out line, and a refusal bias, and re-improvises the reconciliation every turn.\n\nBoth sessions independently invented an ATHLETICS DC 20 kick to open the door. The check adjudicated nothing: the door was open before the roll and open after. Session 2 also spent two turns trying a found copper key on a lock that does not exist.\n\nThis is fix (a): make the prompt tell the truth. Fix (b), modelling a real shut/locked exit, is filed separately.","acceptance_criteria":"waysOut() states that a listed way out can be taken at any time outside a fight. Gallery door-south no longer asserts the door is shut. Crypt door-north keeps 'no handle on this side' — true of a door that still shoves open. Server suite green.","status":"closed","priority":1,"issue_type":"bug","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T20:23:59Z","created_by":"hampton.black","updated_at":"2026-09-07T20:26:19Z","started_at":"2026-09-07T20:24:03Z","closed_at":"2026-09-07T20:26:19Z","close_reason":"waysOut() now states every listed exit is open, unlocked and crossable outside a fight; gallery door-south no longer says shut. Two tests lock it. Server suite green.","dependencies":[{"issue_id":"emberdelve-xgg.10","depends_on_id":"emberdelve-xgg","type":"parent-child","created_at":"2026-09-07T15:23:58Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-cfs","title":"One door in the crypt wall, and make it an actual door","description":"Playtest of the M3 branch: the CARVED wall variant table includes wall_gated and wall_doorway, so the perimeter grows openings that lead nowhere; and the north exit's DOOR prop hashes to one of those same open archways drawn in front of a solid wall segment. Cut the wall where an exit is, put a real door mesh there, and drop the door-shaped variants from the random wall mix.","status":"closed","priority":1,"issue_type":"bug","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-06T19:01:18Z","created_by":"hampton.black","updated_at":"2026-09-06T19:31:54Z","started_at":"2026-09-06T19:01:23Z","closed_at":"2026-09-06T19:31:54Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-x26","title":"Click the door itself to cross, not the square it stands on","description":"Playtest of the M3 branch: crossing a threshold means clicking the floor square the door stands on, which reads as an ordinary move that happens to work. Make the door prop itself the click target — pick_at should resolve props under the ray, and a picked door should be the exit intent. First step toward props being clickable objects generally.","status":"closed","priority":1,"issue_type":"feature","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-06T19:01:18Z","created_by":"hampton.black","updated_at":"2026-09-06T19:31:54Z","started_at":"2026-09-06T19:01:23Z","closed_at":"2026-09-06T19:31:54Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-c6r","title":"Neighbour room reads as lit, not as a dark unknown","description":"Playtest of the M3 branch: the room through the doorway shows all its floor and wall textures, so it reads as a second lit room rather than a dark space you have not been to yet. build_unlit already withholds torches, but the WorldEnvironment ambient is enough to render the kit textures fully. Darken the neighbour geometry so the payoff of walking through the door is a room lighting up.","status":"closed","priority":1,"issue_type":"bug","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-06T19:01:09Z","created_by":"hampton.black","updated_at":"2026-09-06T19:31:54Z","started_at":"2026-09-06T19:01:23Z","closed_at":"2026-09-06T19:31:54Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-xy9","title":"Seed CONTEXT.md and docs/adr/ from AGENTS.md","description":"Single-context layout. CONTEXT.md glossary drawn from the domain language already used in AGENTS.md; ADRs seeded from its Locked decisions, two-model DM, and model selection sections.","status":"closed","priority":1,"issue_type":"task","assignee":"hampton.black","created_at":"2026-09-06T17:29:52Z","updated_at":"2026-09-06T17:34:56Z","started_at":"2026-09-06T17:33:15Z","closed_at":"2026-09-06T17:34:56Z","close_reason":"CONTEXT.md + docs/adr/ (template, README, 7 back-filled ADRs) written; superpowers disabled per-repo","dependencies":[{"issue_id":"emberdelve-xy9","depends_on_id":"emberdelve-1fk","type":"parent-child","created_at":"2026-09-06T12:29:52Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-7xj","title":"Write docs/agents/ config for the Pocock skills with beads as tracker","description":"setup-matt-pocock-skills ships seeds for GitHub/GitLab/local-markdown only. Beads is an Other tracker, so issue-tracker.md must be written from scratch mapping publish/fetch/triage/wayfinder operations onto bd commands. Also triage-labels.md and domain.md.","status":"closed","priority":1,"issue_type":"task","assignee":"hampton.black","created_at":"2026-09-06T17:29:51Z","updated_at":"2026-09-06T17:32:21Z","started_at":"2026-09-06T17:31:33Z","closed_at":"2026-09-06T17:32:21Z","close_reason":"docs/agents/{issue-tracker,triage-labels,domain}.md written; beads mapped as an 'Other' tracker","dependencies":[{"issue_id":"emberdelve-7xj","depends_on_id":"emberdelve-1fk","type":"parent-child","created_at":"2026-09-06T12:29:51Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-z0d","title":"Dedupe the three beads managed blocks","description":"CLAUDE.md has one BEADS INTEGRATION block; AGENTS.md has both a BEADS INTEGRATION block and a BEADS CODEX SETUP block saying nearly the same thing. Collapse to one.","status":"closed","priority":1,"issue_type":"task","assignee":"hampton.black","created_at":"2026-09-06T17:29:49Z","updated_at":"2026-09-06T17:30:54Z","started_at":"2026-09-06T17:30:15Z","closed_at":"2026-09-06T17:30:54Z","close_reason":"CLAUDE.md now imports AGENTS.md; codex-duplicate beads block removed; Agent skills section added","dependencies":[{"issue_id":"emberdelve-z0d","depends_on_id":"emberdelve-1fk","type":"parent-child","created_at":"2026-09-06T12:29:49Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-1fk","title":"Adopt Pocock skills + beads workflow","description":"Move Emberdelve off heavy superpowers use and onto the Matt Pocock engineering skills (grill-with-docs, implement, tdd, triage, wayfinder) with beads as the issue tracker. Covers instruction-file consolidation, per-repo plugin config, docs/agents/ setup, and seeding CONTEXT.md + ADRs.","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-09-06T17:29:47Z","updated_at":"2026-09-06T17:35:18Z","closed_at":"2026-09-06T17:35:18Z","close_reason":"All five children complete: AGENTS.md is source of truth, beads blocks deduped, superpowers disabled per-repo, docs/agents/ written, CONTEXT.md + 7 ADRs seeded","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-auj","title":"Make AGENTS.md the single source of truth; CLAUDE.md imports it","description":"CLAUDE.md is autogenerated boilerplate with empty template sections while AGENTS.md holds the real 834 lines of project rules but is never loaded into Claude Code context. Reduce CLAUDE.md to an @AGENTS.md import plus build/test commands.","status":"closed","priority":1,"issue_type":"task","assignee":"hampton.black","created_at":"2026-09-06T17:29:47Z","updated_at":"2026-09-06T17:30:54Z","started_at":"2026-09-06T17:30:15Z","closed_at":"2026-09-06T17:30:54Z","close_reason":"CLAUDE.md now imports AGENTS.md; codex-duplicate beads block removed; Agent skills section added","dependencies":[{"issue_id":"emberdelve-auj","depends_on_id":"emberdelve-1fk","type":"parent-child","created_at":"2026-09-06T12:29:48Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-ffi.8","title":"How a downed party member reads to the narrator","description":"## Question\n\nHow does a downed party member reach the DM prompt, and how does the narrator convey it\nwithout counting?\n\nGraduated from the map's fog list on 2026-09-09, when emberdelve-ffi.1 landed. Both that\nticket and emberdelve-4h9.13 hoped it would fold into the projection decision. It did not.\n\n## Why it did not fold\n\nffi.1 settled that hurt reaches the prompt as a band, from `BeatRenderer.condition()`'s\nexisting four: barely marked / bloodied / badly hurt / barely standing. Every one of those\ndescribes a creature **still on its feet**. Downed is not the bottom of that scale — it is a\ndifferent state, with a clock attached (emberdelve-4h9.13) and a delve riding on whether the\nclock fills before the fight ends.\n\n## What has to be decided\n\n- Is downed a fifth band, or its own line in `## The party`? A fifth band keeps one\n  vocabulary; a separate line admits it is a different kind of fact.\n- Does the *downed clock* appear in the prompt at all? ffi.1 put LIGHT and ALERT in the block\n  as bands. This clock is short, combat-scoped, and the tensest thing on the table. A band\n  (\"fading\") may be right, or the fight block may own it, or the narrator may be told nothing\n  and left to work from the beat.\n- What `BeatRenderer` says on the blow that drops someone. Today `condition()` returns a band\n  and a kill returns \"is killed by the blow.\" Downed is neither.\n- How the narrator conveys it. `dm.md` forbids reading hit points aloud and `## The fight`\n  forbids stating them, so this is a hit point state that has to land as fiction. It is also\n  the moment the player most needs to understand exactly what is true.\n- Whether the party block's grammar survives a downed member at party size one. \"Session\n  state. It followed them in here and it will follow them out.\" reads oddly about someone on\n  the floor.\n\n## Watch for\n\nThe band vocabulary is shared with combat beats by ffi.1's decision — one wound vocabulary,\ndeliberately. Anything added here is added in both places or it is not added.","acceptance_criteria":"A decision, as a resolution comment, on how downed reaches the DM prompt — band or its own line, whether its clock is carried, what BeatRenderer says on the blow that drops someone — and no number a narrator would read aloud.","status":"closed","priority":2,"issue_type":"task","owner":"hampton.j.black@gmail.com","created_at":"2026-09-09T21:03:23Z","created_by":"hampton.black","updated_at":"2026-09-09T23:58:42Z","closed_at":"2026-09-09T23:58:42Z","close_reason":"Out of scope: every decision it holds is downstream of the downed clock, which emberdelve-ffi.6 ruled out of M4.","labels":["wayfinder:grilling","wayfinder:map"],"dependencies":[{"issue_id":"emberdelve-ffi.8","depends_on_id":"emberdelve-ffi","type":"parent-child","created_at":"2026-09-09T16:03:22Z","created_by":"hampton.black","metadata":"{}"}],"comments":[{"id":"01a0889c-063a-7315-be2a-c94866f4745e","issue_id":"emberdelve-ffi.8","author":"hampton.black","text":"**Closed by emberdelve-ffi.6 — the question no longer exists.**\n\nEvery decision this ticket was opened to make is downstream of the downed clock, and ffi.6 ruled the\ndowned clock out of M4 (see emberdelve-4h9.13): 0 hit points is `PARTY_LOST`, immediately.\n\nTaking its bullets in turn: there is no fifth band, because there is no state past `barely standing`\nthat a creature survives in. The downed clock does not appear in the prompt, because there is no\ndowned clock. `BeatRenderer` needs nothing new for the blow that drops someone — that blow is a kill,\nand `condition()` already returns \"is killed by the blow.\" The party block's grammar never meets a\ndowned member.\n\nWhat remains of the concern — that the player most needs to understand exactly what is true at the\nmoment they lose — is owned by emberdelve-4h9.5, which ships `DelveEnded` with a `PARTY_LOST`\nending and replaces the client-side defeat check.\n\nRuled out of scope on the map rather than resolved: it is not a step on the route, it is a branch\nthe route no longer passes.","created_at":"2026-09-09T23:58:42Z"}],"dependency_count":0,"dependent_count":2,"comment_count":1}
{"_type":"issue","id":"emberdelve-4h9.14","title":"The projection carries the delve","description":"§15 names this as the expensive part of M4 and nothing in this epic owned it: \"the projection work\nthat carries hurt, clock and objective state across a room boundary as fiction, which is the same\nproblem M3 is already inside.\"\n\nThe projection is the hand-built markdown block at `DmService.java:842` — `## The fight`,\n`## Momentum`, `## Established`, `## Ways out`, `## Grid`. It never truncates, which is the whole\npoint of it against the six-turn transcript window, so everything added here is in every prompt for\nthe rest of the session.\n\nWhat it has to carry after M4: hurt, the running clocks, whether the objective is held, and what\nconsumables remain.\n\n**`## Ways out` is the cautionary tale and it is three weeks old.** The block listed doors and said\nnothing about their state, so the model re-improvised the answer every turn — both 2026-09-07\nsessions invented an ATHLETICS DC 20 to kick a door that was never shut, and one spent two turns on\na key against a lock that does not exist. A projection block that is vague about state is worse than\nno block, because the model fills the gap and is then consistent about the wrong thing.\n\nTwo rules that are not negotiable:\n\n- **Nothing here may produce a number the narrator would read aloud.** `dm.md` forbids reading hit\n  points; the fight block forbids stating them; `AGENTS.md` says a wound is described, not counted,\n  because \"handing the model a fraction is an invitation to read it.\" emberdelve-pa8 records that\n  `## Established` already leaks grid coordinates, and every block added is another surface for it.\n- **Terse wins.** `AGENTS.md`: replacing the length rule with a seven-line explanation took narration\n  from 514 to 654 characters; two blunt lines took it to 393. Measure before believing an edit\n  helped.\n\n## The form is decided — emberdelve-ffi.1, 2026-09-09\n\nThis ticket is the build. Do not re-decide any of it; the reasoning is in that ticket's\nresolution comment.\n\n- **One block, `## The party`, placed above `## Entities present` and below nothing.** Always\n  present, never conditional — a conditional pressure block makes its *absence* information, and\n  the model reads that absence as \"no pressure\" on the turn a clock starts running.\n- **The party moves out of `## Entities present`.** That list holds two lifetimes once\n  emberdelve-4h9.1 lands: session-scoped party, room-scoped hostiles rebuilt from room content.\n  The block that survives a crossing must not be the one that gets rebuilt. It stays a list —\n  invariant 2.\n- **`DmService.java:797`'s `5/12 hp` goes.** It ships a fraction for every entity in both prompts\n  today and no decision ever asked for it. It contradicts AGENTS.md and `BeatRenderer`.\n- **Hurt is a band, from one shared source.** Extract `BeatRenderer.condition()`'s four —\n  `barely marked` / `bloodied` / `badly hurt` / `barely standing` at 0.7 / 0.4 / 0.15 — and use\n  the same function in both places. Not a second set of thresholds. A creature described one way\n  in a beat and another way in the block above it is the copper-key failure in miniature.\n- **`## Momentum` keeps its count.** The rule: *a count that drives a tool decision stays a count;\n  a count that only describes state becomes a band.* Momentum feeds `dm-tools.md`'s two-failure\n  escalation rule, and emberdelve-vy7 is what a vague version costs. Consumables keep their counts\n  for the same reason — `use_item` takes one.\n- **Both clocks, as bands, plus a fact-not-cue line.** LIGHT and ALERT. The line is not\n  boilerplate: `## Secrets` proves this model narrates a thing into being when a block reads as a\n  promise. The engine owns the dark. ALERT's band *vocabulary* is emberdelve-ffi.2's.\n- **The objective gets a state line and a closed-world sentence.** The prop listing says where it\n  is; it cannot say the party carries it out of a room the prop no longer belongs to.\n- **All three consumable counters, always, zeroes included.** An omitted line is an invitation.\n- **No new text in `dm.md` or `dm-tools.md`.** Facts in the projection, permissions in the prompts,\n  nothing said twice. The fact-not-cue line inside the block is the only instruction added\n  anywhere.\n- **Grid coordinates stay on the party line.** `move_entity` needs bounds and adjacency;\n  `dm.md:47` already forbids saying one aloud.\n\nThe exact wording of the block is in the resolution comment on emberdelve-ffi.1. Build that, then\nmeasure — AGENTS.md's rule about believing an edit helped applies to this block as much as to the\nlength rule.\n\n**Downed is not covered here** — emberdelve-ffi.8.","acceptance_criteria":"Hurt, running clocks, whether the objective is held, and remaining consumables all reach the DM prompt in the form ffi.1 decided, and survive a room crossing. A test locks the headings and asserts no grid coordinate and no hit point fraction appears anywhere in the projection.","status":"open","priority":2,"issue_type":"feature","owner":"hampton.j.black@gmail.com","created_at":"2026-09-08T23:25:23Z","created_by":"hampton.black","updated_at":"2026-09-09T21:04:20Z","dependencies":[{"issue_id":"emberdelve-4h9.14","depends_on_id":"emberdelve-ffi.1","type":"blocks","created_at":"2026-09-08T18:25:45Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.14","depends_on_id":"emberdelve-4h9","type":"parent-child","created_at":"2026-09-08T18:25:23Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"emberdelve-4h9.13","title":"Downed is a clock, not a screen","description":"Today 0 hit points flips a screen on. Settled on the M4 map (emberdelve-ffi): 0 hit points drops you\nand starts a short clock; if it fills before combat ends, the delve is lost.\n\nWhy a clock rather than the two obvious alternatives:\n\n- **Instant death** makes the gate's own sentence false. §9 wants you \"at 5 hit points and genuinely\n  not know whether to open it\" — at HP 12 against 1d6+2, five hit points is one bad roll from over,\n  so the decision is not tense, it is already lost. A downed state widens the band where the\n  question is live.\n- **Death saves** are §6, and §15 puts the SRD engine in the next milestone with the content it\n  adjudicates. A roll-per-turn here is the rules engine arriving early.\n- **Recovering when the fight ends** is free at party size one: if you are the only combatant, the\n  fight cannot end while you are down.\n\nThe clock is the primitive 4h9.3 builds anyway, at a scale it is already sized for, and the shape\nstays correct when the party grows past one.\n\nSegment count is emberdelve-ffi.6's answer.\n\nWatch the narration: `dm.md` forbids reading hit points aloud and the fight block forbids stating\nthem, so downed is a hit point state the narrator has to convey without counting. **That did not\nfold into emberdelve-ffi.1 as this ticket hoped** — ffi.1's band vocabulary bottoms out at \"barely\nstanding\", which is a creature still on its feet, and downed is a different state with a clock\nattached. It graduated to emberdelve-ffi.8, which now blocks this ticket.","acceptance_criteria":"0 hit points emits a downed event and starts a clock rather than ending the delve. A test downs the player, ends the fight before the clock fills, and asserts they are up; a second lets it fill and asserts DelveEnded with PARTY_LOST. resolveAttack is unchanged in shape and nothing models a condition.","status":"closed","priority":2,"issue_type":"feature","owner":"hampton.j.black@gmail.com","created_at":"2026-09-08T23:24:59Z","created_by":"hampton.black","updated_at":"2026-09-09T23:58:51Z","closed_at":"2026-09-09T23:58:51Z","close_reason":"Downed clock ruled out of M4 by emberdelve-ffi.6: isOver() ends the fight when the only player-controlled entity drops, so the clock is inert at party size one, and the 5-hp premise it rests on measured false. 0 hp is PARTY_LOST.","dependencies":[{"issue_id":"emberdelve-4h9.13","depends_on_id":"emberdelve-4h9","type":"parent-child","created_at":"2026-09-08T18:24:59Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.13","depends_on_id":"emberdelve-ffi.8","type":"blocks","created_at":"2026-09-09T16:03:28Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.13","depends_on_id":"emberdelve-4h9.3","type":"blocks","created_at":"2026-09-08T18:25:44Z","created_by":"hampton.black","metadata":"{}"}],"comments":[{"id":"01a0889c-04b9-7534-bec0-ce4fe7ae7088","issue_id":"emberdelve-4h9.13","author":"hampton.black","text":"**Closed by emberdelve-ffi.6 — the downed clock is ruled out of M4.**\n\nTwo of this ticket's three load-bearing claims are measurably false.\n\n**\"At HP 12 against 1d6+2, five hit points is one bad roll from over.\"** Measured: five hit points\nagainst one goblin is a **72% survival**. Mean loss per goblin fight is 2.65 hp with a **median of\nzero**. The fighter was never fragile.\n\n**\"Recovering when the fight ends is free at party size one: if you are the only combatant, the\nfight cannot end while you are down.\"** `CombatEngine.isOver()` (line 147) returns true when no\nliving **player-controlled** entity remains. At party size one, dropping to 0 hp *ends the fight* —\nso a clock resolved on \"if it fills before combat ends\" can never fill. The mechanism is inert by\nconstruction, and the acceptance criterion here (\"a test downs the player, ends the fight before the\nclock fills, and asserts they are up\") describes something that would pass trivially and always.\n\n**0 hit points is `PARTY_LOST`, immediately.** The tension a downed state was meant to buy is\nbought instead by the brute's doubt window — 4-10 hit points, 45.7% survival at exactly 5 — and\nbought *before* the door rather than after the drop, which is where §9 puts the decision.\n\nThe \"not a screen\" half is already owned by emberdelve-4h9.5: `godot/chrome/defeat.gd`'s\nclient-side `hp \u003c= 0` check on a hardcoded `\"fighter\"` goes with `DelveEnded`.\n\nRe-enters with a second party member, or with §6's death saves in the milestone that brings the SRD\nengine. Not deleted — the reasoning is the point.","created_at":"2026-09-09T23:58:41Z"}],"dependency_count":2,"dependent_count":1,"comment_count":1}
{"_type":"issue","id":"emberdelve-4h9.12","title":"The party carries a light, and the clock is its fuel","description":"The party carries no light. `room.gd:315` builds wall torches from the room's own `LightingPreset`,\ncapped by a per-room `MAX_TORCH_LIGHTS` budget, and `Room.Level` is a separate axis the file is\nemphatic about not confusing with it. So the LIGHT clock §9 names first has nothing to burn down.\n\nSettled on the M4 map (emberdelve-ffi): light moves to the party. A carried lantern, fuelled by the\nLIGHT clock, read as a shrinking radius. Room torches stay room-owned so `Level.DIM` keeps its one\nmeaning — the \"one meaning per colour\" rule the health bar already taught this project.\n\nThe `torch` consumable refuels it. §9's Consumable enum is already potion / torch / rope, so this\nlinks 4h9.2's counter map to 4h9.3's clock and states the greed loop in one line: how deep you go is\nhow many torches you brought.\n\n**The server ships the radius; the client never derives it** (invariant 1), for the same reason\n`SceneState.blocked` and `CombatView.legalMoves` are shipped rather than computed. A client that\npicked its own radius would render a replayed session differently from the session.\n\nThe 2026-09-07 playtest recorded that invented lanterns already travel with the party as facts with\nno prop behind them, and `kaykit_halloween` ships an unused `lantern_standing`. This is the hotdog\nproblem solved the cheap way round: give the board the thing the fiction keeps inventing.\n\nObject model is emberdelve-ffi.3; how the radius reads is emberdelve-ffi.5. This ticket is the\nbuild.","acceptance_criteria":"The party carries a light whose fuel is the LIGHT clock, folded from events. The server ships the radius; the client never derives it. Spending a torch refuels it. A test fills the clock and asserts the light state changes; a second spends a torch and asserts it recovers. Room torches and MAX_TORCH_LIGHTS are untouched.","notes":"**Settled by emberdelve-ffi.6: a torch resets the LIGHT clock to empty**, rather than adding a\nfixed number of segments. LIGHT is 6 segments and **the party starts with 2 torches**, so the whole\nlight budget of a delve is 18 ticks.\n\nAgainst that: a careful five-room delve is about 9 ticks, a greedy one about 15. Comfortable, then\ntight -- the greed decision stated in torches.\n\nThe tick rule that makes the radius mean something: **every room entry ticks, including a room\nalready visited**, so going one room deeper costs two segments, one in and one out. See\nemberdelve-4h9.3, which also carries the reversal on what a failed check does (nothing; a natural 1\nticks one).\n\nNumbers and method: `docs/evidence/m4-attrition-sim.py`.","status":"open","priority":2,"issue_type":"feature","owner":"hampton.j.black@gmail.com","created_at":"2026-09-08T23:24:58Z","created_by":"hampton.black","updated_at":"2026-09-09T23:57:55Z","dependencies":[{"issue_id":"emberdelve-4h9.12","depends_on_id":"emberdelve-ffi.5","type":"blocks","created_at":"2026-09-08T18:25:43Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.12","depends_on_id":"emberdelve-ffi.3","type":"blocks","created_at":"2026-09-08T18:25:43Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.12","depends_on_id":"emberdelve-4h9","type":"parent-child","created_at":"2026-09-08T18:24:58Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.12","depends_on_id":"emberdelve-4h9.3","type":"blocks","created_at":"2026-09-08T18:25:42Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":3,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"emberdelve-4h9.11","title":"Rest — the pressure valve","description":"Nothing in this epic builds a rest, and §9 requires one: \"Healing is scarce, and rests are a clock\ntick rather than a free reset.\" 4h9.3 names rests as a tick trigger and stops there; 4h9.2 covers\npotions. The server contains no rest and no healing of any kind today.\n\nSettled on the M4 map (emberdelve-ffi):\n\n- An explicit player action, not something the narrator can grant. §9 is blunt about why — \"the\n  model always says yes\", and the engine is the only thing here that can make something scarce.\n- Legal only in a room with no living hostile in it. An engine rule, one line, not authored content\n  — a content-marked safe room would not survive contact with generated rooms next milestone.\n- Ticks every running clock one segment.\n- Restores a fixed small amount. Not a roll: the dice are for questions the DM is about to answer,\n  and \"how much did you recover\" is not a moment. Fixed and small keeps the arithmetic in the\n  player's head, which is where the tension lives.\n\nThe rest is the pressure valve that makes the clock legible — you spend a segment on purpose and\nwatch it fill. Without it, \"go deeper or get out\" is a binary the player makes once.\n\nEvent and fold like everything else (invariant 8). Mechanics-phase tool, not reconcile: a false\npositive spends clock segments the player did not agree to.\n\nThe amount, and how hard a rest ticks, are emberdelve-ffi.6's answer, not this ticket's.","acceptance_criteria":"Rest is a mechanics-phase tool, validated server-side, rejected in a room holding a living hostile. It emits an event; the restoration and every clock tick fold from it. A scripted test rests, asserts hit points up and every clock advanced by one, with no model in the path. The narrator cannot grant a rest by describing one.","notes":"**A rest restores 4 hit points** -- settled by emberdelve-ffi.6. A fifth of the 20-point bar.\n\nChosen for legibility, not balance, and the sim says so plainly: across a whole delve a 3-point\nrest and an 8-point rest differ by under 1.5%, because the fights dominate. So the number should be\none a player can hold in their head while deciding whether a segment is worth it. A potion restores\n8 (emberdelve-4h9.2) -- deliberately twice a rest, because being visibly stronger than the free\noption is what justifies it being finite.\n\nTicking stays as settled: one segment on every running clock.\n\nNumbers and method: `docs/evidence/m4-attrition-sim.py`.","status":"open","priority":2,"issue_type":"feature","owner":"hampton.j.black@gmail.com","created_at":"2026-09-08T23:24:57Z","created_by":"hampton.black","updated_at":"2026-09-09T23:57:54Z","dependencies":[{"issue_id":"emberdelve-4h9.11","depends_on_id":"emberdelve-4h9.3","type":"blocks","created_at":"2026-09-08T18:25:41Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.11","depends_on_id":"emberdelve-4h9","type":"parent-child","created_at":"2026-09-08T18:24:57Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.11","depends_on_id":"emberdelve-ffi.6","type":"blocks","created_at":"2026-09-08T18:25:42Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"emberdelve-ffi.7","title":"Write the M4 design spec","description":"## Question\n\nWrite the M4 design spec.\n\n`docs/superpowers/specs/2026-09-XX-m4-delve-design.md`, in the shape of\n`2026-09-05-m3-traversal-design.md` and `2026-08-23-m2-spine-design.md`.\n\nThis is the map's destination. It is written in **one pass at the end**, from the map's Settled\nblock and the resolution comments on every closed ticket — deliberately, because M1/M2/M3's specs\nread as documents with an argument, and a case written in eight sittings by eight agents is not one.\n\n## What goes in it\n\nThe map's `## Settled at charting` block is the raw material for most of it; each closed ticket's\nresolution comment is the rest. The spec should make the *argument*, not minute the decisions —\nwhere §9 was a bet, say what play and design have since settled and why.\n\nSpecifically it has to carry:\n\n- The delve as a state with an ending, and why `Ending` is descriptive rather than a verdict.\n- Attrition: the light clock, the carried lantern, the torch that refuels it, rest, and downed.\n- What the engine owns and what the narrator owns, restated for the clock — §9's \"it never decides\n  that the torch went out\" is the sentence the whole section hangs on.\n- The projection, per emberdelve-ffi.1.\n- What M4 deliberately does not build, and the re-entry condition for each.\n- What this milestone hands the generator, since M1's outstanding half comes next and 4h9.8 is\n  already collecting the list.\n\n## After it is signed\n\nUpdate `AGENTS.md` — the milestone table, the shortcuts table (the two-room shortcut and the\n\"do not add a third room\" anti-goal are retired by 4h9.8), and the LLM tools list if `use_item` or\nanything else lands. `AGENTS.md` is the operational file and it wins where it and the design doc\ndisagree, so it cannot lag.\n\nAmend `docs/ai-dm-system-design.md` §2a with whatever M4 reversed. Every milestone so far has\nreversed something; §9 was written expecting to.\n\n`CONTEXT.md` gains the delve vocabulary — delve, clock, sign, consequence, objective, rest, downed.\nIt is a glossary and not a second rules file.","acceptance_criteria":"The spec exists, reads as a document with an argument rather than minutes, and carries the delve as a state with an ending, attrition, the engine/narrator line for the clock, the projection, what M4 does not build, and what it hands the generator. AGENTS.md, design doc 2a and CONTEXT.md updated.","status":"open","priority":2,"issue_type":"task","owner":"hampton.j.black@gmail.com","created_at":"2026-09-08T23:23:53Z","created_by":"hampton.black","updated_at":"2026-09-08T23:27:17Z","labels":["wayfinder:map","wayfinder:task"],"dependencies":[{"issue_id":"emberdelve-ffi.7","depends_on_id":"emberdelve-ffi.8","type":"blocks","created_at":"2026-09-09T16:03:27Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-ffi.7","depends_on_id":"emberdelve-ffi.6","type":"blocks","created_at":"2026-09-08T18:24:21Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-ffi.7","depends_on_id":"emberdelve-ffi.3","type":"blocks","created_at":"2026-09-08T18:24:20Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-ffi.7","depends_on_id":"emberdelve-ffi.5","type":"blocks","created_at":"2026-09-08T18:24:21Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-ffi.7","depends_on_id":"emberdelve-27l","type":"blocks","created_at":"2026-09-08T18:24:22Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-ffi.7","depends_on_id":"emberdelve-ffi","type":"parent-child","created_at":"2026-09-08T18:23:53Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-ffi.7","depends_on_id":"emberdelve-ffi.1","type":"blocks","created_at":"2026-09-08T18:24:19Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-ffi.7","depends_on_id":"emberdelve-ffi.4","type":"blocks","created_at":"2026-09-08T18:24:20Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-ffi.7","depends_on_id":"emberdelve-ffi.2","type":"blocks","created_at":"2026-09-08T18:24:19Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":8,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-ffi.6","title":"The numbers — tune attrition until 5 hit points is a real decision","description":"## Question\n\nWhat are the numbers, such that standing in front of a door at 5 hit points is a real state?\n\n§9's gate is stated as something to feel: \"you should be able to stand in front of a door at 5 hit\npoints and genuinely not know whether to open it.\" That is an arithmetic claim as much as a design\none, and today's numbers do not support it.\n\n## Why today's numbers fail the gate\n\n`AGENTS.md` shortcuts: Fighter AC 16, HP 12; Goblin +4 to hit, 1d6+2 damage. At HP 12 against\n1d6+2, five hit points is one bad roll from over. The decision at that door is not tense — it is\nalready lost, and a player who understands the numbers knows it. The band in which the gate's\nquestion is live has to be wide enough to stand in.\n\nThis is a `--demo` job. `ScriptedDiceRoller` exists for exactly this: reproducible tuning runs with\nno model in the path.\n\n## What has to be decided\n\n- **Party hit points**, and whether the fighter's 12 moves.\n- **The rest's restoration.** Settled: a fixed small amount, no roll. What amount.\n- **Clock segment counts** — LIGHT, ALERT, and the downed clock. §9's record has `segments` as a\n  field precisely so this is a number and not a constant.\n- **How many torches the party starts with**, which is the greed loop's actual dial: how deep you go\n  is how many torches you brought.\n- **Potion count**, same reasoning.\n- **How hard a rest ticks.** Settled at one segment per clock; whether that survives contact with a\n  4-5 room site is what this ticket measures.\n- **Whether the goblin's damage moves**, and what a second stat block (4h9.9) is worth in the same\n  arithmetic.\n\n## The shape of the answer\n\nA delve of 4-5 rooms in which a careful player reaches the objective with resources left, a greedy\none is genuinely in danger going deeper, and the light clock can fill before the site is exhausted.\nStated as numbers, with the scripted run that produced them.\n\n## Constraint\n\nDo not build a rules engine to get there. The forty lines of `CombatEngine.resolveAttack` are the\nanti-goal's own example, and every dial named above is a content or config number.","acceptance_criteria":"Numbers for party hit points, rest restoration, every clock's segments, starting torches and potions, stated with the --demo run that produced them. A careful delve leaves resources spare; a greedy one is genuinely in danger; the light clock can fill before the site is exhausted.","status":"closed","priority":2,"issue_type":"task","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-08T23:23:52Z","created_by":"hampton.black","updated_at":"2026-09-09T23:58:43Z","started_at":"2026-09-09T23:24:06Z","closed_at":"2026-09-09T23:58:43Z","close_reason":"Resolved: fighter HP 20, goblin weakened to AC12/HP6/+3/1d4+1, new brute AC13/HP16/+4/1d8+2 landing the doubt window on 4-10 with 45.7% survival at exactly 5 hp. Rest 4, potion 8, LIGHT and ALERT 6 segments, 2 torches, 2 potions, 5-room site. Two charting reversals: failed checks no longer tick LIGHT (nat 1 only), and the downed clock is dropped.","labels":["wayfinder:map","wayfinder:task"],"dependencies":[{"issue_id":"emberdelve-ffi.6","depends_on_id":"emberdelve-ffi","type":"parent-child","created_at":"2026-09-08T18:23:52Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-ffi.6","depends_on_id":"emberdelve-ffi.2","type":"blocks","created_at":"2026-09-08T18:24:18Z","created_by":"hampton.black","metadata":"{}"}],"comments":[{"id":"01a0889a-3c54-7d4a-af73-4874b847f3f7","issue_id":"emberdelve-ffi.6","author":"hampton.black","text":"## Resolved\n\nEvery number below comes from `docs/evidence/m4-attrition-sim.py`, seeded, which mirrors\n`CombatEngine.resolveAttack` and `GoblinAi` exactly — natural 20 auto-hits and doubles the damage\ndice only, natural 1 auto-misses, initiative ties go to the goblin's DEX +2, and a creature that\nstarts away from its foe spends its first turn closing.\n\n**This ticket could not be the `--demo` job it was filed as.** `ScriptedDiceRoller` gives\nreproducible dice, but there is no delve loop to run them through — no clock, no rest, no site. The\nreproducible artifact is the simulator instead, committed so every figure in the spec can be\nre-derived.\n\n### The ticket's premise was wrong, and wrong in the informative direction\n\nIt said that at HP 12 against 1d6+2, five hit points is \"one bad roll from over\". Measured, it is a\n**72% survival**. A single goblin costs a mean of 2.65 hp, a **median of zero**, and drops the\nfighter 8.6% of the time. The fighter was never fragile; the goblin was never a threat.\n\nWorse, and this is the finding the whole resolution turns on: **hit points barely inform the\ndecision at all.** 1 hp survives 65%, 5 hp survives 72%, 12 hp survives 91%. What kills you is\nwhether the goblin connects, not what you had left. The number §9 asks the player to agonise over\nwas not carrying information.\n\n### The doubt window, and the two dials that set it\n\nThe **doubt window** is the run of hit points where P(surviving the next room) is between 40% and\n70% — where the gate's sentence is literally true. It has two independent owners, and conflating\nthem is what made this ticket hard to start:\n\n- **The encounter sets where the window sits.** Max HP does not move it: HP 20 and HP 24 against\n  two goblins have the *identical* window, 8–16.\n- **Max HP sets how much bar there is above the window** — the run-up. At HP 12 the brute's window\n  (4–10) is more than half the bar, so the player is in coin-flip territory from the first fight\n  and the delve has no early phase to spend.\n\nToday's window is **1–4 hp, four points wide, sitting below the number the gate names.**\n\n### Party size stays at one\n\nA second body collapses the danger by fourteen times — party of 1 versus two goblins wipes 42.8%,\nparty of 2 wipes 3.1%. Invariant #2's `List\u003cPartyMember\u003e` keeps the door open at no cost. M4's gate\nis about attrition, not party tactics.\n\n### The action economy inverts the obvious encounter ladder\n\nAgainst a party of one, **N weak enemies is always harder than one strong enemy**, because each of\nthem takes a full turn while the fighter takes one. Dropped-from-full at HP 12: one brute 23.7%,\ntwo goblins 43.2%, three goblins 79.9%. \"A few weak mobs\" cannot be the easy fight while the goblin\nis AC 15 / HP 7 / 1d6+2.\n\n### The numbers\n\n| | Was | Is | Why |\n|---|---|---|---|\n| Fighter max HP | 12 | **20** | Buys the run-up above the doubt window. The delve-length dial; nothing else does that job. |\n| Goblin | AC 15, HP 7, +4, 1d6+2 | **AC 12, HP 6, +3, 1d4+1** | Weakened so packs can be the *easy* fight. HP 6 keeps it at 75% one-shot on a hit — usually one blow, sometimes two. HP 4 or 5 dies to every hit, which makes a pack a formality. |\n| Brute (new, 4h9.9) | — | **AC 13, HP 16, +4, 1d8+2** | Lands the doubt window on **4–10, 50% at 6 hp, 45.7% survival at exactly 5**. Drops a full-health fighter only 8.2% of the time: a fight you almost never lose fresh and genuinely might lose hurt. |\n| Rest restores | — | **4** | A fifth of the bar. |\n| Potion restores | — | **8** | Two fifths — visibly worth more than the free option, which is what justifies it being finite. |\n| LIGHT segments | — | **6** | |\n| ALERT segments | — | **6** | Signs at 2 / 4 / 5, as `emberdelve-ffi.2` wrote them. |\n| Starting torches | — | **2** | An 18-tick light budget against a careful delve's ~9 and a greedy delve's ~15. |\n| Starting potions | — | **2** | |\n| Site | — | **5 rooms, ~3 holding a fight** | Objective shallow; two rooms of optional depth, which is where the greed decision lives. |\n\n**The rest and potion figures were chosen for legibility, not balance.** A 3-point rest and an\n8-point rest differ by under 1.5% across a whole delve — the fights dominate completely. Round\nfractions of the bar are worth more than a tuned number nobody can hold in their head.\n\n### The encounter ladder, against fighter HP 20\n\n| Encounter | Dropped from full | 50% at | Doubt window | Survives at 5 hp |\n|---|---|---|---|---|\n| 2 mobs | 1.3% | 3 hp | 1–5 | 63.3% |\n| 1 brute | 8.2% | 6 hp | 4–10 | **45.7%** |\n| 3 mobs | 12.0% | 9 hp | 8–13 | 27.5% |\n| brute + 2 mobs | 52.7% | 20 hp | 18–20 | 5.8% |\n\nTwo shapes land on comparable weight — one brute and three mobs — which is the variety the site\nwants without a difficulty cliff. **brute + 2 mobs is not a designed room**; it is what the ALERT\nclock does to you.\n\n### The whole delve\n\n| | Lost | HP left | Items unspent |\n|---|---|---|---|\n| Careful — 3 fights, objective, out | **26.1%** | 10.8 / 20 | 2.51 |\n| Greedy — 5 fights, two rooms deeper | **77.1%** | 7.7 / 20 | 0.98 |\n\nStated honestly: the careful figure assumes the player **fights everything they meet and never\navoids anything.** Avoidance is what the ALERT signs are for, and a player who reads them should sit\nbelow 26%. It is not tuned lower to look better.\n\nNote what this interacts with: **exits are illegal in combat** (locked at M2, no fleeing). Every\nfight is to the death. So the greed decision is \"do I open this door\", never \"do I finish this\nfight\" — which is exactly why the danger has to be legible *before* the door, and exactly what the\nALERT signs are for.\n\n### The light budget, and why depth costs two\n\nLIGHT ticks on every room entry, every rest, and every natural 1. **Every room entry ticks,\nincluding a room already visited** — the lantern does not care that you have been here. So **going\none room deeper costs two segments, one in and one out**, which is what turns depth from vaguely\nrisky into computable. A torch resets LIGHT to empty. A careful five-room delve is ~9 ticks; a\ngreedy one ~15; two torches plus the starting fill is 18.\n\n## Two decisions settled at charting that do not survive contact\n\n### The failed-check tick hands the scarcity budget to the model\n\nCounted in the recorded sessions. `session-m2-twenty-turns.jsonl`: 20 checks, **14 failures**, 3\nnatural 1s. `session-m3-traversal.jsonl`: 11 checks, **2 failures**, none natural. Same game, same\nplayer — a sevenfold difference in fill rate, because M2's DM reached for DC 15–25 and M3's reached\nfor DC 5–10.\n\nA LIGHT clock fed by that has its fill rate set by the narrator's taste in difficulty. That is the\nopposite of `emberdelve-ffi.2`'s reason for making LIGHT predictable, and it hands scarcity to the\none component §9 says must never own it: *\"the engine is the only thing in this system that can\nmake something scarce.\"*\n\n**Reversed: a natural 1 ticks LIGHT one segment; an ordinary failed check ticks nothing.** The play\nobservation that motivated the original rule survives intact — the dropped lantern on a 1 is still\nan engine tick, a fired table and a narrated event. It lands once or twice a delve. Charting\nrejected nat-1-alone as \"too rare to build pressure\", but that was judged against\nfailed-check-alone; the room tick carries the pressure, and it is fully player-controlled.\n\n### The downed clock cannot fill\n\n`CombatEngine.isOver()` (line 147) returns true when no living **player-controlled** entity remains.\nAt party size one, dropping to 0 hp *ends the fight* — so a clock resolved on \"if it fills before\ncombat ends\" is inert by construction. `emberdelve-4h9.13` states the opposite in its own body\n(\"the fight cannot end while you are down\"), and it also rests on the 5-hp premise this ticket\nmeasured false.\n\n**Reversed: 0 hit points is `PARTY_LOST`, immediately.** The tension the downed clock was meant to\nbuy is already bought by the brute's 4–10 window, and bought *before* the door rather than after the\ndrop, which is where §9 puts the decision. `emberdelve-4h9.13` is closed and the downed clock is\nruled out of M4; it re-enters with a second party member or with §6's death saves.\n\n## Build consequences\n\n- **`CombatEngineTest` builds through `ContentLoader`**, so it reads the real `fighter.json` and\n  `goblin.json`. Changing either moves every scripted-dice fixture in it — the faces are chosen\n  against AC 15 and 7 hp. Real work, flagged on `emberdelve-4h9.9`, not a content-only edit.\n- **Recorded logs are unaffected.** `EntitySpawned` carries the whole `Entity`, so a replayed\n  session uses the stats it was written with. No schema bump, no re-record.\n- **`AGENTS.md`'s shortcuts table becomes untrue the moment this lands** — the Fighter and Goblin\n  rows both. It is deliberately not edited now: this map plans, and a rules file describing a build\n  that has not happened is worse than one that is merely out of date.\n","created_at":"2026-09-09T23:56:45Z"},{"id":"01a08b4b-c379-7153-8373-9370c5b93122","issue_id":"emberdelve-ffi.6","author":"hampton.black","text":"The reversal on what ticks LIGHT is recorded as [ADR-0013](docs/adr/0013-scarcity-is-the-engines-not-the-narrators.md), *Scarcity is the engine's, not the narrator's* — the general rule behind it, which is that no engine resource may be spent as a consequence of a value the model selected. The failed-check tick looked engine-owned because every line of it was server-side; what leaked was that `roll_check`'s difficulty enum, closed and validated exactly as invariant #7 requires, still lets the model choose the failure rate and therefore the burn rate.\n\nThe numbers themselves stay here rather than in the ADR: they are content, they will move when the site is authored, and the spec is written from the resolution comments.","created_at":"2026-09-10T12:29:53Z"}],"dependency_count":1,"dependent_count":2,"comment_count":2}
{"_type":"issue","id":"emberdelve-ffi.5","title":"How the shrinking light radius reads","description":"## Question\n\nHow does the shrinking light radius read?\n\nCall the `prototype` skill. Settled at charting: the party carries a lantern, the LIGHT clock is its\nfuel, and radius is the read — the pool of light shrinks as the clock fills. Radius was chosen over\ncolour-and-flicker because it changes what the player can *do*: less visible floor is less floor to\nclick, so the clock has mechanical teeth without the engine growing a visibility rule.\n\nThat is the decision. This ticket is the judgement, and it is a judgement made by looking.\n\n## What has to be decided\n\n- The radius at full and at empty, and the curve between them.\n- Whether the falloff is soft or hard, and whether the edge is a place the player reads as a\n  boundary or as atmosphere.\n- Whether anything else moves with it — colour, flicker rate, the shadow behaviour of the room's own\n  torches at the edge of the pool.\n- What \"less floor to click\" actually means mechanically, or whether it stays purely visual for M4.\n  Note this is the expensive branch: a real visibility rule is server-owned under invariant 1, the\n  way `SceneState.blocked` and `legalMoves` are, and it is not obviously in scope.\n\n## Two things that will waste the session if forgotten\n\n**Turn off the editor's Preview Sun and Preview Environment.** They are on by default and stack on\ntop of whatever environment a tool scene adds, so the room reads far brighter than the game. From\nthe 4h9.10 spike notes: every appearance decision made under them is made under the wrong light.\n\n**The anti-goal is real.** `AGENTS.md`: \"Do not tune lighting and post-processing for more than one\nevening. Timebox it. This is the single largest time sink in the project and it will consume as\nmuch as you give it.\"\n\n## Blocked on\n\n- emberdelve-27l — at ambient 1.1 a shrinking radius will not read at all.\n- emberdelve-ffi.3 — the prototype needs a thing to shrink, and where the light node lives is that\n  ticket's answer.","acceptance_criteria":"A radius at full and at empty, a falloff, and a screenshot of each judged with Preview Sun and Preview Environment OFF. A stated answer on whether the shrinking pool is visual only in M4. Timeboxed to one evening.","status":"open","priority":2,"issue_type":"task","owner":"hampton.j.black@gmail.com","created_at":"2026-09-08T23:23:20Z","created_by":"hampton.black","updated_at":"2026-09-08T23:27:16Z","labels":["wayfinder:map","wayfinder:prototype"],"dependencies":[{"issue_id":"emberdelve-ffi.5","depends_on_id":"emberdelve-27l","type":"blocks","created_at":"2026-09-08T18:24:17Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-ffi.5","depends_on_id":"emberdelve-ffi","type":"parent-child","created_at":"2026-09-08T18:23:20Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-ffi.5","depends_on_id":"emberdelve-ffi.3","type":"blocks","created_at":"2026-09-08T18:24:18Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"emberdelve-ffi.4","title":"How the end of a delve reads on screen","description":"## Question\n\nHow does the end of a delve read on screen?\n\nCall the `prototype` skill. This is a \"how should it look\" question and the cheapest way to answer\nit is a rough artifact to react to.\n\n## What exists\n\n`godot/chrome/defeat.gd` and the `Overlay/Defeat` node in `chrome.tscn` — a veil, a line of text,\nand a Restart button. It survived the Godot migration intact. Its docstring carries the finding\nthat produced it: \"A dead player character used to be only a dropped token: combat ended, the mode\npill flipped back, and nothing said what had happened. With the input box refusing text and the\nboard refusing clicks, that reads as a crash rather than a death.\"\n\nIt is also the thing being replaced. It decides the run is over client-side, from\n`Table.entity(\"fighter\")` with `hp \u003c= 0` hardcoded — it cannot distinguish three endings, and it\nnames a singleton player against invariant 2. The delve ending becomes a `DelveEnded` event and the\nclient renders what it is told.\n\n## What has to be decided\n\n- **What \"what was spent\" actually shows.** 4h9.5 says the sting is in what was spent — rooms\n  entered, potions drunk, what was carried, what was left behind. Which of those earn a line, and\n  whether a number or a phrase.\n- **Whether the DM narrates it.** 4h9.5: \"That is a read of state, not a narration call, though a\n  narration call over it is cheap and probably right.\" A narration call here is the last thing the\n  player hears, which is either the best place in the game for one or the worst place to be\n  waiting. Note `openScene` measures 515-712ms to first token, 1.8-3.1s total, for a comparable\n  single-phase prose call.\n- **Whether the three endings share one screen.** Extracting with the objective and being wiped are\n  the same *moment* structurally and completely different emotionally.\n- **What the way on is.** Restart today throws the session away. `GameEngine.restart()` reuses the\n  original `SessionStarted` header, so under `--demo` a restarted delve is the *same* delve, same\n  seed. That is a gift for tuning and a problem for a player who just lost.\n- **Whether the board stays visible behind it.** The veil hides what happened; the loss is on the\n  board.\n\n## Constraint\n\nOnly `PARTY_LOST` is losing (settled at charting). Extracting empty-handed is a legitimate cautious\noutcome and the screen must not read as a scolding — §9's greed decision only works if leaving\nearly is respectable.","acceptance_criteria":"A rough artifact linked from this ticket, and a decision on what the ending shows for each of the three endings, whether the DM narrates it, and what the way on is.","status":"open","priority":2,"issue_type":"task","owner":"hampton.j.black@gmail.com","created_at":"2026-09-08T23:23:19Z","created_by":"hampton.black","updated_at":"2026-09-08T23:27:16Z","labels":["wayfinder:map","wayfinder:prototype"],"dependencies":[{"issue_id":"emberdelve-ffi.4","depends_on_id":"emberdelve-ffi","type":"parent-child","created_at":"2026-09-08T18:23:19Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"emberdelve-ffi.3","title":"What the carried lantern is, as an object","description":"## Question\n\nWhat is the carried lantern, as an object?\n\nSettled at charting: light moves from the room to the party. `room.gd:315` builds wall torches from\nthe room's own `LightingPreset`, and `Room.Level` is a separate axis the file is emphatic about not\nconfusing with it — so a LIGHT clock has nothing to burn down unless the party carries something.\n\nThat decision leaves the object model open, and it touches three things that already exist.\n\n## What has to be decided\n\n- **Is it a `Prop`?** Props have an id, a closed `PropType`, `hidden`, and live in a room. A carried\n  light lives on the party and moves with it. That is either a new kind of thing or a stretch of an\n  existing one.\n- **Is it a token attachment?** `godot/world/tokens/token.gd` mounts meshes on a skinned figure;\n  `kaykit_halloween` ships an unused `lantern_standing`. Whether the light is visibly *held* is a\n  separate question from where its state lives.\n- **How does it interact with picking?** emberdelve-7m6 is the open question of which props become\n  pick targets, and ADR-0011 (visible is not addressable) is the rule. A lantern the player can\n  click is a lantern they can put down, which is a mechanic nobody asked for.\n- **Where does the light source node live?** Not in `Room` — `MAX_TORCH_LIGHTS` is deliberately a\n  per-room budget and the carried light must not spend it. Probably on the party token or on\n  `World`, and that decision constrains the radius work.\n- **What happens when it goes out?** The lantern presumably still exists at zero fuel. Whether\n  relighting is a `use_item` on a torch, or something else, is the seam between this and 4h9.2.\n\n## Why this is worth deciding before the radius is prototyped\n\nThe prototype needs a thing to shrink. Deciding the look first and the object afterwards is how the\nrenderer ends up owning state (invariant 1) — and the server must ship the radius, not derive it\nclient-side, for the same reason `SceneState.blocked` is shipped rather than computed.\n\n## Prior art worth reading first\n\nThe 2026-09-07 playtest recorded that invented lanterns already travel with the party as facts with\nno prop behind them. The fiction reached for this object before the engine had it.","acceptance_criteria":"A decision on what the carried light is: whether it is a Prop, where its state lives, where the light node lives, whether it is a pick target, and what relighting is. Enough for 4h9.12 to be built and for ffi.5 to have a thing to shrink.","status":"open","priority":2,"issue_type":"task","owner":"hampton.j.black@gmail.com","created_at":"2026-09-08T23:22:29Z","created_by":"hampton.black","updated_at":"2026-09-08T23:27:15Z","labels":["wayfinder:grilling","wayfinder:map"],"dependencies":[{"issue_id":"emberdelve-ffi.3","depends_on_id":"emberdelve-ffi","type":"parent-child","created_at":"2026-09-08T18:22:29Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"emberdelve-ffi.2","title":"What is on the LIGHT and ALERT tables, and the signs under them","description":"## Question\n\nWhat is on the LIGHT and ALERT consequence tables, and on the sign tables under them?\n\n§9 makes the table the point, not an implementation detail: it is \"the same constrain-the-model\npattern as the DC bands in §7 and the prop enum in §5. The DM narrates what the table produced; it\nnever decides that the torch went out.\" A clock with a vague table is a clock that hands the\ndecision back to the model.\n\n## Two tables per clock, because clocks speak twice\n\nSettled at charting: a **threshold** fires a sign, the **fill** fires the consequence. So each clock\nneeds both.\n\n- **Signs** are §9's region-scale device brought down to delve scale: \"spoor, smoke, tracks, rolled\n  a watch ahead of the meeting itself. Gives the player a chance to avoid it and gives the narrator\n  something to foreshadow, which is the cheapest available source of the thing generated content is\n  usually worst at.\" A distant scrape, a door found open, the lantern flame leaning.\n- **Consequences** are the state change. `LIGHT_OUT` is the obvious one on the light table. The\n  alert table's entries are the ones with room to be interesting — and note that room torches\n  guttering is legitimate *here*, as an engine-fired consequence, where it was ruled out as a DM\n  tool.\n\n## What has to be decided\n\n- The entries themselves, on all four tables.\n- Whether a consequence is drawn or fixed. §9 says \"fires a table\", which implies a draw, but a\n  one-entry table is a fixed consequence and is allowed to be.\n- What a consequence is allowed to *do*. `LIGHT_OUT` changes lighting state. May a consequence\n  spawn? Move something? Tick another clock? Each of those is a different amount of machinery, and\n  the anti-goal against building a rules engine applies.\n- How a consequence reaches the client. It has to be an event and a diff like everything else\n  (invariant 8), and the client renders what it is told (invariant 1).\n- Whether the ALERT table can end in combat, given `start_combat` is currently a model decision and\n  emberdelve-vy7 records that the model already escalates too eagerly. An engine-fired fight is\n  arguably the *fix* for that, not a duplicate of it.\n\n## Constraint\n\nClosed enums, validated server-side (invariant 7). `ConsequenceId` is named in §9's record and is\nthe seam.","acceptance_criteria":"Four tables written out: LIGHT consequences, LIGHT signs, ALERT consequences, ALERT signs. Every entry a closed ConsequenceId. A stated rule for what a consequence may do to the world, and whether ALERT may start a fight.","status":"closed","priority":2,"issue_type":"task","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-08T23:22:28Z","created_by":"hampton.black","updated_at":"2026-09-09T22:49:56Z","started_at":"2026-09-09T21:59:28Z","closed_at":"2026-09-09T22:49:56Z","close_reason":"Resolved: four tables written. A consequence may only do what a tool can already do; a sign is a consequence with an empty bundle. LIGHT fixed one-entry, ALERT drawn from four. Full answer in the resolution comment.","labels":["wayfinder:grilling","wayfinder:map"],"dependencies":[{"issue_id":"emberdelve-ffi.2","depends_on_id":"emberdelve-ffi","type":"parent-child","created_at":"2026-09-08T18:22:28Z","created_by":"hampton.black","metadata":"{}"}],"comments":[{"id":"01a0885c-fa4d-7bb8-aebc-3d3358d097aa","issue_id":"emberdelve-ffi.2","author":"hampton.black","text":"# Resolution — 2026-09-09\n\nFour tables, written out below. The rule that made them writable is the **power budget**:\n\n\u003e **A consequence may only do what a tool can already do.**\n\nA `ConsequenceId` is a named, pre-validated bundle of events the engine already has an\n`Event` + `Diff` pair for — `EntitySpawned`, `EntityMoved`, `PropRevealed`, `CombatStarted` —\nplus state the engine already models as a scalar (lighting, a clock's fill). No new verbs, no\neffect language. §12's anti-goal against a rules engine is the reason, and §15 already puts the\neffect interpreter with the content milestone; an effect language reached through the back door\nof a consequence table is the same tax under a different name.\n\n## The unification that made this smaller than expected\n\n**A sign is a consequence whose bundle is empty.** Same record, same table, same directive\nmachinery, same firing path — the only difference is whether anything changes. That is what lets\nthe acceptance criterion (\"every entry a closed `ConsequenceId`\") be literally true rather than\naspirational, and it is why there is **one enum, not two**.\n\nThe cost, stated so it is not discovered later: nothing type-level stops a sign table from\nholding `LIGHT_OUT`. That is a validation rule — a sign table's entries must have empty bundles —\nrather than a type. Taken deliberately over a separate `SignId`, which would make the mistake\nunrepresentable at the price of duplicating the directive machinery.\n\n---\n\n# LIGHT\n\nThe party's lantern. The player can see this clock; the shrinking radius **is** the read\n(`emberdelve-ffi.5` owns how).\n\n## Consequences — one entry, fixed\n\n| `ConsequenceId` | Bundle | Directive register |\n|---|---|---|\n| `LIGHT_OUT` | Party light drops to its lowest level. ALERT +1, declared on the tick side. | The lantern has gone out. |\n\n**One entry on purpose.** LIGHT being fully predictable is what makes the greed decision\n*computable* — the player should be able to plan against the dark and count torches against\nrooms. ALERT is the clock allowed to surprise you, and that asymmetry is most of what\ndistinguishes them. A one-entry table is a fixed consequence and is allowed to be.\n\nThe clock does not stop at full. It sits full until a `torch` is spent, which states the greed\nloop in one line: how deep you go is how many torches you brought.\n\n## Signs\n\n| Segment (of 6) | `ConsequenceId` | What it is about |\n|---|---|---|\n| 2 | `LIGHT_LOW` | The ring of light has drawn in. |\n| 4 | `LIGHT_GUTTERING` | The flame stutters; the shadows swing. |\n| 5 | `LIGHT_FAILING` | Light at arm's length. One more and it is gone. |\n\nQuiet for two segments, then every segment. That acceleration is the whole reason the sign table\nis a map from segment to `ConsequenceId` rather than a single named threshold.\n\n---\n\n# ALERT\n\nHow much the site has noticed you. The player **cannot** see this clock — its entire read is its\nsigns, which is why it carries three of them and why its fill has variety.\n\n## Consequences — drawn\n\n| `ConsequenceId` | Bundle | Disposition |\n|---|---|---|\n| `PATROL_ARRIVES` | `EntitySpawned` in the party's room + `CombatStarted` | `HOSTILE` |\n| `SOMETHING_WANDERS_IN` | `EntitySpawned` in the party's room | `WARY` |\n| `THE_DARK_CLOSES_IN` | Room lighting drops one level | — |\n| `DRAWN_BY_THE_NOISE` | `EntityMoved` — a hostile already in the site crosses into the party's room + `CombatStarted` | `HOSTILE` |\n\n`DRAWN_BY_THE_NOISE` depends on `emberdelve-4h9.9`; until that lands the table draws from the\nfirst three. It is carried because a site whose creatures **converge** is a better site than one\nthat spawns from nowhere, and entities already carry a `roomId` since schema v2, so it is\nexpressible without new machinery.\n\n`THE_DARK_CLOSES_IN` is the entry the ticket explicitly sanctioned: room torches guttering is\nlegitimate *here*, as an engine-fired consequence, where it was ruled out as a DM tool. It is the\none entry with no mechanical bite, and it earns its place — a table that **always** spawns\nsomething is a table the player reads as \"a fight is coming\" after two draws.\n\n## Signs\n\n| Segment (of 6) | `ConsequenceId` | What it is about |\n|---|---|---|\n| 2 | `SOMETHING_STIRRED` | Far off, something moved and went quiet. |\n| 4 | `THE_FLAME_LEANS` | A draught, from somewhere that was shut. |\n| 5 | `IT_IS_CLOSE` | Near enough to hear. It knows roughly where you are. |\n\n`THE_FLAME_LEANS` reads the ALERT clock **through the lantern**. It ties the two clocks together\nin the fiction without tying them together in the machinery, and it is the kind of foreshadowing\n§9 names as the thing generated content is worst at producing on its own.\n\n## The ALERT band vocabulary\n\n`emberdelve-ffi.1` left this as a named slot for this ticket. Filled. In `## The party`,\nalongside `lantern: guttering`:\n\n```\nthe site: stirring\n```\n\n| Fill | Band |\n|---|---|\n| 0–1 | `quiet` |\n| 2–3 | `stirring` |\n| 4–5 | `hunting` |\n| full | `on you` |\n\nBands, not a count, under ffi.1's rule: no tool takes the alert level as an argument, so the\nnumber buys nothing and costs what AGENTS.md warns about — an invitation to read it aloud.\n\n---\n\n# The rules the tables needed\n\n## What ticks what\n\nThe trap is that two clocks ticking on the same events are one clock with two faces. They share a\nbaseline and differ in what is extra.\n\n| Trigger | LIGHT | ALERT |\n|---|---|---|\n| Room entered | +1 | +1 |\n| Rest taken | +1 | +1 |\n| Failed check | +1 (natural 1: +2) | — |\n| A fight starts | — | +1 |\n| LIGHT fills | — | +1 |\n\nA fight is the noisiest thing in the game, and ticking ALERT on one puts a cost on combat that\ncombat does not currently have — it turns \"fight it or avoid it\" into a resource decision, which\nis the greed loop's sibling, for free.\n\n**LIGHT filling ticks ALERT, and it is declared on the tick side, not buried inside `LIGHT_OUT`.**\nGoing dark making you noisier is the spiral that gives a delve an endgame. Modelling it as a\ncross-clock effect inside a consequence was considered and refused: a clock that ticks a clock is\nthe first step toward a state machine nobody can trace. It is a tick trigger. It stays legible.\nThis is also charting's \"death follows from a filled light clock indirectly, which is enough.\"\n\n## Discharge is asymmetric\n\n**ALERT resets to empty when it fills. LIGHT stays full until a torch is spent.**\n\nNot arbitrary: light is a quantity you *spend*, alert is a state that *discharges*. It is also\nthe only way ALERT can fire more than once in a delve, without which its table is a one-shot.\n\n## Signs fire on crossing upward only, and re-arm on the way down\n\nWithout this, spending a torch to refuel LIGHT fires `LIGHT_GUTTERING` on the way back down — the\nnarrator announcing the light failing on the turn the player fixed it.\n\n## Disposition — two values, not one\n\n`Disposition { HOSTILE, WARY }`, with room for §9's full hostile-through-friendly band later.\n\n**An ALERT consequence brings something in; it does not decide how the meeting goes.** Combat\nfollows from the disposition, not from the spawn.\n\n`WARY` needs **no new machinery**: the creature is on the board, combat has not started, and if\nthe player swings, `start_combat` fires as a provoked fight. That is exactly the state the M2 and\nM3 sessions already produced by accident (a goblin spawned, no combat started). It is §9's\n\"avoid the encounter\" play for free, and it is `SOMETHING_WANDERS_IN`'s whole point.\n\nA one-value enum was refused for the reason AGENTS.md records about capability flags: **a claim,\nnot a behaviour**. An unexercised seam is one that will be wrong the day a neutral NPC exists —\nand the standing worry is precisely that a player should not kill an NPC they were not determined\nto kill. This makes that structural rather than a prompt rule.\n\n## What the log says\n\n`ConsequenceFired` **plus** the bundled events. Inert in the fold, carried for the record.\n\nRefused hardest: a `ConsequenceFired` that the fold expands via the tables. That would make a\ncontent file part of replay correctness — edit a table after a session is recorded and it\nsilently replays differently. Same argument `Event.PartyMoved` already makes for carrying its\nlanding square rather than recomputing it from `Exit.inward()`.\n\nBundled-events-only was refused too: it loses the causal record, and this project's whole\ndiagnostic workflow is \"the file that proves it is already on disk.\" A session where a fight\nappeared from nowhere and the log cannot name the clock is a session you cannot diagnose. One\ninert event is the price, and `ToolCallIssued` already pays exactly that price for exactly that\nreason.\n\nTwo things this forces rather than chooses:\n\n- **`ClockTicked` must be an event**, or a clock is not in the log and is not a fold (invariant 8).\n- **It must carry the resulting fill** rather than have the fold count ticks — same argument as\n  above.\n\nThe draw on a multi-entry table is logged **in `ConsequenceFired`**, not as a `RollResult`. It is\nnot a die the player watches, and `isDramatic()` would gate it out anyway.\n\n## Which rail a sign rides\n\n**The latched directive rail** — `pendingArrival`'s, not `narrateCombat`'s.\n\nA sign is foreshadowing, not an event: nothing happened, something is *going to*. There is\nnothing on screen to cover an interrupting prose call — the dice cover an exploration turn, the\nanimation covers a combat beat, a sign has neither — so an immediate call buys an empty screen,\nwhich is the 2.5s row the project already misses. And a dropped sign is worse than a late one,\nbecause the whole point is giving the player a chance to act on it.\n\n**This makes signs free: they cost no extra model call at all.**\n\nIt is also less late than it first looks. Every tick trigger fires inside a player turn, and the\nmechanics phase runs in front of the prose phase — so a failed-check tick's sign drains on **that\nsame turn's** prose call. Only a room-entry tick waits, because `WsHandler.enterExit` runs no\nprose phase, and the 2026-09-07 playtest already judged a silent crossing to be right.\n\n## Two directives, one turn\n\nThe arrival directive and a sign directive **both go**, sign appended to arrival.\n\nNot an edge case: rooms entered ticks both clocks, so once the clocks are biting this is the\n**normal** shape of an arrival. Dropping the sign there silences the clock at exactly the moment\nit should speak. A queue was refused — the engine latches and every player turn drains, so more\nthan two cannot accumulate without a turn passing.\n\nThe cost is named because it is measured: AGENTS.md records that *writing more* made the model\nmore verbose, 514 → 654 characters. **A sign directive is one clause**, in `ARRIVAL_FIRST`'s\nregister — \"The lantern has burned lower. Work it in.\" — never a paragraph about what the light\nmeans.\n\n## A sign that fires during combat is dropped\n\nCombat is the one case where the latch would wait past usefulness — the next exploration turn\ncould be many rounds away. AGENTS.md's asymmetry rule already decides this: engine-generated\nnarration gives up rather than arriving late. The ALERT band stays in the projection continuously,\nso the DM still knows; only the beat is lost. Riding the next combat beat was refused because\ncombat directives are deliberately terse and appending to them fights the one thing combat\nnarration has measured.\n\n---\n\n# What this decides for `emberdelve-vy7`\n\nTaken here rather than deferred, because the ALERT table is what replaces it.\n\n**`start_combat` stays, for provoked fights. `dm-tools.md`'s two-failure escalation rule goes.**\nThe engine's ALERT clock becomes the only source of an *unprovoked* fight.\n\nA player who swings a sword needs a fight to start, so routing that through a clock is absurd;\nwhat was wrong was never that the model could start a fight, but that it decided *unprovoked*\nescalation by reading a fuzzy rule. Both 2026-09-07 sessions escalated on the **first** failure,\nand one put the player in initiative before they had done anything hostile. That rule was not\nproducing tension, it was producing a coin flip — and it decides what kind of game the session is\non turn one.\n\n**The behavioural change, named rather than left to be found in play:** ALERT does not tick on\nfailed checks. So a player who fails check after check in one room now generates no escalation at\nall, where today they would get a fight. That is correct — failed checks still cost, in LIGHT,\nwhich is the currency that does not ambush you — but it is a real change to a milestone that\npassed its gate.\n\n---\n\n# New build work this surfaces\n\n- **Room lighting has no `Diff`.** `RoomView` carries `LightingPreset lighting`, but nothing in\n  the `Diff` union can change it after the scene is sent. `THE_DARK_CLOSES_IN` needs one.\n- **`Disposition` on a spawn**, and the rule that combat follows from it.\n- **`ClockTicked` and `ConsequenceFired`** events, and the schema bump they imply.\n\nFiled against `emberdelve-4h9.3`, which owns the Clock primitive.\n\n# Not decided here\n\n- **Segment counts, and how hard a rest ticks** — `emberdelve-ffi.6`. The tables above are written\n  against a working assumption of 6 segments; the thresholds are relative positions (quiet early,\n  every segment late), and ffi.6 pins the integers.\n- **What the shrinking radius looks like** — `emberdelve-ffi.5`.\n- **What the lantern is as an object** — `emberdelve-ffi.3`.\n","created_at":"2026-09-09T22:49:50Z"},{"id":"01a08865-4f80-7e63-889d-255ccbdfa54c","issue_id":"emberdelve-ffi.2","author":"hampton.black","text":"Recorded as ADR-0012 — 'A consequence may only do what a tool can already do' (docs/adr/0012-a-consequence-may-only-do-what-a-tool-can-do.md). Status Proposed until it is built. The ADR carries the power budget and the three rejections (an effect language, a separate SignId, a fold-expanded ConsequenceFired); the four tables themselves stay here and go into the spec via emberdelve-ffi.7.","created_at":"2026-09-09T22:58:56Z"}],"dependency_count":0,"dependent_count":3,"comment_count":2}
{"_type":"issue","id":"emberdelve-4h9.10","title":"A dev-only room preview and prop-placing tool in the Godot editor","description":"Placing props by typing x/y into room JSON, restarting the server and walking to the room\nis the loop that authoring 4-5 rooms with 20+ props each would run several hundred times.\nA dev-only Godot editor tool collapses it: load a room, see it with the real meshes, drag\na prop to a square, write the position back.\n\nIt also happens to be where the appearance work in 4h9.7 gets done. Choosing which of\neleven container meshes reads right in a torchlit crypt is a judgement made by looking,\nand in the tool it is a dropdown rather than an edit-restart-walk cycle.\n\nWHAT MAKES THIS CHEAP HERE\n\nroom.gd contains no reference to Table - its own docstring says so, \"this node reads\nnothing from Table, so both go through one build path\". And room.gd:554 _to_world already\nfalls back to a self-centred grid when there is no World ancestor. A Room node standing\nalone in an editor scene builds itself correctly. That fallback exists for another reason\n(an unregistered room draws on the entrance rather than at NaN) but it is what makes the\npreview a small script rather than a port.\n\nRoom.build reads eight keys: roomId, width, height, floorType, wallType, lighting, exits,\ncoveredWalls, plus visited via level_for.\n\nProps are not built by Room. world.gd:236 _instance_prop does it, reading id, hidden,\ntype, x, y, rotation, and calling configure(prop, room_id) - which mesh_prop.gd:32 does\nwithout touching Table either. So props are editor-safe too.\n\nTWO RULES THAT DECIDE WHETHER THIS IS AN ASSET OR A LIABILITY\n\nExtract the prop instancing into one shared function that world.gd and the tool both\ncall. Do not copy it. _rebuild_rooms already makes this argument about rooms: \"One build\npath for all of them (Room.build), so the wall rule cannot come out differently on either\nside of a door.\" The tool is a third caller. A preview that builds props its own way is a\npreview that lies.\n\nRound-trip the whole JSON file on write. Parse it, mutate only the x, y and rotation of\nthe prop that moved, re-serialize. Never rebuild the file from the fields the tool knows\nabout - that silently drops description, contains, hidden and revealHint, and the loss\ndoes not show up until a session plays wrong.\n\nTHE SOURCE OF TRUTH DOES NOT MOVE\n\nRoom JSON stays the definition; the tool is an editor for it and never a second place a\nroom is defined. Same reasoning as invariant 8, applied to content instead of state.\n\nFor a single room the content shape is close enough to the wire shape to read directly:\ncrypt.json already carries roomId, width, height, floorType, wallType, lighting, props\nand exits at the top level, and its exits already carry x, y and direction. Add\nvisited=true and coveredWalls=[] and Room.build accepts it - and for a lone room the empty\ncoveredWalls is correct rather than a shortcut, per scene_fixtures.gd: \"A lone room is\nnearest the entrance by definition and so covers nothing.\"\n\nThat stops being true the moment you want to preview two rooms side by side, because\ncoveredWalls is then a real answer only Rooms.coveredWalls() can give. At that point add a\nserver flag - dump the actual RoomView JSON next to what RoomDumper already writes - and\nhave the tool eat the wire shape. Do not reimplement coveredWalls in GDScript; it took\nthree attempts server-side and the failures were holes in walls.\n\nPATHS\n\nRoom JSON lives at server/src/main/resources/content/rooms/, outside res:// (which is\ngodot/). Reach it with ProjectSettings.globalize_path(\"res://\") plus a relative hop, not a\nhardcoded absolute path and not a symlink, so it survives the repo being cloned elsewhere.\nGradle copies resources at build; a running server picks up an edit on restart.\n","acceptance_criteria":"A dev-only editor tool loads a room from its JSON, renders it with the real kit meshes and\nthe game's own build path, and writes a moved prop's x, y and rotation back to the source\nfile with every other field preserved byte for byte. Prop instancing is one function that\nthe game and the tool share. Nothing in the tool is on the shipped path.","notes":"SPIKE RUN 2026-09-07, and what it settled. godot/dev/preview_room.gd is on disk and works:\nthe crypt draws in the editor viewport with the real kit meshes - 144 floor, 48 walls,\n16 torches, 7 of 8 props, the eighth being door-north (DOOR), which is correct because\nthe door is the wall segment. It is a prototype, not this issue's deliverable: read-only,\nsingle-room, and carrying three copies of code the real tool must share instead.\n\nTHE RULE THE SPIKE COST FOUR ROUND TRIPS TO LEARN\n\nA non-@tool script is a PLACEHOLDER INSTANCE in the editor. It keeps its exported values\nand loses every method. So anything editor tooling needs to CALL needs @tool; anything it\nmerely instantiates from code (Room) does not. The symptom is always the same line -\n\"Attempt to call a method on a placeholder instance\" - and it is worth recognising on\nsight, because it names the wrong file: it names the caller.\n\nNow @tool, and all four are permanent rather than spike-only:\n\n  world/prop_table.gd     - scene_for() could not be called, so no prop drew at all\n  world/props/mesh_prop.gd - configure() builds PILLAR and RUBBLE meshes at runtime\n  world/props/sarcophagus.gd - _ready() mounts the lid skull\n  world/props/alcove.gd   - _ready() positions the recess pieces\n\nThe last two carry an _editing_myself() guard and need it. Their _ready writes to\ntransforms that belong to the .tscn, so a bare @tool would bake computed positions into\nthe authored file the next time someone opened and saved alcove.tscn. The guard is\n`Engine.is_editor_hint() and get_tree().edited_scene_root == self` - skip when this node\nIS the scene being edited, run when it is an instance built somewhere else, which is all\nthe preview ever makes.\n\nworld/world.gd MUST NOT get @tool. Its _ready connects to the Table autoload, so an\neditor-resident World is a live websocket client inside the editor. Everything the tool\nwants from World - grid_to_world, room_origin - has to come from somewhere else.\n\nTWO TRAPS WITH NO ERROR MESSAGE BETWEEN THEM\n\nAdding @tool to a script does not take effect in a running editor. The placeholder stays\nloaded and the same error repeats, which reads exactly like the edit not working.\nProject \u003e Reload Current Project after any @tool change.\n\nDo not open world.tscn to host the preview. Its root IS a World, Room._host_world walks\nup the tree and finds it, and every call through it fails on the placeholder\n(room.gd:551 room_origin, room.gd:558 grid_to_world). preview_room.gd now refuses that\ncase by name. A blank 3D scene plus its own WorldEnvironment is the arrangement that\nworks.\n\nONE MORE FOR WHOEVER IS LOOKING AT PROPS\n\nThe editor's Preview Sun and Preview Environment toggles are on by default and stack on\ntop of whatever environment the tool adds, so the room reads far brighter than the game.\nTurn both off before judging how a mesh looks, or every appearance decision in 4h9.7 gets\nmade under the wrong light.\n\nWHAT THE REAL TOOL STILL HAS TO DO\n\nWrite back (the whole point, and absent). Share the prop instancing with world.gd instead\nof copying it. Share grid_to_world instead of copying it. Read the environment off\nworld.tscn instead of restating its four numbers. Handle more than one room, which is\nwhere the --dump-view server flag becomes necessary because coveredWalls stops being [].\nPick the room from a list rather than a const you edit and re-run.\n\nUnrelated and pre-existing, so nobody chases it twice: the three \"Unicode parsing error /\nUnexpected NUL character\" lines at editor startup are addons/gut/source_code_pro.fnt, a\nbinary bitmap font Godot tries to read as UTF-8. Committed in 47fae7f, untouched. Every\ntext file in the project is clean UTF-8 with no NUL bytes.","status":"open","priority":2,"issue_type":"feature","owner":"hampton.j.black@gmail.com","created_at":"2026-09-08T00:00:24Z","created_by":"hampton.black","updated_at":"2026-09-08T12:33:13Z","dependencies":[{"issue_id":"emberdelve-4h9.10","depends_on_id":"emberdelve-4h9","type":"parent-child","created_at":"2026-09-07T19:00:24Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"emberdelve-gl8.3","title":"Combat verbs and initiative live in the frame, only during a fight","description":"combat_bar.gd is a 42px strip pinned to the top of the overlay. Relocate its\ncontents into the frame: initiative chips along the top of the playfield (over\nthe world, like the party badge), and Attack / Move / End Turn in the chin,\nright of the log.\n\nThese exist only while Table.mode is COMBAT (and while the existing bar would\nhave been visible, including the dissolve). Exploration has no chips and no\nverbs; the log takes the chin width.\n\nEnd Turn keeps Net.end_turn(activeId). Attack and Move do not grow new click\nmodes — they are visible verbs for click-the-board, dimmed from\nactionAvailable / movementRemaining the way the current bar already does.\n\nReuse combat_bar.gd timing (arrival(), CHIP_* constants, backdating from\nopened_at) so a mid-fight reconnect does not replay the drums. Layout changes;\nthe ceremony does not.","design":"The 42px CombatBar Control can move or the script can be hung on new nodes in\nthe frame. Do not duplicate the fold of CombatView. Debug → start combat is\nthe play path.\n\nThe old top strip going away is the point: mode is legible from whether chips\nand verbs exist, not from a bar across the sky.","acceptance_criteria":"- Exploration: no initiative chips, no Attack/Move/End Turn.\n- Debug → start combat: chips appear over the playfield, verbs appear in the chin, End Turn still ends the turn.\n- Movement remaining and action available still gate the verbs from the server's CombatView.\n- Arrival timing still uses opened_at / arrival() so a reconnect does not restage the ceremony.\n- Existing combat_bar tests updated for the new nodes and still pass.","status":"closed","priority":2,"issue_type":"task","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T22:39:27Z","created_by":"hampton.black","updated_at":"2026-09-07T23:37:57Z","started_at":"2026-09-07T23:27:34Z","closed_at":"2026-09-07T23:37:57Z","close_reason":"Chips over the playfield, Attack/Move/End Turn in the chin, only during a fight (including dissolve). CombatView still gates the verbs; arrival() timing unchanged; End Turn still Net.end_turn.","labels":["ready-for-agent"],"dependencies":[{"issue_id":"emberdelve-gl8.3","depends_on_id":"emberdelve-gl8","type":"parent-child","created_at":"2026-09-07T17:39:26Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-gl8.3","depends_on_id":"emberdelve-gl8.1","type":"blocks","created_at":"2026-09-07T17:39:28Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"emberdelve-gl8.4","title":"Dramatic rolls take the chin as a free-standing die","description":"dice_tray.gd is a bottom-right overlay Control drawing tumble.gd samples. Move\nthat presentation into the chin's right side. The die is free-standing — no\ninner tray well, no recessed pit. Stakes, arithmetic, and outcome sit around it\n(ATHLETICS CHECK · DC 20, 14 + 3 = 17, HIT/SUCCESS) the way the tray already\ndoes from the first frame.\n\nVisible only while a dramatic roll is in flight (Tumble.is_dramatic: attacks,\nchecks, saves). Damage and initiative stay log-only. When nothing is in flight\nthe chin-right is empty (or occupied only by combat verbs if a fight is open).\n\nA check during exploration shows the die without Attack/Move/End Turn. An\nattack during combat can show the die next to the verbs. No full-screen check\noverlay in this task.\n\ntumble.gd stays pure and stays the only thing that decides where a sample is.\nClock.hold timing is unchanged. No click-to-throw.","design":"Keep DiceTray as overlay chrome, not a 3D object in World (test_chrome already\npins that). Re-anchor it into the chin. COMBAT_HOLD_MS, land_at, reveal_at,\nIMPACT_BEAT_MS stay.\n\nThe log already records the same roll; the chin die is the ritual, the log is\nthe record. They must not drift — caption() is shared.","acceptance_criteria":"- No die in the chin when no dramatic roll is in flight.\n- debug → roll d20 (or a skill check) shows a free-standing d20 in the chin with stakes and 14 + 3 = 17-style arithmetic around it, not inside a tray well.\n- Exploration check: die appears, combat verbs do not.\n- Combat attack: die may appear beside the verbs; tray dismissal on the swing is unchanged.\n- tumble.gd is not rewritten. dice_tray and chrome tests pass.","status":"closed","priority":2,"issue_type":"task","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T22:39:27Z","created_by":"hampton.black","updated_at":"2026-09-08T12:44:00Z","started_at":"2026-09-08T12:39:53Z","closed_at":"2026-09-08T12:44:00Z","close_reason":"Dramatic rolls stand as a free-standing die in the chin: no tray well, stakes above, arithmetic below. Exploration checks show the die without combat verbs; an attack can sit beside them. tumble.gd unchanged; chrome and dice-tray tests pass.","labels":["ready-for-agent"],"dependencies":[{"issue_id":"emberdelve-gl8.4","depends_on_id":"emberdelve-gl8.1","type":"blocks","created_at":"2026-09-07T17:39:29Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-gl8.4","depends_on_id":"emberdelve-gl8","type":"parent-child","created_at":"2026-09-07T17:39:27Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-gl8.4","depends_on_id":"emberdelve-gl8.3","type":"blocks","created_at":"2026-09-07T17:39:29Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-gl8.2","title":"Float the party badge over the playfield","description":"The party is not a left sidebar. A circular portrait floats over the top-left of\nthe playfield (over WorldView, not in its own stone column) so the crypt keeps\nthat width. Under the circle: name, a green HP bar (allegiance colour, not a\nwarning hue), and AC. That is all.\n\nHP/name/kind come from Table's player-controlled entity. AC comes from a local\nkind table (fighter 16, goblin 15) — do not add Entity.ac to the wire in this\ntask. Portrait is a placeholder (silhouette or a still of the knight); a\nportrait key on the entity is out of scope.\n\nEmpty party slots are out of scope so they cannot rebuild a left column.","design":"Subscribe to Table.scene_changed (and hp diffs already folded into scene). The\nbadge owns no facts. Green is yours (#7fae56), matching combat_bar.gd.\n\nMouse filter ignore on the badge so it cannot swallow a click meant for the\nboard — same lesson as brazier-east shadowing the door.","acceptance_criteria":"- No left character column. The crypt is full playfield width.\n- A circular badge over the playfield shows Roderick, HP 12/12, AC 16 from a local table.\n- The bar shortens when HP changes. Colour stays green (allegiance).\n- Clicks on the board through the badge's rect still move, or the badge is ignored by picking.\n- Headless test covers the badge reading Table.","status":"closed","priority":2,"issue_type":"task","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T22:39:26Z","created_by":"hampton.black","updated_at":"2026-09-07T23:23:44Z","started_at":"2026-09-07T23:15:48Z","closed_at":"2026-09-07T23:23:44Z","close_reason":"Circular badge over the playfield shows Roderick, HP, and AC from a local table; the bar shortens on HP change and stays green; mouse filter ignore so it cannot swallow board clicks.","labels":["ready-for-agent"],"dependencies":[{"issue_id":"emberdelve-gl8.2","depends_on_id":"emberdelve-gl8","type":"parent-child","created_at":"2026-09-07T17:39:25Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-gl8.2","depends_on_id":"emberdelve-gl8.1","type":"blocks","created_at":"2026-09-07T17:39:27Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-gl8.1","title":"Inset WorldView in a stone frame and move the log into the chin","description":"WorldView currently fills the window (chrome.tscn) and Overlay/Log is a 42% veil\nat the bottom-left. test_chrome.gd pins SubViewport size to the window and the\nlog as a translucent panel.\n\nBuild the frame as architecture: dark stone StyleBoxFlat bezel, brass inner\nkeyline, room name on the top lintel (from scene.roomId via a local name table\nis fine), a playfield hole, and a chin below. Constrain WorldView to the\nplayfield. Relocate Overlay/Log and InputBox into the chin, full width.\n\nClick forwarding (WorldView.gui_input → World.handle_pointer) and Q/E through\nchrome.gd must keep working on the smaller Control.\n\nTitle, defeat, toast, debug bar, and the server-down banner stay; re-place only\nas far as the frame requires. Do not restyle them.","design":"Do not overlay the chin on a full-window world. If WorldView stays full-window\nthe camera centres on the window and the near edge of the room sits under the\nchin — that is the failure the old spec §9 warned about, solved here by making\nthe SubViewport the playfield.\n\nProportions are of the window, judged in the editor against the D3 mockups, not\nthe 12.5%/21% numbers in the 2026-08-22 spec.\n\nFiles: godot/chrome/chrome.tscn, chrome.gd if forwarding needs a tweak,\ngodot/test/test_chrome.gd (the 1:1 window-size and log-veil assertions).","acceptance_criteria":"- WorldView no longer fills the window; SubViewport size matches the playfield Control, not the window.\n- The log and 'What do you do?' sit in the chin below the crypt, not as a veil over it.\n- test_the_project_boots_into_chrome_with_an_empty_stretching_world is updated and passes.\n- Click-to-move and Q/E still work in the headless chrome tests and in godot .\n- Room name is visible on the lintel.","status":"closed","priority":2,"issue_type":"task","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T22:39:25Z","created_by":"hampton.black","updated_at":"2026-09-07T23:03:32Z","started_at":"2026-09-07T22:47:16Z","closed_at":"2026-09-07T23:03:32Z","close_reason":"WorldView is the playfield hole; the log and input sit in the chin; the lintel names the room. Headless chrome tests pass, including click-to-move and Q/E.","labels":["ready-for-agent"],"dependencies":[{"issue_id":"emberdelve-gl8.1","depends_on_id":"emberdelve-gl8","type":"parent-child","created_at":"2026-09-07T17:39:24Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"emberdelve-gl8","title":"Chrome prototype — the crypt sits in a frame","description":"The overlay still reads as software on top of a 3D room: full-window WorldView,\na translucent log in the corner, dice and the combat bar floating over the crypt.\nA played mockup pass (D3) locked a CRPG frame instead: heavy stone bezel, brass\nkeyline, the crypt in a playfield hole, a chin below for the log, a circular\nparty badge floating over the room, combat verbs only in a fight, and a\nfree-standing die only while a dramatic roll is in the air.\n\nThis epic is a layout prototype over chrome that already works. Transcript,\ntumble.gd, combat_bar, title, defeat, and Table stay. We stop compositing the\nUI over a full-window world.\n\nReference mockups (Cursor assets, not in git):\nemberdelve-chrome-d3-combat.png, d3-explore.png, d3-check.png.\nThe 2026-08-22 chrome-direction spec is background only — its proportions,\n\"no wood\", and full-screen check overlay are not this prototype.","design":"WorldView is inset into the playfield (the hole in the bezel). The camera already\nframes its viewport, so that viewport is the crypt window. Chin, dice, and\nbuttons live below it, not as a veil over the near edge.\n\nGame state stays in Table (invariant #3). Chrome subscribes. No new tools,\nentity types, or rules (invariant #10). Attack and Move stay click-the-board;\nthe new buttons are visible verbs. End Turn keeps Net.end_turn.\n\nAC is a local kind table (fighter 16, goblin 15). EntityView still omits ac;\nshipping it is a later wire change, not this epic.\n\nPlaceholder StyleBoxFlats, not painted stone or KayKit UI meshes.","acceptance_criteria":"- godot . against a running server: the crypt is fully visible in the bezel hole, Q/E still works, typing still works.\n- Exploration: log and input in the chin, no combat verbs, no die.\n- A skill check: log plus a free-standing die with stakes and arithmetic around it, no combat verbs.\n- Debug → start combat: initiative chips and Attack / Move / End Turn appear; End Turn still ends the turn.\n- Headless test_chrome.gd matches the new tree (WorldView is the playfield, not the whole window).","status":"closed","priority":2,"issue_type":"epic","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T22:39:24Z","created_by":"hampton.black","updated_at":"2026-09-08T12:44:01Z","closed_at":"2026-09-08T12:44:01Z","close_reason":"Frame prototype complete: crypt in the bezel hole, log in the chin, party badge over the playfield, combat verbs only in a fight, free-standing die only while a dramatic roll is in the air.","labels":["ready-for-agent"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-4h9.9","title":"More than one hostile on the board","description":"\"Several skeletons in one room.\" Today the board holds one goblin and GoblinAi runs it.\n\nWHAT IS IN\n\nMore than one hostile alive at once, in initiative, each taking a turn. A second stat\nblock so a fight has composition rather than a stack of one thing - the Skeleton_Warrior\nmesh is already the goblin's, so this is content and turn order, not art.\n\nAuthored spawns. M3 carried this forward: \"Authored secrets still have no spawn.\nListening at the lid produced a durable fact and no goblin. theSarcophagus says Vessk is\nin the box; spawn_entity is still a model decision. The hotdog class, now behind a door.\"\nA room that says something is in the sarcophagus should be able to put it there without\nthe model choosing to.\n\nWHAT IS OUT, AND THIS IS THE LINE THAT MATTERS\n\nNot the effect interpreter. Not conditions, resistances, reach, cover, opportunity\nattacks. Section 15 puts the SRD engine, the content and the stance layer in one\nmilestone together, after this one, because each is a tax alone: \"an effect interpreter\nwith a single attack to interpret, or a stance call choosing between aggressive and\naggressive, is overhead.\"\n\nN enemies against the existing forty lines of resolveAttack is the whole scope. If this\ntask starts modelling conditions, it has gone wrong.\n\nNot the stance layer either. Section 16's re-entry condition is \"a second monster kind\nexists, so there is a choice to make and a fight can read as coordinated\" - which this\ntask creates. Note it when it lands; do not build it here.\n\nWATCH THE NARRATION BUDGET\n\nOne narration call per enemy turn, move and swing together, is the existing rule and it\nexists to stop combat going to a five-second-per-click clock. With four hostiles that\nrule is load-bearing rather than a nicety. AGENTS.md, Combat narration, and the open\n\"attacking during an enemy-turn narration drops the kill's narration\" defect both get\nworse with more enemies - decide whether the board locks or the beat queues before\nfinding out at four skeletons.\n\nInitiative currently throws one roll at a time in the tray. Every combatant at once is\nwhy initiative is not dramatic; check that holds at N.\n","acceptance_criteria":"A fight with at least three hostiles of two kinds resolves end to end with no model in\nthe path, under debug start combat. Room content can spawn a hostile without the model\ncalling spawn_entity. resolveAttack is unchanged in shape. One narration call per enemy\nturn still holds, and a full enemy round stays inside the four-second budget.","notes":"**Stat blocks settled by emberdelve-ffi.6.**\n\n- **The goblin is weakened: AC 12, HP 6, +3, 1d4+1** (from AC 15, HP 7, +4, 1d6+2).\n- **The new block is a brute: AC 13, HP 16, +4, 1d8+2.**\n\n**Why the goblin has to move, which was not obvious.** Against a party of one the action economy\ninverts the intuitive ladder: N weak enemies is *always* harder than one strong enemy, because each\nof them takes a full turn while the fighter takes one. At HP 12 today: one brute drops the fighter\n23.7% of the time, two goblins 43.2%, three goblins 79.9%. So \"a few weak mobs\" cannot be the easy\nfight while the goblin is AC 15 / HP 7 / 1d6+2. It has to become the mob it is already described as\n-- `goblin.json` says \"a wiry goblin scavenger, filthy with grave-dust\" and gives it **AC 15**,\nwhich is chain mail.\n\n**HP 6 specifically.** The fighter's 1d8+3 has a minimum of 4, so at HP 4 or 5 the mob dies to\nliterally every hit that connects and a pack is a formality. HP 6 leaves it at 75% one-shot --\nusually one blow, sometimes two.\n\n**The brute is what meets §9's gate.** It puts the doubt window at **4-10, 50% at 6 hp, 45.7%\nsurvival at exactly 5**, while dropping a full-health fighter only 8.2% of the time: a fight you\nalmost never lose fresh and genuinely might lose hurt. That is the sentence \"stand in front of a\ndoor at 5 hit points and genuinely not know\" made arithmetically true.\n\n**Build warning, and it is not a content-only edit.** `CombatEngineTest` constructs its fixture\nthrough `new GameEngine(new ContentLoader(), ...)`, so it reads the real `fighter.json` and\n`goblin.json`. Its scripted die faces are chosen against AC 15 and 7 hp and will all need reworking\n-- e.g. `CombatEngineTest.java:350` asserts \"12 hp less 4+2 damage\". `WorldStateTest` and\n`EventLogTest` construct `Entity` records directly and are unaffected.\n\n**Recorded logs are unaffected.** `EntitySpawned` carries the whole `Entity`, so a replayed session\nuses the stats it was written with. No schema bump, no `recordFixture`.\n\n`AGENTS.md`'s shortcuts table Goblin row becomes untrue when this lands.\n\nNumbers and method: `docs/evidence/m4-attrition-sim.py`.","status":"open","priority":2,"issue_type":"feature","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T22:30:26Z","created_by":"hampton.black","updated_at":"2026-09-09T23:57:54Z","dependencies":[{"issue_id":"emberdelve-4h9.9","depends_on_id":"emberdelve-4h9","type":"parent-child","created_at":"2026-09-07T17:30:25Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.9","depends_on_id":"emberdelve-4h9.8","type":"blocks","created_at":"2026-09-07T17:30:45Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"emberdelve-4h9.8","title":"Author the site - 4-5 rooms, an objective, a way out","description":"Retires the AGENTS.md anti-goal \"do not add a third room\". That was M3-scoped and it is\nspent: two rooms cannot hold a decision about whether to push deeper.\n\nSection 9, Bounded sites: \"Sites are finite: a handful of rooms, an objective, an exit,\nan ending. Quest-linked, scattered across the region, many of them. Not a hole that\ngenerates downward forever.\"\n\nSHAPE\n\nFour or five rooms. An entrance that is also the way out. Two middles. A room holding\nthe objective, sitting shallow. Something worth taking that sits deeper than the\nobjective does - that is the greed decision, and section 9 calls it \"tension for free,\ninside a bounded space, with an ending\".\n\nThe crypt and the gallery are the fixed points every M0 through M3 measurement was taken\nin. Extend the site around them rather than replacing them, or the comparison is gone.\n\nWHY THIS IS ALSO THE GENERATOR SPEC\n\nM1's outstanding half comes after this epic, and the reason it comes after is that\nnobody yet knows what a generated room has to contain. M3 carried the symptom forward:\n\"Generated rooms still have nothing to find. PropPlacer marks nothing hidden, so\nreveal_prop is never offered.\"\n\nAuthoring the site answers it. Whatever these rooms need - a hidden prop, an encounter\nslot, an objective anchor, a place for a clock to tick, a way out - is what\nLayoutGenerator and PropPlacer have to be able to produce. Write that list down as it\nemerges; it is the input to the next milestone.\n\nTWO THINGS THE 2026-09-07 PLAYTEST SAYS TO GET RIGHT\n\nDoor descriptions must match what crossExit actually enforces. Both sessions invented an\nATHLETICS DC 20 to kick a door that was never shut, because every authored door said\n\"no handle on this side\" while the engine refused nothing but leaving mid-fight. If a\ndoor in this site is meant to be shut, it needs emberdelve-ql3 behind it. If it is not,\nits description must not say so.\n\nPut revealHints where a player will actually look. The gallery niche was never found in\neither session - its hint is on the west wall and both players searched the brazier and\nthe pillars.\n","acceptance_criteria":"Four or five authored rooms forming one site with an entrance that is also the exit, an\nobjective placed shallow, and something worth more placed deeper. Every door description\nagrees with what the engine enforces. A written list of what a room in this site must be\nable to contain, as the generator milestone's input.","notes":"**Site shape settled by emberdelve-ffi.6: five rooms, about three holding a fight.**\n\nThe objective sits shallow, leaving two rooms of genuinely optional depth -- which is where the\ngreed decision lives at all.\n\n**The encounter ladder**, against the settled fighter (HP 20) and the settled stat blocks\n(emberdelve-4h9.9):\n\n| Encounter | Dropped from full | 50% at | Doubt window |\n|---|---|---|---|\n| 2 mobs | 1.3% | 3 hp | 1-5 |\n| 1 brute | 8.2% | 6 hp | 4-10 |\n| 3 mobs | 12.0% | 9 hp | 8-13 |\n| brute + 2 mobs | 52.7% | 20 hp | 18-20 |\n\nOne brute and three mobs land on comparable weight -- two shapes, one difficulty, which is the\nvariety the site wants without a cliff. **brute + 2 mobs is not a designed room**; it is what the\nALERT clock does to a party that let it fill.\n\nWhole delves: careful (3 fights, objective, out) is lost **26.1%** of the time, leaving 10.8/20 hp\nand 2.5 items; greedy (5 fights, two rooms deeper) is lost **77.1%**. The careful figure assumes\nthe player fights everything and avoids nothing -- avoidance is what the ALERT signs are for.\n\nInteracts with a locked M2 decision worth restating here: **exits are illegal in combat**, so every\nfight is to the death. The greed decision is therefore \"do I open this door\", never \"do I finish\nthis fight\" -- which is why the danger must be legible *before* the door.\n\nNumbers and method: `docs/evidence/m4-attrition-sim.py`.","status":"open","priority":2,"issue_type":"task","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T22:30:25Z","created_by":"hampton.black","updated_at":"2026-09-09T23:57:53Z","dependencies":[{"issue_id":"emberdelve-4h9.8","depends_on_id":"emberdelve-4h9","type":"parent-child","created_at":"2026-09-07T17:30:24Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.8","depends_on_id":"emberdelve-4h9.10","type":"blocks","created_at":"2026-09-07T19:00:37Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.8","depends_on_id":"emberdelve-4h9.7","type":"blocks","created_at":"2026-09-07T17:30:45Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"emberdelve-4h9.7","title":"Spend the kits - a prop appearance layer, not forty more PropTypes","description":"The kits on disk hold 145 GLBs - dungeon_props 48, ruins 92, characters 5. The game\nuses six PropType values and ships five client scenes (DOOR came out of prop_table.tres\nat the M3 playtest, because the wall hangs the door). Barrels, crates, chests, pots,\nbookcases, tables, statues, cobwebs, skulls, banners, candles, bear traps, trapdoors and\nspikes are all sitting there unused. The 4-5 room site is the reason to spend them.\n\nTHE TRAP TO AVOID\n\nPropType is the enum ToolSchema builds the model's tool definitions from. Growing it\nfrom six values to forty grows every schema the model reads every turn and hands it\nforty things to hallucinate - and AGENTS.md already records that the tools model\nhallucinates props. Section 9 says the way out: \"props are handles, not depictions...\nOne generic container mesh does an enormous amount of work that way.\"\n\nPROPOSED SHAPE - overrule this if you disagree, but decide it before authoring rooms\n\nSeparate what a prop MEANS from what it LOOKS LIKE.\n\n  PropType stays small and mechanical. It answers: does this block a square, is it a\n  container, is it a light source, is it a recess in a wall, is it scenery. Roughly\n  today's six plus CONTAINER, STATUE, FURNITURE, REMAINS and a scenery catch-all.\n\n  A new closed appearance field per type picks the mesh. CONTAINER covers Chest,\n  Chest_Gold, Barrel, Barrel2, Crate, Pot1, Pot2, Pot3, Bag_Standing, Vase and Bucket -\n  one type, eleven looks. STATUE covers Fox, Horse and Stag.\n\nThe tool schema names the type. reveal_prop and clicking both address props by id\nanyway, so schema width stops growing with the kit while the room stops looking the same\ntwice.\n\nTHREE RULES THAT FALL OUT OF THAT\n\nAppearance must never change blocking. PropType.blocksMovement is derived from the type\non purpose - the comment says a per-prop flag \"would only be a chance to disagree with\nthe mesh\". A CONTAINER blocks whether it is a chest or a barrel, or that reasoning is\ngone.\n\nThe DM reads description, not the mesh name. The prop description field already carries\nthe specific look in prose. Appearance is a renderer concern and the seam is already\nthere.\n\nThe server ships appearance, the client never picks it (invariant 1). A client that\nchose its own mesh would render a replayed session differently from the session.\n\nTHE PICKING HAZARD, AND IT IS NOT HYPOTHETICAL\n\nFrom the 2026-09-06 playtest: \"Only things you can act on are pick targets\"... brazier-east,\n\"a 1x1 box around a narrow bowl, standing between the camera corner and the north wall -\nwon the ray, had no intent to offer, and fell through to a square that was null because\nthe ray had already left the room. The click did nothing and the failure was silent.\"\n\nA room with twenty-five props is exactly where that comes back. Scenery must not shadow\na handle. And \"a one-prop fixture will not find a picking bug\" - test_overlay.gd's\nDOORWAY fixture carries the crypt's furniture today and needs to carry a dense room.\n\nHOUSEKEEPING\n\nkits/crypt.json is the only kit manifest. ruins/ is an overgrown outdoor palette and is\nmost of where room-to-room variety will actually come from - a second manifest is\nprobably part of this.\n\nkits/kaykit_dungeon/ and kits/kaykit_halloween/ hold zero GLBs. Find out whether they\nare empty or unimported before assuming the inventory is 145.\n\nEach kit keeps its own folder. Kit GLBs reference textures by relative path and mixing\ntwo kits in one directory silently paints one with the other atlas (AGENTS.md,\nCharacters). ruins/ and dungeon_props/ are already separate; keep it that way.\n\nAdding props is not a licence to retune lighting. The anti-goal timeboxes that and\nemberdelve-27l already holds the one open lighting question.\n","acceptance_criteria":"A room renders around twenty props drawn from at least two kits and does not read as the\nsame room twice. The model's tool schema is no wider than it is today. Blocking is still\nderived from PropType alone. test_overlay.gd holds a densely furnished room and turns red\nif a scenery prop shadows the door or any other handle.","notes":"CORRECTION 2026-09-07: the inventory figures in the description undercount. The original survey searched *.glb only, and two kits ship *.gltf. Real totals: dungeon_props 48 glb, ruins 92 glb, kaykit_dungeon 26 gltf, kaykit_halloween 15 gltf, characters 5 glb - 186 mesh files, not 145. kaykit_dungeon is not empty; it is the kit already in use, and room.gd names its floors, walls and torches directly. kaykit_halloween is 15 unused meshes and it is the most on-theme set on disk for this site: coffin, coffin_decorated, crypt, shrine, shrine_candles, plaque, plaque_candles, arch_gate, lantern_standing, pillar, skull, ribcage, bone_A/B/C. Note lantern_standing in particular - the 2026-09-06 playtest recorded that invented lanterns travel with the party as facts with no prop behind them, and there is a mesh for one.","status":"open","priority":2,"issue_type":"feature","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T22:30:24Z","created_by":"hampton.black","updated_at":"2026-09-07T23:34:34Z","dependencies":[{"issue_id":"emberdelve-4h9.7","depends_on_id":"emberdelve-4h9.10","type":"blocks","created_at":"2026-09-07T19:00:36Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.7","depends_on_id":"emberdelve-4h9","type":"parent-child","created_at":"2026-09-07T17:30:23Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"emberdelve-4h9.5","title":"A delve can end - extracted, empty-handed, or lost","description":"The gate is \"does losing it sting\", so a delve has to be losable and the loss has to\nland. Section 9: \"A delve can be lost, and losing it costs the run rather than a click.\"\n\nThree endings at least: extracted with the objective, extracted without it, and lost.\nLost is a party wipe today because the party is one member; the shape should not assume\nthat.\n\nThere is prior art in this repo for the moment itself. From AGENTS.md, Since fixed:\n\"A dead player character used to be only a dropped token. Combat ended, the mode pill\nflipped back to EXPLORATION, and nothing said what had happened - with the input box\nrefusing text and the board refusing clicks, that reads as a crash rather than a death.\"\nThe React Defeat screen solved it by marking the moment and offering the way on. That\nscreen predates the Godot migration; check what survived it. Either way the delve ending\nneeds the same treatment and more of it, because now there is a run to have lost rather\nthan a session to restart.\n\nWhat makes it sting is what was spent, so the ending should say it: rooms entered,\npotions drunk, what was carried, what was left behind. That is a read of state, not a\nnarration call, though a narration call over it is cheap and probably right.\n","acceptance_criteria":"Three endings reachable and distinguishable, each ending the delve as a state rather\nthan a screen. The client shows what was spent and what was carried out. Restarting is\none action and does not need the terminal.","notes":"SETTLED ON THE M4 MAP 2026-09-08 (emberdelve-ffi). Three amendments:\n\n**`Ending` is descriptive, never a verdict.** `EXTRACTED_WITH_OBJECTIVE` / `EXTRACTED_WITHOUT` /\n`PARTY_LOST`, not WON/LOST. §9's own first argument for bounded sites is that \"a cleared site is a\ndurable fact\" the campaign layer consumes; scoring the delve here pre-empts §10. The standing design\nintent is that a bad outcome must never guarantee campaign failure — player creativity and DM\nimprovisation should be able to open another path — and an enum that judges takes that away.\n\n**Only `PARTY_LOST` is losing.** Extracting empty-handed is a legitimate cautious outcome and\nnothing should read as a scolding. §9's greed decision only works if leaving early is respectable;\npunish the empty-handed exit and the site has one correct line through it.\n\n**`DelveEnded` is an event.** Folded into `WorldState`, shipped in `SceneState`. Under invariant 8\nthere is no second write path, and the end of a delve is the single most important thing that\nhappens in a session — if it is not in the log it will not replay. `godot/chrome/defeat.gd`'s\nclient-side `hp \u003c= 0` check on a hardcoded `\"fighter\"` goes: it cannot distinguish three endings and\nit names a singleton player against invariant 2.\n\nDeath is no longer instant — 4h9.13 makes 0 hit points a downed clock. How the ending reads is\nemberdelve-ffi.4.","status":"open","priority":2,"issue_type":"feature","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T22:29:04Z","created_by":"hampton.black","updated_at":"2026-09-08T23:26:20Z","dependencies":[{"issue_id":"emberdelve-4h9.5","depends_on_id":"emberdelve-4h9.4","type":"blocks","created_at":"2026-09-07T17:30:43Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.5","depends_on_id":"emberdelve-4h9.3","type":"blocks","created_at":"2026-09-07T17:30:44Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.5","depends_on_id":"emberdelve-4h9","type":"parent-child","created_at":"2026-09-07T17:29:03Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.5","depends_on_id":"emberdelve-ffi.4","type":"blocks","created_at":"2026-09-08T18:25:46Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.5","depends_on_id":"emberdelve-vy7","type":"blocks","created_at":"2026-09-07T17:30:44Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":4,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"emberdelve-4h9.4","title":"The objective is an engine-owned prop, and the exit checks for it","description":"Section 9, Props are handles: \"The objective is engine-owned, never narrator-asserted.\nIt is a prop with an id, taking it emits an event, and the exit checks for it. This is\nthe difference between an objective and a copper key.\"\n\nThe copper key is the reference failure and it is worth restating because this task is\nthe fix for it. The M2 gate produced a copper key found in a lamp's fat, asserted,\ndurable, and turning a lock in a door that still could not open (m2-evaluation.md\nsection 4). assert_fact made the DM consistent about it; nothing made the board agree.\nAn objective that lives in facts is another copper key. This one is a Prop with an id,\na take event, and a fold.\n\nDepends on props being click targets. Routing \"take the relic\" through free text alone\nputs a mechanical outcome back on the narrator's side of the governing principle —\nsection 2a says exactly this, and it is why the exits-only rule was reversed.\n\nSection 9 also says where it sits: \"the objective sits shallow and there is more worth\ntaking deeper, so the player can leave the moment they have what they came for. That is\ntension for free, inside a bounded space, with an ending.\"\n\nThe exit checking for it is half the task. Leaving without the objective is legal and\nmeans something different from leaving with it.","acceptance_criteria":"The objective is a Prop with an id in room content. Taking it emits an event that folds\ninto WorldState. The exit reads that state. A test takes it, leaves, and asserts the\ndelve ended carrying it; a second test leaves without it and asserts a different ending.\nNo fact and no narration is load-bearing in either path.","status":"open","priority":2,"issue_type":"feature","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T22:27:26Z","created_by":"hampton.black","updated_at":"2026-09-07T22:27:26Z","dependencies":[{"issue_id":"emberdelve-4h9.4","depends_on_id":"emberdelve-7m6","type":"blocks","created_at":"2026-09-07T17:30:42Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.4","depends_on_id":"emberdelve-4h9","type":"parent-child","created_at":"2026-09-07T17:27:26Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"emberdelve-4h9.3","title":"The Clock primitive, at DELVE scale","description":"Section 9, The delve clock: \"Without pressure, go deeper costs nothing and the greed\ndecision is fake. A clock is the pressure.\"\n\nBuild the primitive from section 9 exactly, at DELVE scale only:\n\n  record Clock(ClockId id, ClockScale scale, int filled, int segments,\n               ClockKind kind, ConsequenceId consequence)\n\nThree scales are named in the design (DELVE, WATCH, FRONT) and only DELVE is in scope.\nThe enum carries all three because the record is meant to be built once — section 9,\nOne primitive, three scales. Do not build the other two.\n\nTicks on rooms entered, rests taken, and noisy failures. When it fills it fires a table.\nThe table is the point: it is the same constrain-the-model pattern as the DC bands in\nsection 7 and the prop enum in section 5. The DM narrates what the table produced; it\nnever decides that the torch went out.\n\nConsequences reach the prompt through the world-state projection (section 12), which\nnever truncates, so a filling clock is something the DM can foreshadow honestly. That is\nthe same reason the projection works at all: the DM cannot contradict a board that is in\nfront of it.","acceptance_criteria":"A Clock record with a tick event and a fold. At least two clocks running in a delve —\nlight and one of alert or wandering. Filling one fires a consequence from a table, not\nfrom the model. The DM prompt shows clock state every turn. A scripted test fills a clock\nand asserts the consequence, with no model in the path.","notes":"SETTLED ON THE M4 MAP 2026-09-08 (emberdelve-ffi). Three amendments to this ticket:\n\n**Clocks speak twice, not once.** A threshold fires a SIGN; the fill fires the consequence. Signs\nare §9's region-scale \"spoor, smoke, tracks\" brought to delve scale — engine-fired, table-driven,\nnarrated by the DM. Reason: a clock that speaks only when it fills is a jump scare, and one that\nspeaks every tick is a segment counter with extra steps. Blocked on emberdelve-ffi.2 for what is\nactually on all four tables.\n\n**Noisy failures are specified.** A failed check ticks the LIGHT clock one segment; a natural 1\nticks two. §9 already named noisy failures as a trigger. This is also the reconciliation of a real\nplay observation — the DM narrating a dropped lantern on a rolled 1 was the right beat reached the\nwrong way, since the board stayed bright. Now the engine ticks, the table fires, and the DM narrates\na real event. The natural die is already special under invariant 5.\n\n**The clock is diegetic and unlabelled.** No segment counter on screen. The LIGHT clock reads as a\nshrinking radius (4h9.12); the ALERT clock's entire read is its signs. The projection lets the DM\nforeshadow the alert clock honestly, which is 4h9.14's job.\n\nTwo more clocks now exist at DELVE scale beyond the two this ticket names: rest ticks every running\nclock one segment (4h9.11), and downed is a short clock of its own (4h9.13). Segment counts are\nemberdelve-ffi.6's answer. **The clock cannot kill directly** — its job is to make leaving\nattractive, not to be a second health bar.\n\n---\n\nRESOLVED BY emberdelve-ffi.2 — 2026-09-09. The four tables are written out in that ticket's\nresolution comment; this ticket is no longer blocked on them. What the build has to honour:\n\n**The power budget, which is the load-bearing rule.** A consequence may only do what a tool can\nalready do — a named, pre-validated bundle of events that already have an `Event` + `Diff` pair\n(`EntitySpawned`, `EntityMoved`, `PropRevealed`, `CombatStarted`) plus state the engine already\nmodels as a scalar. **No effect language.** §15 already puts the effect interpreter with the\ncontent milestone; reaching it through the back door of a consequence table is the same tax under\na different name.\n\n**A sign is a consequence whose bundle is empty.** One enum (`ConsequenceId`), one firing path,\none directive mechanism. A sign table's entries having empty bundles is a validation rule, not a\ntype — a separate `SignId` was considered and refused.\n\n**The sign table is a map from segment to `ConsequenceId`**, not a single named threshold, so a\nclock can be quiet early and loud late. **Signs fire on crossing upward only and re-arm on the way\ndown** — otherwise spending a torch fires `LIGHT_GUTTERING` on the turn the player fixed the light.\n\n**Tick triggers, final:** rooms entered and rests tick both clocks; a failed check ticks LIGHT\n(natural 1, two); a fight starting ticks ALERT; **LIGHT filling ticks ALERT**. That last one is\ndeclared on the tick side, never inside `LIGHT_OUT` — a clock that ticks a clock from inside a\nconsequence is the first step toward a state machine nobody can trace.\n\n**Discharge is asymmetric.** ALERT resets to empty when it fills; LIGHT stays full until a `torch`\nis spent. Light is a quantity you spend, alert is a state that discharges — and without the reset\nALERT's table is a one-shot.\n\n**Two new events, forced by invariant 8.** `ClockTicked`, carrying the *resulting fill* rather\nthan having the fold count ticks; and `ConsequenceFired`, inert in the fold, emitted **alongside**\nthe bundled events rather than instead of them. A `ConsequenceFired` the fold expands via the\ntables would make a content file part of replay correctness — edit a table after a session is\nrecorded and it replays differently. Same argument `Event.PartyMoved` makes for carrying its\nlanding square. Schema bump, so `./gradlew recordFixture` after.\n\n**A draw is logged inside `ConsequenceFired`, not as a `RollResult`.** Not a die the player\nwatches; `isDramatic()` would gate it out anyway.\n\n**Signs ride the latched directive rail** (`pendingArrival`'s, not `narrateCombat`'s), so they\ncost no extra model call. Arrival and sign both go, sign appended, **one clause** — AGENTS.md's\nmeasured finding is that writing more made the model more verbose. A sign latched during combat is\n**dropped**, under the standing asymmetry rule.\n\n**New build work this surfaces:**\n\n- **Room lighting has no `Diff`.** `RoomView` carries `LightingPreset lighting` but nothing in the\n  `Diff` union can change it after the scene is sent. `THE_DARK_CLOSES_IN` needs one.\n- **`Disposition { HOSTILE, WARY }` on a spawn**, and the rule that combat follows from the\n  disposition rather than from the spawn. `WARY` needs no new machinery — the creature is on the\n  board, no combat started, and a player swing starts one via `start_combat`. Two values, not one:\n  an unexercised seam is a claim, not a behaviour.\n- `DRAWN_BY_THE_NOISE` wants emberdelve-4h9.9 (more than one hostile). Not a hard block — until\n  4h9.9 lands the ALERT table draws from the other three entries.\n\n---\n**Segment counts and tick rules settled by emberdelve-ffi.6.**\n\n- **LIGHT: 6 segments. ALERT: 6 segments.** Signs at 2 / 4 / 5, as emberdelve-ffi.2 wrote them.\n- **A torch resets LIGHT to empty** (not \"adds N segments\"). With 2 starting torches that is an\n  18-tick light budget, against a careful five-room delve's ~9 ticks and a greedy one's ~15.\n- **Every room entry ticks, including a room already visited.** The lantern does not care that you\n  have been here. This is load-bearing: it makes going one room deeper cost **two** segments, one\n  in and one out, which is what turns depth from vaguely risky into computable. A revisit-free rule\n  would make deep exploration free to undo and the greed decision fake.\n\n**REVERSAL -- what ticks LIGHT off a die.** Charting settled \"a failed check ticks one segment; a\nnatural 1 ticks two\". That is withdrawn. **A natural 1 ticks LIGHT one segment; an ordinary failed\ncheck ticks nothing.**\n\nMeasured in the recorded sessions: `session-m2-twenty-turns.jsonl` has 20 checks and **14\nfailures**; `session-m3-traversal.jsonl` has 11 checks and **2 failures**. Same game, same player --\na sevenfold difference in fill rate, because M2's DM reached for DC 15-25 and M3's for DC 5-10. A\nclock fed by failed checks has its fill rate set by the narrator's taste in difficulty, which is\nexactly what emberdelve-ffi.2 made LIGHT predictable to avoid, and it hands scarcity to the one\ncomponent §9 says must never own it.\n\nThe play observation that motivated the original rule survives whole: the dropped lantern on a 1 is\nstill an engine tick, a fired table and a narrated event. It lands once or twice a delve; the room\ntick carries the pressure and is fully player-controlled.\n\nNumbers and method: `docs/evidence/m4-attrition-sim.py`.","status":"open","priority":2,"issue_type":"feature","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T22:26:55Z","created_by":"hampton.black","updated_at":"2026-09-09T23:57:52Z","dependencies":[{"issue_id":"emberdelve-4h9.3","depends_on_id":"emberdelve-ffi.2","type":"blocks","created_at":"2026-09-08T18:25:45Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.3","depends_on_id":"emberdelve-4h9","type":"parent-child","created_at":"2026-09-07T17:26:54Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":1,"dependent_count":4,"comment_count":0}
{"_type":"issue","id":"emberdelve-4h9.2","title":"Consumables as a counter map, and one use_item tool","description":"Section 9, Attrition: \"Consumables are a counter map, not an inventory.\nMap\u003cConsumable, Integer\u003e on PartyMember over a closed enum — potion, torch, rope — and\none use_item tool. No item entities, no equipping, no weight, no UI beyond a count.\"\n\nThe reasoning is worth keeping in front of whoever builds this: \"The instinct to build\ninventory here drags in an item pipeline, a screen, and an enum that grows forever, in\nservice of three numbers.\" Section 16 has the re-entry condition for real inventory and\nthis is not it.\n\nHealing is the scarce one. Section 9: the engine has to impose scarcity because the\nnarrator never will. \"The model always says yes. That is not a prompt failure to be\ntuned out; it is what a narrator trained to be agreeable does.\"\n\nuse_item is a mechanics-phase tool, not reconcile: a false positive spends a potion the\nplayer still has, which is expensive. Same test as every other tool placement in\nAGENTS.md, LLM tools.\n\nCounts fold out of the log like everything else (invariant 8). A new event, not a field\nsomebody sets.","acceptance_criteria":"Consumable is a closed enum. PartyMember carries a counter map folded from an event.\nuse_item is in the mechanics tool set, validated server-side, rejected when the count is\nzero. The chrome shows counts as text and nothing more. A potion at zero cannot be drunk\nhowever the player phrases it.","notes":"SETTLED ON THE M4 MAP 2026-09-08 (emberdelve-ffi): the `torch` consumable refuels the LIGHT clock.\n\n§9's enum was already potion / torch / rope, and this gives the torch a job: it links this counter\nmap to 4h9.3's clock and states the greed loop in one line — how deep you go is how many torches you\nbrought. It is also what keeps the light clock non-terminal, which emberdelve-ffi records as a\ndeliberate choice (a clock that cannot be refuelled is a race, and §9 wants a greed decision).\n\nCounts are emberdelve-ffi.6's answer. See also 4h9.12, which builds the light the torch feeds.\n\n---\n**Numbers settled by emberdelve-ffi.6.**\n\n- **A potion restores 8** hit points, on a bar of 20 -- two fifths.\n- **The party starts with 2 potions and 2 torches.** No rope in the starting kit; the enum carries\n  it, the site does not need it yet.\n\nChosen for legibility rather than balance, and the sim is explicit about why: across a whole delve\na 3-point rest and an 8-point rest differ by under 1.5%, because the fights dominate completely.\nThe potion is deliberately worth twice a rest (emberdelve-4h9.11) -- being visibly stronger than\nthe free option is what justifies it being finite.\n\nNumbers and method: `docs/evidence/m4-attrition-sim.py`.","status":"open","priority":2,"issue_type":"feature","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T22:26:54Z","created_by":"hampton.black","updated_at":"2026-09-09T23:57:51Z","dependencies":[{"issue_id":"emberdelve-4h9.2","depends_on_id":"emberdelve-4h9.1","type":"blocks","created_at":"2026-09-07T17:30:41Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.2","depends_on_id":"emberdelve-4h9","type":"parent-child","created_at":"2026-09-07T17:26:53Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"emberdelve-4h9.1","title":"Prove party state is session-scoped, not room-scoped","description":"Section 9 lists this as the small refactor everything else in the delve depends on:\n\"Party state is session-scoped, not room-scoped. Today entities are built from room\ncontent, which makes hit points implicitly reset at a room boundary. Rooms should\ncontribute their occupants and props and nothing else.\"\n\nM3 appears to have already done most of it. GameEngine.java:73 spawns the party once\nat the entrance from entrance.startPositions().party(), and crossExit emits PartyMoved\nrather than respawning, so entities survive a crossing in WorldState.entities. That is\na reading of the code, not a measurement.\n\nThe job is to prove it and close whatever gap is left, because every other child of\nthis epic is built on the assumption. Attrition that silently resets at a door is the\nwhole milestone failing quietly.\n\nLook specifically at: whether anything rebuilds an Entity from RoomDefinition after the\nfirst spawn; whether a room a party returns to re-contributes its occupants (a goblin\nleft alive in the crypt must be the same goblin, at the same hit points, per the\n2026-09-07 playtest); and whether hit points, revealed props and facts all cross\ncorrectly in both directions.","acceptance_criteria":"A test that walks the party across a room boundary and back with damage taken in each\nroom, asserting hit points, position and revealed state survive both crossings. If any\nof it already holds, the test is the deliverable and says so.","notes":"**Settled by emberdelve-ffi.6 — the fighter's max hit points move 12 -\u003e 20.**\n\nNot PC tuning for its own sake: it is the delve-length dial, and no other dial does that job. The\ndoubt window (the hit points where surviving the next room is 40-70%) is set by the *encounter*,\nnot by max hit points -- HP 20 and HP 24 against the same foes have the identical window. What max\nHP buys is the bar *above* the window. At 12 the brute's window (4-10) is more than half the bar,\nso the player is in coin-flip territory from the first fight and the delve has no early phase to\nspend. At 20 they descend 20 -\u003e 10 through survivable ground and then enter it.\n\nOne number in `content/entities/fighter.json`. `AGENTS.md`'s shortcuts table Fighter row becomes\nuntrue when this lands and must be updated in the same change.\n\nNumbers and method: `docs/evidence/m4-attrition-sim.py`.","status":"open","priority":2,"issue_type":"task","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T22:26:53Z","created_by":"hampton.black","updated_at":"2026-09-09T23:57:50Z","dependencies":[{"issue_id":"emberdelve-4h9.1","depends_on_id":"emberdelve-4h9","type":"parent-child","created_at":"2026-09-07T17:26:52Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"emberdelve-27l","title":"EnvTorchlit's 1.1 ambient is the suspect number, not EnvDim's 0.7","description":"RECORDED, NOT SCHEDULED. Read the anti-goal before opening this.\n\nPlaytest 2026-09-07. The traversal design predicted the ambient drift between the two viewing directions and accepted it as a cost: standing in the gallery puts the crypt under EnvDim (ambient_light_energy 0.7, world.tscn:23), standing in the crypt puts the gallery under EnvTorchlit (1.1, world.tscn:14). The worry was that the darker direction would read as a void.\n\nPlay inverted it. 0.7 was judged the better look, and the crypt is the room it was judged on. So the drift is not a cost to tolerate — if anything the brighter environment is the weak one.\n\nTwo readings of the observation, both pointing the same way, neither resolved:\n- the crypt seen from the gallery at 0.7 beat the gallery seen from the crypt at 1.1; or\n- the crypt at 0.7 beat the crypt at 1.1, which is how it looks when you are standing in it.\nThe second is the more interesting claim, because it says EnvTorchlit is too bright for the room it was tuned for.\n\nAGENTS.md § Anti-goals: 'Do not tune lighting and post-processing for more than one evening. Timebox it. This is the single largest time sink in the project and it will consume as much as you give it.' One observation from one session is not a tuning session. This is filed so the observation is not lost, not so someone acts on it.","acceptance_criteria":"If ever opened: timeboxed to one evening, and judged by looking at both rooms from all four camera corners, not by reading numbers.","notes":"PULLED INTO THE M4 MAP 2026-09-08, and rescoped from \"recorded, not scheduled\" to a prerequisite.\n\nM4's light clock is read as a shrinking radius around a party-carried lantern (emberdelve-ffi,\nSettled at charting). At ambient_light_energy 1.1 a shrinking radius will not read at all — the\nenvironment lifts the surfaces the radius is supposed to be leaving. So the judgement the light\nwork depends on cannot be made until this number moves.\n\nThe scope is deliberately narrower than the description above. This is NOT the tuning session the\nanti-goal warns about, and it must not become one:\n\n  - change EnvTorchlit's ambient_light_energy (world.tscn:14) from 1.1 toward EnvDim's 0.7\n  - stand in the crypt and look\n  - keep the number that wins\n\nThe second reading in the description is the one being tested — that EnvTorchlit is too bright for\nthe room it was tuned for — and it is testable by standing in the crypt, which does not require the\ngallery or a crossing.\n\nBlocks the light-radius prototype (emberdelve-ffi.5). Nothing else in the map waits on it.","status":"open","priority":2,"issue_type":"task","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T21:50:07Z","created_by":"hampton.black","updated_at":"2026-09-08T23:22:52Z","labels":["wayfinder:task"],"dependencies":[{"issue_id":"emberdelve-27l","depends_on_id":"emberdelve-ffi","type":"parent-child","created_at":"2026-09-08T18:22:52Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"emberdelve-vy7","title":"Turn-1 escalation is a coin flip","description":"Playtest 2026-09-07. Two sessions opened with the identical move — 'I inspect the sarcophagus', failed INVESTIGATION, the first check of the session — and produced two different games.\n\nSession 1 (20260907-030629): spawn_entity only. Five turns of parley followed, and the fight started later when the player chose it.\nSession 2 (20260907-195254): spawn_entity AND start_combat in the same response, 4s apart. In initiative before the player had done anything hostile.\n\ndm-tools.md:47 says the situation must change on the SECOND consecutive failure. Both runs escalated on the first failure of the session, and read the same rule opposite ways.\n\nThis is the M2 finding 'talking to yourself can start a fight' in a new form, and it is not cosmetic: it decides whether the player gets a negotiation or a fight, on turn one, before they have any information.\n\nThe Momentum block only appears when consecutiveFailedChecks \u003e 0 (DmService.java:833), so on the first failure of a session the model sees no momentum line at all and escalates anyway.","acceptance_criteria":"A single failed check on turn 1 does not start combat. The two-failure rule holds across repeated runs of the same opening — check it by replaying, not by reading the prompt.","notes":"Blocks emberdelve-4h9.5 as of 2026-09-07. Left standalone rather than pulled into M4, but it stops being cosmetic there: an attrition game whose first fight is decided by a coin flip on turn one is unfair in a way the current no-stakes game hides.\n\nDECIDED ON THE M4 MAP by emberdelve-ffi.2 — 2026-09-09. Not yet built; this ticket is now an\nimplementation, not an open question.\n\n**`start_combat` stays, for provoked fights. `dm-tools.md`'s two-failure escalation rule goes.**\nThe engine's ALERT clock becomes the only source of an *unprovoked* fight.\n\nWhat was wrong was never that the model could start a fight — a player who swings a sword needs\none to start, and routing that through a clock is absurd. What was wrong is that *unprovoked*\nescalation was decided by a model reading a rule it reads two ways. Both 2026-09-07 sessions\nescalated on the **first** consecutive failure, against a rule that says the second; one then put\nthe player in initiative before they had done anything hostile. That rule was not producing\ntension, it was producing a coin flip, and it decides what kind of game the session is on turn one.\n\nThe replacement is a clock the player has been given signs about: ALERT ticks on rooms entered,\nrests, and fights starting, speaks at thresholds, and fires a table when it fills. Escalation\nbecomes something the player can read coming and act on, which is what the rule was reaching for.\n\n**The behavioural change, named rather than left to be found in play:** ALERT does not tick on\nfailed checks (that is LIGHT's trigger). So a player who fails check after check in one room now\ngenerates no escalation at all, where today they would get a fight. That is intended — failed\nchecks still cost, in the currency that does not ambush you — but it is a real change to a\nmilestone that passed its gate, and the M4 gate session (emberdelve-4h9.15) should watch for it.\n\n`## Momentum` keeps its count regardless: emberdelve-ffi.1 settled that a count driving a tool\ndecision stays a count, and momentum is still a fact the narrator should have even once it stops\ngating an escalation rule.","status":"open","priority":2,"issue_type":"bug","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T20:25:36Z","created_by":"hampton.black","updated_at":"2026-09-09T22:51:25Z","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"emberdelve-xgg.8","title":"Play the dungeon and decide whether DIM reads","description":"The gate for this epic. Everything else in it is decided by a fixture; this is\ndecided by looking.\n\nMust be played in the real Godot client, not headless. AGENTS.md still carries\n\"Dark neighbour unread\" from the M3 gate for exactly this reason — that session\nwas headless, so whether a room through a doorway reads as a dungeon or as a\nhole has never actually been judged.\n\nFollow the M3 gate's shape: cross at least four times, spend real turns in each\nroom, and rotate the camera (Q/E) through all four corners in both rooms.\n\nWHAT TO LOOK AT\n\n1. The complaint. Does crossing still read as a cut to a new box, or as walking\n   into the next part of one place?\n2. The crypt's north corners, seen from the gallery, from ALL FOUR camera\n   corners. This is the xgg.7 verification and the whole reason the wall rule\n   was rewritten. A wall can be right from three corners and wrong from the\n   fourth.\n3. DIM in both directions, and compare them. The crypt seen from the gallery is\n   lit by ambient 0.7; the gallery seen from the crypt by ambient 1.1. That\n   drift is predicted and accepted (ADR-0010 era decision, Q9 of the design) —\n   the question is whether the darker direction still reads as somewhere you\n   have been rather than as a void.\n4. Props across a crossing. Reveal the crypt's alcove, cross, look back: still\n   there? Reveal the gallery's niche, cross twice: still there? This is\n   traversal itself plus the room-qualified PropRef.\n5. The camera in the gallery, whose origin is not (0,0). Does the clamp fight\n   the follow? Then `debug -\u003e start combat` there: does the framing centre on\n   the gallery or snap back toward the crypt?\n6. Picking. A click in the DIM room does nothing and swallows no move; the door\n   back is still clickable; brazier-east still does not shadow the door.\n7. A goblin left behind. Spawn one in the crypt, cross to the gallery, look\n   back. The crypt should show no token — ADR-0011's accepted cost. Confirm it\n   reads as absence rather than as a bug.\n8. The DM, asked about a room it is not in. Type something like \"I look back\n   through the door at the crypt\" and see whether the narration stays coherent\n   without the prompt having been widened.","design":"The one decision this session exists to make: DIM or LIT for visited rooms.\n\nThat was chosen as DIM in the design grilling with LIT named as the fallback,\nand Room.level_for was built to keep the switch to one line. If the darker\ndirection reads as a void, pull it. If it reads as a room you have been in,\nleave it. Either answer closes this ticket; \"it was fine\" without having looked\nat both directions does not.\n\nTwo other outcomes ride on this session:\n\n- ADR-0011 is Proposed, not Accepted. It flips to Accepted if item 6 holds in\n  play — that is the picking confinement being exercised rather than merely\n  compiled.\n- If the ambient drift is the problem rather than DIM itself, the second knob is\n  a per-room tint, not a change of level. Recorded in the epic's design so the\n  fix is not reinvented as \"raise the ambient\", which AGENTS.md already warns\n  against for a different reason.\n\nThe session writes itself to server/sessions/. If anything goes wrong, the file\nthat proves it is already on disk — copy it into docs/evidence/ and it replays\non every build. That is the M2 workflow and it is the reason this ticket does\nnot need a reproduction script.","acceptance_criteria":"- Four or more crossings played in the real client, camera rotated through all\n  four corners in both rooms.\n- No hole at the crypt's north corners from any camera corner.\n- A written verdict on DIM vs LIT, having looked at both directions, and the\n  one-line change made if the verdict is LIT.\n- ADR-0011 moved from Proposed to Accepted, or a note saying why not.\n- Any fault found is filed with its session file, copied into docs/evidence/ if\n  it is worth replaying.","notes":"Item 4 was not actually exercised by the 2026-09-07 sessions. Session 1 revealed zero props. Session 2 revealed the crypt alcove at 20:04:57, after the last crossing at 20:04:24 — so no prop was ever revealed and then crossed away from and returned to. The gallery niche was never found in either run: the player searched the brazier and the pillars, and the revealHint is on the west wall. 'Props survive a crossing' and the room-qualified PropRef are still untested in play.\n\nEverything else on the list was covered, and the epic's DIM question was answered by looking.","status":"closed","priority":2,"issue_type":"task","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T02:44:34Z","created_by":"hampton.black","updated_at":"2026-09-07T21:50:21Z","closed_at":"2026-09-07T21:50:21Z","close_reason":"Played in the real client over two sessions on 2026-09-07. DIM stands — the fallback to LIT is not taken, and the darker direction was judged the better look, inverting the design's expectation that the drift was a cost. ADR-0011 flips to Accepted: picking confinement held in play, and the goblin left behind read as absence rather than as a bug. Findings recorded in AGENTS.md § Judged in play — playtest 2026-09-07. Item 4 (a prop revealed and then crossed away from) was not exercised and is noted there rather than left implied.","labels":["ready-for-human"],"dependencies":[{"issue_id":"emberdelve-xgg.8","depends_on_id":"emberdelve-xgg","type":"parent-child","created_at":"2026-09-06T21:44:33Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-xgg.8","depends_on_id":"emberdelve-xgg.7","type":"blocks","created_at":"2026-09-06T21:44:39Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-xgg.7","title":"Draw the shared wall the way coveredWalls() decides it","description":"Rooms.coveredWalls() holds the shared-wall rule server-side, with tests, since emberdelve-5sc: the plane is owned by whichever room is nearer the entrance (ties by roomId), and omission is a 1D interval overlap rather than a direction match. Nothing ships it. RoomView carries no covered-segment field, so room.gd still omits by direction — the room the party is standing in draws the plane, every other room drops the whole run its answering door is in.\n\nTwo consequences on screen: the crypt's two 1-unit corner overhangs are missing when seen from the gallery (a hole in a wall is a way out), and with more than two rooms drawn the ownership flips with the party rather than staying put.\n\nWire it: add the covered segments to RoomView (or a per-room list on SceneState), have GameEngine.scene() fill it from Rooms.coveredWalls(), and have Room._build_walls skip exactly those segments instead of whole runs. Room.build already takes a RoomView and is the one build path for every room, current or not, so this is the last piece.","design":"Deliberately left out of emberdelve-xgg.4, which delivered the render levels, the persistent world frame on the client and per-room prop holders. Wiring coveredWalls() is a wire change plus a client rule swap, and it is testable on its own — the synthetic three-room chain in RoomsTest already proves the server half.","acceptance_criteria":"A neighbour drawn beside the current room keeps the part of its perimeter that overhangs the shared run: no hole at the crypt's north corners seen from the gallery.\nNo two rooms draw stone on the same plane (the seam does not go to noise, and no door grows a second ring handle).\nOwnership does not change when the party crosses: the same room draws the same plane from either side.\nAGENTS.md's 'decided server-side and not yet drawn' bullet is replaced by what is drawn.","notes":"Second hole the whole-run omission leaves, found reviewing emberdelve-xgg.4: with three rooms drawn, a shared plane can be drawn by nobody. omit_shared_wall is now 'every room but the one the party is in', and _on_a_shared_wall drops the run by direction — so in A(current)-B-C, B and C both omit their mutual run and the wall between them is missing entirely. Latent while M3 ships two rooms; live the day a third exists. coveredWalls() already gets this right (exactly one room owns each plane).","status":"closed","priority":2,"issue_type":"task","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T02:07:03Z","created_by":"hampton.black","updated_at":"2026-09-07T03:04:26Z","started_at":"2026-09-07T02:47:18Z","closed_at":"2026-09-07T03:04:26Z","close_reason":"RoomView.coveredWalls ships Rooms.coveredWalls()' answer and room.gd omits exactly those segments — the crypt's north corners are stone from the gallery, every plane is drawn once by the same room from either side, and the A-B-C plane two neighbours both used to drop is drawn. The doorway is the deliberate exception: a door is one object two rooms address by two ids and World._target_under only scans the current room's walls, so the room the party is in hangs it and every other room leaves its openings open. Room.build takes the current room id rather than a Level so that the agreed DIM-to-LIT fallback cannot hang a second door. Five TDD'd tests, all three client ones mutation-checked against the old direction-match rule. AGENTS.md, ADR-0010 and CONTEXT.md amended. Filed emberdelve-xgg.9 for the generator-era case where a covering room is not on the wire.","labels":["ready-for-agent"],"dependencies":[{"issue_id":"emberdelve-xgg.7","depends_on_id":"emberdelve-xgg","type":"parent-child","created_at":"2026-09-06T21:07:03Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"emberdelve-xgg.6","title":"Record the world-frame decisions and fix the docs they contradict","description":"Written after the server half proves itself, before the client half is wired.\n\n- docs/adr/0009: the world frame is server-owned and anchored at the entrance;\n  a shared wall is owned by BFS distance and omitted by geometric overlap;\n  visible is not addressable. One ADR, because they are one decision about where\n  world-space authority lives.\n- CONTEXT.md: add \"world frame\" and \"render level\"; correct \"scene state\", which\n  currently reads \"the client's whole current picture of a room\" and is no longer\n  true. Flag any term the code needs that the glossary lacks.\n- AGENTS.md's shared-wall bullet is NOT amended here — it goes in emberdelve-5sc,\n  in the same commit that makes it false.","design":"Deliberately not written up front. This repo's docs are empirical — AGENTS.md is\nwall-to-wall \"measured\" and \"found in play\", and ADR-0008 is literally \"model\npicks come from played sessions\". An ADR written before implementation is a\nprediction, and the interval-overlap wall rule is the piece most likely to come\nback different. Writing it once the synthetic three-room fixture proves the world\nframe and the wall rule means it records something demonstrated, while still\nlanding early enough that the client is built against a written-down rule.\n\nGlossary entries land with the code introducing each term, not in a batch.","acceptance_criteria":"- ADR-0009 exists, follows docs/adr/0000-template.md, and states what was\n  rejected (client-side offset accumulation; full world-space packing) and why.\n- CONTEXT.md's \"scene state\" entry is true of the shipped SceneState.\n- No rule in AGENTS.md or CONTEXT.md contradicts the code after this epic closes.","status":"closed","priority":2,"issue_type":"task","owner":"hampton.j.black@gmail.com","created_at":"2026-09-06T21:52:06Z","created_by":"hampton.black","updated_at":"2026-09-07T02:41:37Z","closed_at":"2026-09-07T02:41:37Z","close_reason":"ADR-0009 (world frame anchored at the entrance), ADR-0010 (the room nearer the entrance owns the shared wall) and ADR-0011 (visible is not addressable, Proposed until the picking confinement is exercised in play) written in 8b3b1eb; index updated and the back-fill note narrowed to 0001-0008. CONTEXT.md in d01a01c: 'scene state' corrected, 'room view', 'world frame' and 'render level' added, Diff's union completed with EntityRemoved and its current-room scoping stated. AGENTS.md needed no further amendment — the shared-wall bullets were already rewritten under 5sc and already flag xgg.7 as the outstanding client half.","dependencies":[{"issue_id":"emberdelve-xgg.6","depends_on_id":"emberdelve-xgg","type":"parent-child","created_at":"2026-09-06T16:52:05Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-xgg.6","depends_on_id":"emberdelve-5sc","type":"blocks","created_at":"2026-09-06T16:52:20Z","created_by":"hampton.black","metadata":"{}"}],"comments":[{"id":"01a078de-a57c-700b-b944-10d85fe88f6d","issue_id":"emberdelve-xgg.6","author":"hampton.black","text":"ADRs written 2026-09-06, ahead of the client half as planned. Split into three rather than the one agreed in the grilling: docs/adr/README.md says one decision per file, and origins (a45640f) and wall ownership (03b7dd3) landed as separate commits with separate test classes — you can have entrance-anchored origins and still omit whole runs, which is exactly what a45640f did.\n\n- ADR-0009 the world frame is server-owned and anchored at the entrance (Accepted)\n- ADR-0010 the room nearer the entrance owns the shared wall (Accepted)\n- ADR-0011 visible is not addressable (Proposed — flips to Accepted when xgg.5 lands the picking confinement)\n\nIndex updated; the back-fill note now reads 0001-0008 so it stays true.\n\nSTILL OPEN ON THIS TICKET: the CONTEXT.md glossary work. Per the agreed sequencing those terms land with the code that introduces them — 'world frame' and 'render level' plus the corrected 'scene state' entry, which currently reads 'the client's whole current picture of a room' and stops being true at xgg.2.","created_at":"2026-09-06T22:37:32Z"}],"dependency_count":1,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"emberdelve-xgg.5","title":"Keep picking and the camera on the room the party is in","description":"Two consequences of the current room no longer sitting at the world origin.\n\nPICKING. world_to_grid still assumes the live room is centred on the origin, so\nit silently mis-resolves the moment origins are entrance-anchored. Make it\nroom-aware — the exact inverse of grid_to_world for a given room — and have\npick_at resolve which room a floor hit landed in. A hit outside the current\nroom's rectangle returns null: a DIM room is scenery and a click in one must not\nissue a move.\n\n_target_under needs no change for this epic. It scans Room/Walls, and the door\nback is in the current room's own wall run, so chaining crossings already works.\nNeighbour walls live under a different holder and are never scanned, so a DIM\nroom cannot steal a pick.\n\nCAMERA. Two places assume the origin is the current room's centre:\n_framing_target() centres combat framing on Vector3.ZERO, and _clamp_to_room()\nbuilds its clamp corners at +/- extent around the origin. Left alone, standing in\nthe gallery clamps the camera to a rectangle around the crypt while it tries to\nfollow a party in the gallery — which is exactly the failure camera_rig.gd:23\nwarns about, \"clamp-to-room quietly cancels the follow\". Translate both to the\ncurrent room's origin.","design":"Clamp to the current room's rectangle rather than the discovered bounding box.\nThe bounding box would let the camera drift a long way from the party in a large\ndungeon, and _combat_half_height is tuned to fit one room. Adjacent DIM rooms\nshow up at the frame edges on their own, which is the continuous-dungeon read the\nepic is after, without giving up the framing already tuned.\n\nPicks confined to the current room are what hold the \"visible is not addressable\"\nline from the epic's decision 8. Travel to a room two doors away is out of scope\nand this is where that stays true rather than half-true.","acceptance_criteria":"- world_to_grid round-trips against grid_to_world for a room at a non-zero origin.\n- A click on a DIM room's floor produces no intent and sends nothing.\n- The door back is still clickable and still crosses.\n- The camera follows the party in a room whose origin is not (0,0), with no\n  clamp fighting the follow.\n- Combat framing centres on the current room, not the entrance.","notes":"Live after emberdelve-xgg.4: the current room now stands at its absolute origin, so World.world_to_grid (world.gd) — still origin-centred — mis-resolves every floor click in any room but the entrance. pick_at drops the click silently, _sync_tokens re-slides tokens on every scene change, and token.gd paths from the wrong square. The branch is playable in the crypt and not in the gallery until this lands. test_world_to_grid_still_inverts_the_current_room asserts the old origin-centred behaviour and needs replacing with a round-trip against a room at a non-zero origin.","status":"closed","priority":2,"issue_type":"task","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-06T21:52:05Z","created_by":"hampton.black","updated_at":"2026-09-07T02:35:37Z","started_at":"2026-09-07T02:21:09Z","closed_at":"2026-09-07T02:35:37Z","close_reason":"world_to_grid is room-aware and pick_at resolves the room a floor hit landed in; camera clamp and combat framing translate to the current room's origin. 232 godot tests green, server suite exit 0. Commit c01d33a.","labels":["ready-for-agent"],"dependencies":[{"issue_id":"emberdelve-xgg.5","depends_on_id":"emberdelve-xgg","type":"parent-child","created_at":"2026-09-06T16:52:04Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-xgg.5","depends_on_id":"emberdelve-xgg.4","type":"blocks","created_at":"2026-09-06T21:15:52Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-xgg.5","depends_on_id":"emberdelve-xgg.3","type":"blocks","created_at":"2026-09-06T16:52:19Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-xgg.4","title":"Draw visited rooms at a DIM render level","description":"The visual half.\n\n- Give the client one policy function returning a render level per room: BLACK\n  (never seen), DIM (visited, not current), LIT (current). Everything else keys\n  off that, so switching visited rooms from DIM to LIT is a one-line change.\n- DIM builds with real materials and torch models but no OmniLight3D and no\n  flames. Only the current room contributes lights.\n- The origin table persists across a crossing. World stops clearing _origins and\n  _sizes on a roomId change and stops re-centring the new room on the world\n  origin; rooms sit where the server put them and the camera moves instead.\n- Props get per-room holders. Today Props is flat and keyed by bare prop id, and\n  _instance_prop silently returns early when the name is taken. Prop ids are only\n  unique within a room (see PropRef): PropPlacer names every room's first pillar\n  pillar-0, so drawing two generated rooms would silently drop props. Per-room\n  holders fix that and give dimming a natural traversal.\n- Room._build_walls' `get_parent() is World` special case goes: every room takes\n  its registered origin, including the current one.","design":"DIM as \"no lights\" rather than \"reduced-energy lights\" or \"tint override\" keeps\nMAX_TORCH_LIGHTS a per-room budget however large the dungeon gets — only the\ncurrent room ever adds lights. Reduced-energy torches would cost 10 omni lights\nper visited room and hand the generator milestone a light-count problem on day\none.\n\nKnown and accepted: a DIM room's brightness drifts with the current room's\nLightingPreset, because the WorldEnvironment ambient is global (TORCHLIT 1.1,\nDIM 0.7, DARK 0.35). The crypt seen from the gallery reads darker than the\ngallery seen from the crypt. If that reads badly in play, a per-room tint is the\nsecond knob; the first knob is DIM -\u003e LIT.\n\nThe prop-id collision is latent today — crypt and gallery ids happen to differ —\nand is fixed here rather than deferred because this is the change that makes\nmulti-room prop rendering exist, and the failure mode is a prop that silently\ndoes not appear. That is the project's characteristic silent failure.","acceptance_criteria":"- Crossing to the gallery leaves the crypt drawn at DIM with its props; the scene\n  does not re-centre and no room is rebuilt at the world origin.\n- Crossing back returns the crypt to LIT and the gallery to DIM, as left.\n- A room never entered is still BLACK.\n- Only the current room contributes OmniLight3D nodes; count asserted in a test.\n- Two rooms carrying the same prop id both draw their prop.\n- DIM -\u003e LIT for visited rooms is a one-line change (demonstrated in the commit\n  message or a comment naming the line).","status":"closed","priority":2,"issue_type":"task","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-06T21:51:41Z","created_by":"hampton.black","updated_at":"2026-09-07T02:16:09Z","started_at":"2026-09-07T01:49:12Z","closed_at":"2026-09-07T02:16:09Z","close_reason":"Render levels landed: Room.level_for is the one policy (LIT current / DIM visited / BLACK never entered), Room.build is the one build path for every room, the client's origin table is never cleared so crossing moves the camera rather than the dungeon, and props hang under Props/\u003croom id\u003e so two rooms sharing a prop id both draw. Godot suite 224 tests green; Java suite 297 green, untouched. Follow-ups filed: emberdelve-xgg.7 (wire Rooms.coveredWalls()), and emberdelve-xgg.5 now depends on this and must land before a non-entrance room is clickable.","labels":["ready-for-human"],"dependencies":[{"issue_id":"emberdelve-xgg.4","depends_on_id":"emberdelve-xgg","type":"parent-child","created_at":"2026-09-06T16:51:40Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-xgg.4","depends_on_id":"emberdelve-xgg.3","type":"blocks","created_at":"2026-09-06T16:52:19Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"emberdelve-xgg.3","title":"Read the current room through Table.room() everywhere","description":"Mechanical churn to match the new wire shape. Around fourteen call sites read\nroom-level fields straight off Table.scene — width, height, floorType, wallType,\nlighting, exits, props, roomId — across world.gd, room.gd, camera_rig.gd,\nlighting.gd and table.gd. Those fields no longer live at the top level.\n\nAdd a Table.room() accessor returning the current room's RoomView, and a\nTable.room_by_id() for the others. Convert the call sites. No behaviour change in\nthis task: the world still draws one lit room and the rest black, exactly as\ntoday, just read through the new shape.\n\nKept separate from the visual work so that a regression here is obviously churn\nand a regression there is obviously the feature.","design":"Table remains the single owner of game state (invariant #3, and CONTEXT.md's\n\"Table\"). The accessor is what stops fourteen call sites each doing their own\nlookup into rooms[] by roomId.\n\nTable currently mutates scene[\"props\"] on a PropRevealed diff. That write moves\nto the current room's RoomView, which is correct because the diff channel is\ncurrent-room-scoped.","acceptance_criteria":"- No production .gd file reads width/height/floorType/wallType/lighting/exits/\n  props directly off Table.scene.\n- PropRevealed still lands on the current room's props.\n- Both suites pass with no visible change to the running game.","status":"closed","priority":2,"issue_type":"task","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-06T21:51:40Z","created_by":"hampton.black","updated_at":"2026-09-06T23:05:57Z","started_at":"2026-09-06T22:55:17Z","closed_at":"2026-09-06T23:05:57Z","close_reason":"Table.room()/room_by_id() land; room.gd, camera_rig.gd, lighting.gd and world.gd read the current room through them instead of Table.scene's old flat fields. PropRevealed now writes to the current room's RoomView. Client test suite (scene_fixtures.gd helper + all fixture updates) converted to the new SceneState wire shape. Both suites green: 217/217 Godot GUT, server ./gradlew test.","labels":["ready-for-agent"],"dependencies":[{"issue_id":"emberdelve-xgg.3","depends_on_id":"emberdelve-xgg","type":"parent-child","created_at":"2026-09-06T16:51:39Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-xgg.3","depends_on_id":"emberdelve-xgg.2","type":"blocks","created_at":"2026-09-06T16:52:18Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"emberdelve-xgg.2","title":"SceneState describes every known room, not one","description":"SceneState carries one room at top level plus a neighbours list of bare outlines.\nReshape it so every room is described the same way:\n\n  SceneState { roomId, rooms: List\u003cRoomView\u003e, entities, blocked, mode, combat }\n\nRoomView { roomId, width, height, floorType, wallType, lighting, props, exits,\noriginX, originZ, visited }. roomId says which room the party is in; entities,\nblocked, mode and combat stay session-level and stay scoped to that room.\n\nGameEngine.scene() ships a RoomView for every room in Rooms that the party has\neither visited or can see through an exit of the current room. A visited room\ncarries its visible props, dressed via room(roomId) and filtered by the\nroom-qualified revealedProps. An unvisited one carries geometry only and\nvisited=false.\n\nEntities are NOT shipped for non-current rooms. entitiesHere() is unchanged.","design":"Forced by emberdelve-5sc. The shared-wall owner is decided by BFS distance from\nthe entrance, not by which room the party stands in, so the current room and a\nnon-current room have to go through the same wall-building code. Two wire shapes\nmeans two code paths and the rule implemented twice — which is how you get a wall\nright from one side and wrong from the other.\n\nRoomView matches the EntityView / CombatView naming already in dm.model.\n\nEntities excluded deliberately: a token in a non-current room is a creature the\nplayer can see that the DM's prompt was never told about, which is spec section\n8b's contradiction risk with a monster in it. Spec section 4b already keeps\nentities in the fold where they were left; this only declines to draw them.\n\nDiff is unchanged. Diff.PropRevealed carries no roomId and does not need one:\nreveal_prop only ever offers the current room's hidden props, so the diff channel\nstays current-room-scoped by convention. Worth stating rather than rediscovering.\n\nNo Event carries SceneState, so SCHEMA_VERSION does not bump and recordFixture is\nnot required.","acceptance_criteria":"- rooms[] contains the current room, every visited room, and every room visible\n  through a current-room exit; nothing else.\n- The current room appears in rooms[] in the same shape as every other room.\n- A visited room's props include secrets revealed in THAT room and exclude\n  secrets revealed elsewhere (PropRef is room-qualified).\n- An unvisited room carries visited=false and no props.\n- No non-current room carries entities.\n- SCHEMA_VERSION unchanged; session-m3-traversal.jsonl replays with exit 0.","status":"closed","priority":2,"issue_type":"task","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-06T21:51:14Z","created_by":"hampton.black","updated_at":"2026-09-06T22:50:26Z","started_at":"2026-09-06T22:41:30Z","closed_at":"2026-09-06T22:50:26Z","close_reason":"SceneState reshaped to { roomId, rooms: List\u003cRoomView\u003e, entities, blocked, mode, combat }; GameEngine.scene() ships visited + exit-visible rooms uniformly. Java suite (299 tests) and Godot suite (217 tests) pass; session-m3-traversal.jsonl replays identical, exit 0.","labels":["ready-for-agent"],"dependencies":[{"issue_id":"emberdelve-xgg.2","depends_on_id":"emberdelve-xgg","type":"parent-child","created_at":"2026-09-06T16:51:14Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-xgg.2","depends_on_id":"emberdelve-xgg.1","type":"blocks","created_at":"2026-09-06T16:52:17Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-xgg.2","depends_on_id":"emberdelve-5sc","type":"blocks","created_at":"2026-09-06T16:52:18Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"emberdelve-xgg.1","title":"Anchor every room's origin at the entrance, server-side","description":"RoomOutline.beside computes an offset relative to the room the party is standing\nin. That works for one hop and cannot place a room two hops away: composing the\noffsets is the client's problem today only because the client never has to.\n\nWalk the exit graph from Rooms.first() breadth-first, composing beside() offsets,\nand produce an absolute origin for every room in Rooms. The entrance sits at the\nworld origin and never moves. beside() itself is unchanged and stays the single\nimplementation of the door-alignment arithmetic.\n\nCycles: first path wins. A second path to an already-placed room is ignored, not\nreconciled — reconciliation is real world-space packing and belongs to the\ngenerator milestone (spec section 3, \"world-space packing\").","design":"Chosen over having the client accumulate C_abs = B_abs + offset. RoomOutline's\ndoc comment states the reason the arithmetic is server-side: \"so there is one\nimplementation of the arithmetic and it can be tested without a renderer.\"\nClient-side accumulation moves composition somewhere it can only be tested with a\nrenderer, and turns a cycle disagreement into an emergent client behaviour rather\nthan a defined server one.\n\nA one-way exit already yields null from beside() and is filtered out; a room\nreachable only through one is simply not placed and not drawn.","acceptance_criteria":"- A pure function maps Rooms to roomId -\u003e origin, unit-tested with no renderer.\n- Rooms.first() is at (0, 0).\n- A three-room chain A-B-C places C correctly relative to A.\n- A cycle A-B-C-A places every room once, deterministically, first path winning.\n- crypt/gallery origins match what RoomOutlineTest already asserts for one hop.","status":"closed","priority":2,"issue_type":"task","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-06T21:50:36Z","created_by":"hampton.black","updated_at":"2026-09-06T22:04:42Z","started_at":"2026-09-06T22:01:26Z","closed_at":"2026-09-06T22:04:42Z","close_reason":"Added Rooms.origins(): BFS from first(), composing RoomOutline.beside offsets, into an unmodifiable roomId-\u003eRoomOrigin map. TDD'd in RoomsOriginsTest (single room, one-hop matching RoomOutlineTest, three-room chain composition, cycle first-path-wins, one-way-exit exclusion).","labels":["ready-for-agent"],"dependencies":[{"issue_id":"emberdelve-xgg.1","depends_on_id":"emberdelve-xgg","type":"parent-child","created_at":"2026-09-06T16:50:36Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"emberdelve-xgg","title":"Visited rooms stay on the board","description":"Today the world shows one room at a time. Crossing a threshold re-centres the\nscene on the room just entered and the room just left drops to a near-black\noutline, so the dungeon reads as a series of boxes rather than one place.\n\nThis epic keeps every room the party has stood in drawn and legible, at a DIM\nrender level, in a single world frame anchored at the entrance. The party still\noccupies exactly one room and can still only act in that room: what changes is\nwhat is drawn, not what is addressable.\n\nScope is chaining crossings without the scene cutting. Clicking a door two rooms\naway and walking there is NOT in scope — that needs multi-room routing the\nserver does not have and collides with the arrival-narration beat.","design":"Ten decisions, taken in a grilling session on 2026-09-06.\n\n1. RENDER LEVEL. Three levels chosen by one policy function: BLACK (never seen),\n   DIM (visited, not current), LIT (current). Making visited rooms LIT instead of\n   DIM must stay a one-line change — it is the agreed fallback if DIM looks wrong\n   in play.\n2. DIM MEANS NO LIGHTS. Real materials, torch models placed, but no OmniLight3D\n   and no flames. Only the current room contributes lights, so MAX_TORCH_LIGHTS\n   stays a per-room budget however large the dungeon gets. Known consequence: a\n   DIM room's brightness drifts with the current room's LightingPreset ambient\n   (TORCHLIT 1.1 vs DIM 0.7). Accepted; a per-room tint is the second knob if it\n   reads badly.\n3. PAYLOAD IS GEOMETRY + PROPS. Visible props including secrets already revealed\n   there. No entities — a creature the player can see but the DM was never told\n   about is spec section 8b's contradiction problem in a new costume.\n4. WORLD FRAME IS SERVER-OWNED, ANCHORED AT THE ENTRANCE. BFS from Rooms.first()\n   composing RoomOutline.beside offsets; ship each known room's absolute origin.\n   Cycles resolve first-path-wins. Chosen over client-side accumulation because\n   RoomOutline's own rationale is that the arithmetic lives server-side \"so there\n   is one implementation and it can be tested without a renderer\". Real\n   world-space packing stays deferred to the generator milestone (spec section 3).\n5. SHARED WALL OWNERSHIP. The plane is drawn once by whichever room is nearer the\n   entrance in that BFS, ties broken by roomId. Omission is a 1D interval overlap\n   on the shared plane, not a direction match. This replaces \"the room the party\n   is standing in owns the shared wall\", which stops being a usable rule once more\n   than one room is visible.\n6. UNIFORM WIRE. SceneState becomes { roomId, rooms: List\u003cRoomView\u003e, entities,\n   blocked, mode, combat }. Every room including the current one is a RoomView\n   carrying origin, size, floorType, wallType, lighting, props, exits, visited.\n   Forced by decision 5: the wall owner is decided by BFS distance, not by which\n   room the party stands in, so current and non-current rooms must go through one\n   code path or the rule gets implemented twice.\n7. CAMERA. Clamp and combat framing translate to the current room's origin\n   instead of Vector3.ZERO. Not the discovered bounding box — that would let the\n   camera drift off the party and _combat_half_height is tuned to one room.\n8. DM UNCHANGED. Prompt and ToolSchema stay scoped to the current room. The line\n   is: visible is not addressable. This is what keeps out-of-scope travel out\n   rather than half-in.\n9. FIXTURES ARE SYNTHETIC. Rooms.of() three-room chains and a cycle in the suite;\n   no third authored room in content/, so the AGENTS.md anti-goal is untouched.\n10. DOCS. One ADR for decisions 4/5/8, written after the server half proves them\n    rather than before. AGENTS.md's shared-wall line amended in the same commit\n    that makes it false. CONTEXT.md gains \"world frame\" and \"render level\" and a\n    corrected \"scene state\" entry, each with the code that introduces the term.\n\nFREE, CONFIRMED: no Event carries SceneState, so SCHEMA_VERSION does not bump,\nrecordFixture is not needed, and session-m3-traversal.jsonl keeps replaying. Diff\nis unchanged and stays current-room-scoped by convention.\n\nRISK: the interval-overlap wall rule is the riskiest piece. It takes on exactly\nthe parity problem the current code deliberately dodged, and a wall right from\none side and wrong from the other is only visible from one camera corner. Build\nit server-side and pure, prove it on the synthetic three-room chain, then wire\nthe client.","acceptance_criteria":"- Crossing from the crypt to the gallery leaves the crypt drawn, DIM, with its\n  props and any secrets revealed there. The scene does not re-centre.\n- Crossing back finds the gallery as it was left, and the crypt returns to LIT.\n- No hole appears at the crypt's north corners when viewed from the gallery.\n- A room never visited is still BLACK and still absent from the DM's prompt.\n- Clicking anything in a DIM room does nothing and cannot issue a move.\n- Both suites pass; session-m3-traversal.jsonl still replays with exit 0.","status":"open","priority":2,"issue_type":"epic","owner":"hampton.j.black@gmail.com","created_at":"2026-09-06T21:50:21Z","created_by":"hampton.black","updated_at":"2026-09-06T21:50:21Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-7m6","title":"Props are not click targets yet","description":"world.gd _target_under considers only wall segments carrying an exit_id. An earlier cut scanned Props as well, so that objects would be clickable generally, and it broke the door: picking takes the nearest hit, and brazier-east stood between the camera corner and the north wall. It won the ray, had no intent, and fell through to a null square. When a prop gains a real action (open the sarcophagus, search the alcove), add it to _target_under and decide how it ranks against a doorway and against the floor square behind it. test_overlay.gd's DOORWAY fixture now carries the crypt's real furniture, so a regression shows up.","notes":"Pulled into M4 (emberdelve-4h9) 2026-09-07. Two children hard-depend on it: 4h9.4 (the objective is a prop you take) and 4h9.6 (DM-placed markers). Section 2a of the design doc is the argument - an objective the player cannot pick up is not an objective, and routing 'take the relic' through free text alone puts a mechanical outcome back on the narrator's side of the governing principle.","status":"open","priority":2,"issue_type":"task","owner":"hampton.j.black@gmail.com","created_at":"2026-09-06T20:24:51Z","created_by":"hampton.black","updated_at":"2026-09-07T22:31:08Z","dependencies":[{"issue_id":"emberdelve-7m6","depends_on_id":"emberdelve-4h9","type":"parent-child","created_at":"2026-09-07T17:31:07Z","created_by":"hampton.black","metadata":"{}"}],"comments":[{"id":"01a078b5-689f-72ca-a47a-335e4b1171f1","issue_id":"emberdelve-7m6","author":"hampton.black","text":"emberdelve-xgg moves props into per-room holders (prop ids are only unique within a room). When this is picked up, _target_under must scan the current room's holder only — a prop in a DIM room must never win a ray, for the same reason a DIM floor square resolves to no intent.","created_at":"2026-09-06T21:52:30Z"}],"dependency_count":0,"dependent_count":2,"comment_count":1}
{"_type":"issue","id":"emberdelve-5sc","title":"A neighbour room's wall may overhang the shared plane","description":"room.gd now has a neighbour omit the entire wall run its answering door is in, so the two rooms do not draw coincident geometry. When the neighbour is WIDER than the room the party is standing in, the part of its perimeter that overhangs the live room's wall is now missing too — its floor runs to an open edge there. Invisible today because a neighbour is painted near-black and the crypt (12) is wider than the gallery (10). Will show the moment the generator packs rooms of mixed sizes. Fix when it does: keep the overhanging segments and drop only the coincident run.","design":"Promoted from latent to live by emberdelve-xgg: the overhang is invisible only\nwhile a non-current room is painted near-black. At the DIM render level it is two\n1-unit holes at the crypt's north corners, visible from the gallery. Shipping\nthat would regress the playtest fix recorded in AGENTS.md — \"a hole in the wall\nis a way out, and there are no other holes\".\n\nThe rule \"the room the party is standing in owns the shared wall\" is also no\nlonger usable. With several rooms visible at once a middle room has shared walls\non both sides, RoomOutline.back carries only one answering door, and two adjacent\nDIM rooms would each omit their shared run and delete the wall between them.\n\nREPLACEMENT RULE, two halves:\n\n1. OWNERSHIP. The shared plane is drawn once, by whichever of the two rooms is\n   nearer the entrance in the BFS from emberdelve-xgg.1, ties broken by roomId so\n   it is stable and testable.\n2. OMISSION. The non-owner omits only the segments the owner geometrically\n   covers, computed as a 1D interval overlap on the shared plane using the\n   absolute origins. Not a direction match — _on_a_shared_wall's whole-run\n   omission is what loses the overhang.\n\nRiskiest piece of the epic. It takes on precisely the parity problem the current\ncode dodged (\"matching segment against segment can half-work when the rooms'\nwidths differ in parity\"), and a wall correct from one side and wrong from the\nother is only visible from one camera corner. Absolute server-computed origins\nare what make it tractable: the comparison is on known world-space intervals\nrather than grid indices of two differently-sized rooms.\n\nBuild and prove it server-side and pure, on the synthetic three-room chain,\nbefore any client wiring.\n\nAGENTS.md's shared-wall bullet is amended in the same commit as this change — it\nis true until then, and false after.","acceptance_criteria":"- The shared plane between two rooms carries exactly one set of wall segments.\n- Crypt (12) viewed from gallery (10): both overhanging north segments present,\n  no hole at either corner.\n- Two adjacent rooms that are both non-current still have their shared wall.\n- A middle room in an A-B-C chain draws correct walls on both shared planes.\n- Ownership is deterministic across runs and asserted without a renderer.\n- AGENTS.md's \"the room the party is standing in owns the shared wall\" bullet is\n  amended in this commit.","status":"closed","priority":2,"issue_type":"task","assignee":"hampton.black","owner":"hampton.j.black@gmail.com","created_at":"2026-09-06T19:31:53Z","created_by":"hampton.black","updated_at":"2026-09-06T22:28:48Z","started_at":"2026-09-06T22:13:18Z","closed_at":"2026-09-06T22:28:48Z","close_reason":"Rooms.coveredWalls(): the shared plane is drawn once, by whichever room is fewer doors from the entrance (ties by roomId), and the loser omits only the segments the owner's run contains whole — a 1D interval overlap on absolute origins, not a direction match. Pure and server-side; client wiring is xgg.2/xgg.4. Five TDD'd cases in RoomsCoveredWallsTest, mutation-checked against reversed tiebreak, ignored hop distance, and whole-run omission. AGENTS.md's shared-wall bullet amended.","labels":["ready-for-agent"],"dependencies":[{"issue_id":"emberdelve-5sc","depends_on_id":"emberdelve-xgg.1","type":"blocks","created_at":"2026-09-06T16:50:57Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-5sc","depends_on_id":"emberdelve-xgg","type":"parent-child","created_at":"2026-09-06T16:50:40Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"emberdelve-z71","title":"Add PreCompact/PostCompact beads hooks for Claude Code","description":"Codex has four beads hooks wired in .codex/hooks.json (SessionStart, UserPromptSubmit, PreCompact, PostCompact) but .claude/settings.json only has SessionStart running 'bd prime --hook-json'. After a compaction in Claude Code the beads workflow context is gone and nothing restores it, so an agent mid-task can forget to use bd. Add PreCompact and PostCompact entries so context is refreshed the way Codex does it.","status":"closed","priority":2,"issue_type":"task","assignee":"hampton.black","created_at":"2026-09-06T18:11:24Z","updated_at":"2026-09-06T18:35:26Z","started_at":"2026-09-06T18:34:42Z","closed_at":"2026-09-06T18:35:26Z","close_reason":"PreCompact and PostCompact hooks added to .claude/settings.json; both verified by executing the stored command strings verbatim","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-w0a","title":"Record the gemini-3-8-flash prose switch in ADR-0003","description":"ADR-0003 currently ends at the M2 split decision and still implies any strong writer is usable in the prose slot. The M2 gate found otherwise: venice-uncensored-role-play was the better voice and the worse follower, so DM_MODEL_PROSE moved to gemini-3-8-flash with DM_REASONING_EFFORT_PROSE=low. Record the amendment and the rule-following bar it establishes for the prose slot.","status":"closed","priority":2,"issue_type":"task","assignee":"hampton.black","created_at":"2026-09-06T17:52:20Z","updated_at":"2026-09-06T17:55:20Z","started_at":"2026-09-06T17:52:21Z","closed_at":"2026-09-06T17:55:20Z","close_reason":"ADR-0003 amended with the gemini-3-8-flash/low switch; ADR-0008 added for the pick evidence; AGENTS.md model sections compressed 108 -\u003e 39 lines with every fact preserved","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-ggp","title":"Disable superpowers plugin for this repo only","description":"Superpowers is a user-scope plugin enabled globally. Turn it off in project .claude/settings.json so its SessionStart injection and skills stop competing with the Pocock skills here, without affecting other repos.","status":"closed","priority":2,"issue_type":"task","assignee":"hampton.black","created_at":"2026-09-06T17:29:50Z","updated_at":"2026-09-06T17:34:56Z","started_at":"2026-09-06T17:31:00Z","closed_at":"2026-09-06T17:34:56Z","close_reason":"CONTEXT.md + docs/adr/ (template, README, 7 back-filled ADRs) written; superpowers disabled per-repo","dependencies":[{"issue_id":"emberdelve-ggp","depends_on_id":"emberdelve-1fk","type":"parent-child","created_at":"2026-09-06T12:29:50Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-4h9.6","title":"DM-placed markers - a fact you can click","description":"Section 9, Props are handles, not depictions: \"If only rendered props are interactable,\nthe player learns to ignore everything the narrator improvises - which is the exact\nopposite of what this game is for. The fix is the one assert_fact already uses: let the\nmodel place a marker. It emits a position and a closed-enum tag, the engine drops a\ngeneric interaction glyph there, and the scorched patch the prose just mentioned becomes\ntouchable. A fact you can click is a fact the board can back.\"\n\nThis is the partial answer to risk 3, the narrator describing a world change nothing can\nback. It does not make the board agree with the hotdog. It does make the scorched patch,\nthe scratched sigil and the fresh tracks things the player can point at.\n\nThe tag is closed and validated like every other enum (invariant 7). The marker text is\na fact and makes one round trip back into the prompt, exactly as assert_fact already\ndoes - no roll, no legal move, no renderer decision.\n\nReconcile-phase tool, not mechanics: the narrator is the one holding the information and\na false positive is a glyph nobody clicks. That is the rule from AGENTS.md, LLM tools,\napplied straight.\n\nWatch the interaction with anchors. emberdelve-pa8 records that assert_fact anchors\nalready drift, duplicate, and leak grid coordinates. A marker is an anchor that is now\nvisible, so pa8 stops being cosmetic the day this ships.\n","acceptance_criteria":"The DM can place a marker at a square with a closed-enum tag. It renders as a generic\nglyph, is a pick target, and clicking it returns the fact to the DM as context. Grid\ncoordinates never reach narration. A test asserts an invalid tag or an out-of-bounds\nsquare is rejected server-side.","status":"open","priority":3,"issue_type":"feature","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T22:29:05Z","created_by":"hampton.black","updated_at":"2026-09-07T22:29:05Z","dependencies":[{"issue_id":"emberdelve-4h9.6","depends_on_id":"emberdelve-7m6","type":"blocks","created_at":"2026-09-07T17:30:42Z","created_by":"hampton.black","metadata":"{}"},{"issue_id":"emberdelve-4h9.6","depends_on_id":"emberdelve-4h9","type":"parent-child","created_at":"2026-09-07T17:29:04Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"emberdelve-pa8","title":"assert_fact anchors drift, duplicate, and still leak grid coordinates","description":"Playtest 2026-09-07, both sessions. Four separate faults in what reaches ## Established.\n\nWRONG ANCHOR, both sessions. Searching the goblin's body anchored to 'sarcophagus':\n  s1 03:20:14 'The fighter's hurried hands found only sharp bits of scavenged iron... in the goblin's rags' -\u003e on/sarcophagus\n  s2 20:06:xx 'The sarcophagus lid has fresh scratches...' asserted after a turn about the goblin's body\n\nNO DEDUP. Session 2 asserted the same sentence verbatim two turns apart with different anchors:\n  19:59:5x 'Their fingers found no seam in the fluted pillars or the brazier's rusted joints.' -\u003e at_square (5,0)\n  20:00:3x the identical string -\u003e ambient\nWorldState.asserted (WorldState.java:233) appends unconditionally, so ## Established now carries it twice and the model is told twice that it is true.\n\nRE-ASSERTING AUTHORED DRESSING. Session 2's last fact is crypt.json's propDescriptions.sarcophagus almost word for word. Nothing is learned and a slot in the per-room cap is spent.\n\nGRID COORDINATES, still. at_square (0,6) in session 1, (5,0) in session 2. This is the m2-evaluation §8 finding recurring, and AGENTS.md already warns the coordinates will be read aloud the moment a writer is sloppier than Gemini.\n\nThe anchor is a closed enum and validated, so this is not an invariant-7 hole — it is the model choosing badly inside a legal set.","acceptance_criteria":"A verbatim repeat of an existing fact in the same room does not create a second one. Consider whether an anchor pointing at a prop the turn never mentioned should be rejected rather than recorded.","status":"open","priority":3,"issue_type":"bug","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T20:25:58Z","created_by":"hampton.black","updated_at":"2026-09-07T20:25:58Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-xgg.11","title":"A fact about one side of a door never reaches the other","description":"Playtest 2026-09-07, session 2. The player kicked door-north open in the crypt and the reconcile pass recorded it: 'The north door's mortise gave way and the slab door is now open.' From the gallery they asked what they could see back in the crypt, and the DM answered that the door 'sits shut against the mouth of the stair.'\n\nWorldState.factsHere() is room-scoped, so the crypt's fact cannot reach the gallery. The gallery's prompt carried door-south's authored description, which said shut, with nothing contradicting it.\n\nOne door, two props, two descriptions, two truths. Same shape as the client bug already fixed on this branch — 'the thing you see and the thing you click are one object' — except the DM's view still has two of everything a doorway is. Exit carries the shared prop id and toRoomId, but there is no back-pointer to the answering exit; you resolve it by scanning the target room's exits for toRoomId == thisRoom.\n\nemberdelve-xgg.10 removed the immediate trigger by taking 'shut' out of the gallery description and telling the model every exit is open. That makes this much harder to hit and does not fix it: any fact anchored on an exit prop still stops at the room boundary.\n\nOptions, none chosen: mirror an exit-anchored fact into the paired room at assert time; widen the prompt to include the neighbour's exit-anchored facts; or make the exit's state real, which is emberdelve-ql3 and makes this moot.","status":"open","priority":3,"issue_type":"bug","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T20:25:48Z","created_by":"hampton.black","updated_at":"2026-09-07T20:25:48Z","dependencies":[{"issue_id":"emberdelve-xgg.11","depends_on_id":"emberdelve-xgg","type":"parent-child","created_at":"2026-09-07T15:25:47Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-ql3","title":"Give an exit a real shut/locked state","description":"Fix (b) to emberdelve-xgg.10's (a). That one made the prompt tell the truth: every exit is always passable. This one would make the fiction possible instead.\n\nToday crossExit (GameEngine.java:284) refuses exactly one thing, leaving mid-fight. Exit is (id, x, y, direction, toRoomId) with no state.\n\nPlaytest 2026-09-07 wanted this twice without being offered it. Session 2 found a copper key on the goblin's body, tried it on the north door, and was told there was no keyway — because there is no lock in the model to put one in. Both sessions invented an ATHLETICS DC 20 to force a door that was never shut. The player is reaching for a mechanic that does not exist.\n\nWeigh against AGENTS.md's anti-goals before starting: 'do not build a rules engine', 'do not generalize'. A locked door is arguably not a rule so much as the thing that makes two rooms a dungeon, but it is a new mechanical concept and it wants the content that justifies it. Likely belongs with the generator plan, not before it.\n\nSketch, not a design: Exit gains a closed state; crossExit refuses when shut and says so in its own words; something opens it (a check, a prop, a key as an item concept that does not exist yet). The client already ships SceneState.blocked for squares — an unusable exit wants the same treatment, decided server-side.","acceptance_criteria":"Not ready to build. Decide first whether a locked exit belongs before the generator or with it.","notes":"PULLED INTO M4 2026-09-08. No blocking edge on purpose — the relationship with 4h9.8 runs the other\nway and is conditional.\n\n4h9.8 (author the site): \"If a door in this site is meant to be shut, it needs emberdelve-ql3 behind\nit. If it is not, its description must not say so.\" So authoring the site DECIDES whether this is\nneeded. A hard dependency would say the site cannot be authored until doors can lock, which is not\ntrue and would make `bd ready` lie.\n\nIf 4h9.8 authors a shut door, close this first. If it authors none, this stays open past M4.","status":"open","priority":3,"issue_type":"feature","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T20:25:30Z","created_by":"hampton.black","updated_at":"2026-09-08T23:26:18Z","dependencies":[{"issue_id":"emberdelve-ql3","depends_on_id":"emberdelve-4h9","type":"parent-child","created_at":"2026-09-08T18:26:18Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-xgg.9","title":"coveredWalls can name a room that is not on the wire","description":"GameEngine.coveredWallsOf() reads Rooms.coveredWalls(), which is computed over every room the entrance can reach. GameEngine.knownRoomIds() ships a subset — rooms the party has visited, plus rooms visible through one of the current room's exits. So a shipped room can be told to give up a segment to a room that is not on the wire at all, and the client draws a gap where nobody draws stone. A hole in a wall is a way out.\n\nUnreachable in M3's two authored rooms, and unreachable in a chain too: a room is only shipped when it is visited or adjacent to the current room, and in both cases the room nearer the entrance is shipped as well. It stops being unreachable the moment the generator makes a dungeon with more than one path between rooms, because then the covering room need not be on any path the party has taken.\n\nFound by the spec review of emberdelve-xgg.7.","design":"Two candidate fixes, neither obviously right yet:\n\n1. Compute coveredWalls over the shipped set rather than every placed room. Correct by construction, but ownership then changes as rooms come into view, which is exactly the party-relative rule ADR-0010 threw out.\n2. Ship the covering room whenever it covers something shipped. Keeps ownership stable, widens what the client is told exists — and RoomView already withholds an unvisited room's props, so a room drawn BLACK costs nothing but geometry.\n\nOption 2 looks closer to ADR-0010's intent. Decide it against a real generated dungeon rather than on paper.","acceptance_criteria":"A room shipped in SceneState.rooms is never told to give up a segment to a room absent from that list. Proven on a synthetic layout where the covering room is neither visited nor adjacent to the current room.","notes":"ADR-0010 is the rule; ADR-0011 (visible is not addressable) is the constraint on widening what is shipped.","status":"open","priority":3,"issue_type":"bug","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T03:03:15Z","created_by":"hampton.black","updated_at":"2026-09-07T03:03:15Z","dependencies":[{"issue_id":"emberdelve-xgg.9","depends_on_id":"emberdelve-xgg","type":"parent-child","created_at":"2026-09-06T22:03:15Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-j1u","title":"Give world.gd one room table instead of two parallel dictionaries","description":"_origins and _sizes in world.gd are two dictionaries keyed by the same room id,\nand the world-frame work made them travel together everywhere: register_room()\nwrites both, grid_to_world and world_to_grid read both, _covers reads both\nthrough _size_of, and room_at iterates _origins while resolving size through\n_sizes. A small per-room value — origin and size together — wants to be born.\n\nReplace the pair with one dictionary of a small typed inner class (GDScript has\nno records; an inner class keeps it typed where a nested Dictionary would not).\n\nLatent, not live. register_room is the only writer and writes both keys in the\nsame call, so the two cannot currently diverge. This removes a bug class, not a\nbug — which is why it is P3 and why it should land AFTER the played verification\nof emberdelve-xgg rather than before.","design":"THE TRAP, and the reason this is worth writing down rather than just doing.\n\nThe asymmetry is real: room_at iterates _origins, and the size it bounds-checks\nagainst comes from _size_of, which FALLS BACK to the current room's dimensions\nfor an unknown id. So a room present in _origins and absent from _sizes would be\ntested against the wrong rectangle and return a confident wrong answer — no\nerror, no empty result, just a click resolving to the wrong room. That is the\nproject's characteristic silent failure, and it is exactly the failure two\nparallel dictionaries invite.\n\nThe wrong fix is to delete that fallback. It is deliberate and documented in\ntwo places: _size_of and _covers both say why. A current room the server has\nsomehow not placed stays clickable at the origin, making the same guess\ngrid_to_world makes, because refusing it there would leave a room drawn on\nscreen with every click on it dropped without a word. grid_to_world and\nworld_to_grid have to make that guess identically or they stop being inverses.\n\nSo: one dictionary so the keys cannot diverge, fallback preserved exactly as it\nis. The fallback stops being reachable-by-accident and stays reachable-by-design\nfor the unplaced-room case it exists for.\n\nSequencing: after emberdelve-xgg.7 and after the played verification. This\ntouches the placement and picking path, which is precisely what that session is\nchecking. Refactoring it first means two candidate causes for anything that\nlooks off in play, for no observable gain.\n\nSource: code review of emberdelve-xgg.5, 2026-09-06.","acceptance_criteria":"- world.gd holds one room table, not two dictionaries keyed by room id.\n- register_room's signature still takes size and origin together (its callers,\n  including three in test_world.gd, are the check that they belong together).\n- _size_of's fallback to the current room's dimensions is preserved, and\n  grid_to_world / world_to_grid remain exact inverses under it.\n- room_at no longer iterates one structure and resolves through another.\n- godot suite green; no behaviour change in play.","status":"open","priority":3,"issue_type":"chore","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T02:41:58Z","created_by":"hampton.black","updated_at":"2026-09-07T02:41:58Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-v5o","title":"Extract playtest findings from AGENTS.md into their own doc","description":"AGENTS.md is 772 lines; its Voice section and subsections are 183 of them (Measured and worth keeping, Combat narration, The opening, Since fixed, Known unfixed, Carried out of M2, Carried out of M3). These are playtest findings, not rules — they are observations with dates, and they keep growing one milestone at a time. Moving them to docs/playtest-findings.md would cut AGENTS.md by roughly a quarter and give the findings somewhere to accumulate without diluting the rules. Needs a judgement call about which findings have hardened into rules and should stay behind.","status":"open","priority":3,"issue_type":"task","created_at":"2026-09-06T17:55:21Z","updated_at":"2026-09-06T17:55:21Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-drf","title":"Assess whether AGENTS.md can be stripped now that ADRs exist","description":"AGENTS.md is 841 lines and is the operational document. With CONTEXT.md and seven ADRs in place, some of it is duplicated. Identify what genuinely moved versus what only looks duplicated, and cut only where the ADR is a strict superset.","status":"closed","priority":3,"issue_type":"task","assignee":"hampton.black","created_at":"2026-09-06T17:52:21Z","updated_at":"2026-09-06T17:55:20Z","started_at":"2026-09-06T17:52:21Z","closed_at":"2026-09-06T17:55:20Z","close_reason":"ADR-0003 amended with the gemini-3-8-flash/low switch; ADR-0008 added for the pick evidence; AGENTS.md model sections compressed 108 -\u003e 39 lines with every fact preserved","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-l13","title":"Remove stale 'client' entry from .claude/launch.json","description":"The 'client' configuration in .claude/launch.json runs 'npm run dev' in a cwd of 'client' on port 5173. That directory no longer exists — the Vite/Three.js client was replaced by Godot on 2026-08-23 (ADR-0006). The entry will fail if anyone selects it. Either delete it or replace it with a Godot launch entry.","status":"closed","priority":3,"issue_type":"chore","assignee":"hampton.black","created_at":"2026-09-06T17:35:18Z","updated_at":"2026-09-06T18:45:47Z","started_at":"2026-09-06T18:39:41Z","closed_at":"2026-09-06T18:45:47Z","close_reason":"Deleted the client entry. The Vite/Three.js client it pointed at was replaced by Godot (ADR-0006); no Godot replacement entry was added because launch.json feeds the browser preview pane and Godot is a native desktop client with nothing to serve.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"emberdelve-xgg.12","title":"A click crossing narrates nothing, and arms a stale arrival directive","description":"RECORDED, NOT SCHEDULED. Play says this is fine — see the note at the end before working on it.\n\nWsHandler.enterExit (WsHandler.java:118) sends the new Scene, calls dm.noteCrossing, and stops. noteCrossing arms pendingArrival, but DmService.takePendingArrival (DmService.java:934) is only consumed by a free-text prose phase, which the click path never runs. So crossing by clicking the door produces no narration at all.\n\nPlaytest 2026-09-07: 15 of 17 crossings across the two sessions were silent. Only the two typed ones ('I go through the doorway', 'I enter the doorway') got an arrival line.\n\nSecondary, and the part that is arguably wrong regardless: the armed directive is not discarded. It waits and attaches to whatever the player types next, which may be several crossings later. Session 2 clicked gallery-\u003ecrypt-\u003egallery at 19:59:27 and 19:59:30; the stale 'you have come back' directive landed on a key-searching turn 16 seconds afterwards. Repeated clicks also push a threshold marker into history each time with no reply between them.\n\nWHY THIS IS P4. The DmService.pendingArrival javadoc says the silence is deliberate. The player who ran both sessions confirmed it did not bother them: clicking a door and walking through is the honest path, and narrating it is not what was missing. What was missing was knowing whether the door could be crossed at all — that is emberdelve-xgg.10, now fixed.\n\nIf it is ever wanted, it is small: a narrateArrival(TurnSink) modelled on openScene (DmService.java:177) with takePendingArrival() as the directive and tryLock rather than lock, plus one submit in enterExit. turnSink(ctx) already exists. The stale-directive half could be fixed on its own without adding any narration.","status":"open","priority":4,"issue_type":"bug","owner":"hampton.j.black@gmail.com","created_at":"2026-09-07T20:26:11Z","created_by":"hampton.black","updated_at":"2026-09-07T20:26:11Z","dependencies":[{"issue_id":"emberdelve-xgg.12","depends_on_id":"emberdelve-xgg","type":"parent-child","created_at":"2026-09-07T15:26:10Z","created_by":"hampton.black","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
